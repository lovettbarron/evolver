---
phase: 9
slug: patch-documentation-demo-patches
status: approved
shadcn_initialized: false
preset: none
created: 2026-03-31
revised: 2026-03-31
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for Cascadia patch documentation pages. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom components) |
| Icon library | lucide-react |
| Font | Inter (sans), JetBrains Mono (mono) |

Source: Existing project globals.css and package.json. No shadcn — project uses Tailwind v4 with `@theme` custom properties.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, tag gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, content margins |
| xl | 32px | Layout gaps, Mermaid diagram vertical margin |
| 2xl | 48px | Page vertical padding |
| 3xl | 64px | Not used this phase |

Exceptions: none

Source: Existing `--spacing-*` tokens in globals.css. No new tokens needed.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.6 |
| Label / Table Header / Secondary Text | 13px | 700 | 1.4 |
| Module Group Heading | 20px | 700 | 1.2 |
| Patch Title (h1) | 24px | 700 | 1.2 |

4 sizes total: 13px, 16px, 20px, 24px. 2 weights total: 400, 700. No other sizes or weights permitted in this phase.

Source: Existing `.prose` styles in globals.css. Body at 1.6 line-height is the established project convention (not 1.5).

Additional rules for this phase:
- Cable routing values (source/destination) render in `font-mono` at 13px, weight 400, color `--color-param` (#a3e635) -- matches existing parameter value treatment
- Knob position values render in `font-mono` at 13px, weight 400, color `--color-param` -- same treatment as Evolver parameter tables
- Clock position strings ("2 o'clock", "full CW") render as plain text, not code -- they are human-readable descriptions
- Module group headings in knob settings use `.prose h3` styling: 20px, weight 700, line-height 1.2
- Mermaid edge labels: 13px, `--color-muted` (diagram library renders at this size via theme config)
- Purpose text below cable rows: 13px, `--color-muted`, `font-sans`
- Audio preview placeholder text: 13px, `--color-muted`
- "Rest at default" notes: 13px italic, `--color-muted`

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #0a0a0a (`--color-bg`) | Page background, content area |
| Secondary (30%) | #161616 (`--color-surface`) | Cards, table row alternation, Mermaid diagram background, cable routing list items |
| Accent (10%) | #c8ff00 (`--color-accent`) | Patch type badge, "View Diagram" toggle active state, cable count badge, audio preview placeholder icon |
| Muted | #737373 (`--color-muted`) | Table headers, secondary labels, "rest at default" notes, Mermaid connection lines |
| Param | #a3e635 (`--color-param`) | Cable source/destination labels, knob setting values, module names in Mermaid nodes |

Accent reserved for: patch type badge text, active toggle indicator on cable diagram view, cable count number, audio preview placeholder icon. Never for body text, headings, or table borders.

Source: Existing color tokens in globals.css. No new colors introduced. `--color-param` already used for parameter values in Evolver patches -- reused for consistency.

---

## Component Inventory

### New Components (this phase)

| Component | Purpose | Renders When |
|-----------|---------|--------------|
| `CableRoutingList` | Renders cable connections as a styled list with source, destination, purpose | Cascadia patch has `cable_routing` array with 1+ entries |
| `CableRoutingDiagram` | Auto-generates and renders Mermaid flowchart from `cable_routing` data | User toggles "Show Diagram" on a Cascadia patch with 2+ cables |
| `KnobSettingsTable` | Renders knob/slider positions grouped by module as tables | Cascadia patch has `knob_settings` map with 1+ modules |
| `AudioPreviewPlaceholder` | Shows "Audio preview not yet recorded" with speaker icon | Cascadia patch has `audio_preview` field (present or absent) |

### Modified Components (this phase)

| Component | Change |
|-----------|--------|
| `PatchDetail` | Add conditional branch: if instrument has `sysex: false` and `cable_routing` exists, render CableRoutingList + KnobSettingsTable instead of prose parameter tables |
| `PatchCard` | Add cable count badge for Cascadia patches: "{N} cables" in muted text next to type badge |

### Existing Components (no changes)

| Component | Reason |
|-----------|--------|
| `MermaidRenderer` | Already handles `.mermaid-placeholder` elements -- CableRoutingDiagram generates compatible markup |
| `PatchGrid` | Patch cards already render instrument-agnostic -- no changes needed |

---

## Layout Contract

### Primary Focal Point

On the Cascadia patch detail page, the **patch title (h1)** is the primary visual anchor. It is the largest text element on the page (24px, weight 700) and appears immediately below the sticky header. The type badge sits inline to its right at 13px, providing context without competing for attention. All other content flows below in reading order.

### CableRoutingList Layout

```
[Cable Routing]                          <- h2, 24px bold
[N cables]                               <- accent badge, 13px

+------------------------------------------+
| Source Label  -->  Destination Label      |  <- one row per cable
| Purpose description                      |  <- muted text below
+------------------------------------------+
| Source Label  -->  Destination Label      |
| Purpose description                      |
+------------------------------------------+
```

- Each cable is a `surface` background row with `lg` (24px) padding
- Source and destination in `font-mono`, `--color-param`, 13px
- Arrow separator: literal " --> " in `--color-muted`
- Purpose text below in 13px, `--color-muted`, `font-sans`
- Gap between cable rows: `sm` (8px)
- Border-radius on each row: 6px (matches existing card rounding)

### CableRoutingDiagram Layout

- Toggle button: "Show Diagram" / "Hide Diagram" text button, no fill, `--color-accent` text, positioned right-aligned below CableRoutingList header
- Mermaid graph direction: `LR` (left to right) -- signal flows left to right matching conventional synth signal flow
- Node styling: module names as rounded rectangle nodes, `--color-surface` fill, `--color-text` label
- Edge labels: purpose text on each connection line, `--color-muted`, 13px
- Container: `.mermaid-placeholder` class for pre-hydration state (reuses existing CSS)
- Max width: 100% of content area (720px), horizontal scroll if diagram exceeds

### KnobSettingsTable Layout

```
[Knob Settings]                          <- h2, 24px bold

[Module Name]                            <- h3, 20px bold
+------------------+---------------------+
| Control          | Value               |
+------------------+---------------------+
| Knob name        | 2 o'clock           |
| Switch name      | LP                  |
+------------------+---------------------+
| Rest at default                        |  <- muted italic note
                                            (only if module has
                                             omitted controls)

[Module Name]                            <- next module group
+------------------+---------------------+
...
```

- Reuses existing `.prose table` and `.param-table` CSS classes
- Control column: `font-sans`, `--color-text`
- Value column: `font-mono`, `--color-param`, 13px
- "Rest at default" note: 13px italic, `--color-muted`, below table with `sm` (8px) top margin
- Module groups separated by `lg` (24px) vertical spacing

### AudioPreviewPlaceholder Layout

```
+------------------------------------------+
| [speaker-icon]  Audio preview not yet    |  <- surface bg, muted text
|                 recorded                 |
+------------------------------------------+
```

- Full-width container, `--color-surface` background, 6px border-radius
- Icon: `lucide-react` `Volume2` icon, 20px, `--color-accent`
- Text: 13px, `--color-muted`
- Padding: `md` (16px) all sides
- Positioned after patch description, before cable routing section

### Patch Detail Page Order (Cascadia)

1. Sticky header (existing -- "Patches" back link, patch name)
2. **Patch title (h1) + type badge + "standalone" label -- PRIMARY FOCAL POINT** (no session origin)
3. Audio preview placeholder (if `audio_preview` field exists)
4. Patch description (markdown body prose)
5. Cable Routing section (CableRoutingList + optional CableRoutingDiagram toggle)
6. Knob Settings section (KnobSettingsTable, grouped by module)

---

## Mermaid Diagram Contract

Auto-generated from `cable_routing` array. Each cable becomes an edge.

### Graph Template

```
graph LR
  ModuleA["Module A Name"] -->|purpose text| ModuleB["Module B Name"]
  ModuleA -->|purpose text| ModuleC["Module C Name"]
```

### Node Extraction Rules

- Extract module name from source/destination label: "VCO-A Saw Out" becomes node `VCOA["VCO-A"]`
- Node IDs: strip spaces and special characters from module name
- Node labels: human-readable module name in quotes
- Deduplicate nodes: if multiple cables reference the same module, reuse the node

### Edge Rules

- Edge label: the `purpose` field from cable_routing entry
- Direction: source node --> destination node
- If purpose is empty string, render edge without label

### Styling Overrides

None -- use existing MermaidRenderer dark theme configuration (`primaryColor: '#161616'`, `primaryTextColor: '#e8e8e8'`, `lineColor: '#737373'`).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Explore Patches" (on instrument overview, links to patch list) |
| Cable routing section heading | "Cable Routing" |
| Cable count badge | "{N} cables" (e.g. "4 cables") |
| Knob settings section heading | "Knob Settings" |
| Rest at default note | "All other controls at default" |
| Audio preview placeholder | "Audio preview not yet recorded" |
| Empty state heading (no Cascadia patches) | "No patches yet" |
| Empty state body | "Cascadia demo patches are coming soon. Check back after the next update." |
| Diagram toggle (show) | "Show Diagram" |
| Diagram toggle (hide) | "Hide Diagram" |
| Standalone patch label | "Standalone" (replaces session origin when `session_origin: null`) |

No destructive actions in this phase. All operations are read-only (browsing patches).

---

## Interaction Contract

### Cable Diagram Toggle

- Default state: diagram hidden (list view only)
- Toggle is a text button, not a switch or checkbox
- Toggle position: right-aligned next to "Cable Routing" heading
- State is local to the component (no URL param, no localStorage)
- Transition: diagram fades in with CSS `opacity` transition, 150ms ease

### Patch Type Filter (existing)

- Existing filter pills on patch list page must include new `fx` type
- No other filter changes needed

### Mermaid Hydration

- Server renders `.mermaid-placeholder` div with `data-chart` attribute containing the generated Mermaid source
- Existing `MermaidRenderer` client component hydrates on mount
- No changes to hydration mechanism

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No registries | none | not applicable |

No shadcn. No third-party registries. All components are hand-built with Tailwind v4 utility classes and the project's existing CSS custom properties.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-03-31
