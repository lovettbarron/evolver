---
phase: 01-content-pipeline-curriculum
plan: 06
type: execute
wave: 5
depends_on: ["05", "02"]
files_modified:
  - src/content/instruments/evolver/overview.md
  - src/content/instruments/evolver/signal-flow.md
  - src/content/instruments/evolver/basic-patch.md
  - src/content/instruments/evolver/modules.md
  - src/content/sessions/evolver/
  - src/content/patches/evolver/README.md
  - src/content/references/
  - scripts/bundle-content.ts
  - src/lib/content/__tests__/curriculum.test.ts
autonomous: true
requirements:
  - PIPE-05
  - PIPE-02

must_haves:
  truths:
    - "src/content/ directory mirrors the vault structure exactly: instruments/, sessions/, patches/"
    - "All 35 session files are bundled in src/content/sessions/evolver/"
    - "All 4 instrument files are bundled in src/content/instruments/evolver/"
    - "Reference PDFs are accessible from src/content/references/ (symlinked or copied)"
    - "Content reader in bundled mode (no vaultPath) can read all bundled content"
    - "validate-content script passes when run against src/content/"
    - "Curriculum integration tests verify session count, structure, and ADHD compliance"
  artifacts:
    - path: "src/content/sessions/evolver/"
      provides: "Bundled session files for demo mode"
      min_lines: 35
    - path: "src/content/instruments/evolver/"
      provides: "Bundled instrument docs for demo mode"
      min_lines: 4
    - path: "src/content/references/"
      provides: "Reference PDFs accessible for demo/local app"
    - path: "scripts/bundle-content.ts"
      provides: "Script to copy content from project root to src/content/"
      contains: "copyFileSync"
    - path: "src/lib/content/__tests__/curriculum.test.ts"
      provides: "Integration tests for curriculum completeness"
      contains: "session count"
  key_links:
    - from: "src/content/"
      to: "instruments/, sessions/, patches/"
      via: "mirrored directory structure"
      pattern: "instruments/evolver|sessions/evolver|patches/evolver"
    - from: "src/lib/content/reader.ts"
      to: "src/content/"
      via: "getContentRoot returns src/content/ in demo mode"
      pattern: "src/content"
---

<objective>
Populate the bundled content directory (src/content/) with the complete curriculum, instrument docs, and reference PDFs. Create a bundle script for repeatable content sync. Write curriculum integration tests that verify session count, structure, and ADHD compliance.

Purpose: Without src/content/ populated, PIPE-02 (bundled fallback) has no content to serve, and PIPE-05 (reference PDFs) has no serving path. This plan wires the authored curriculum into the content pipeline.
Output: Populated src/content/ directory, bundle script, curriculum integration tests.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-pipeline-curriculum/01-CONTEXT.md
@.planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
@.planning/phases/01-content-pipeline-curriculum/01-05-SUMMARY.md
@.planning/phases/01-content-pipeline-curriculum/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bundle-content script and populate src/content/</name>
  <files>scripts/bundle-content.ts, src/content/instruments/evolver/, src/content/sessions/evolver/, src/content/patches/evolver/README.md, src/content/references/</files>
  <read_first>
    src/lib/content/reader.ts
    src/lib/config.ts
    .planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
    package.json
  </read_first>
  <action>
1. Create scripts/bundle-content.ts:
   - A script that copies content from project root directories to src/content/
   - Copy instruments/ -> src/content/instruments/
   - Copy sessions/ -> src/content/sessions/
   - Copy patches/ -> src/content/patches/ (create patches/evolver/README.md if no patches exist yet)
   - Symlink or copy references/ -> src/content/references/ (per RESEARCH.md recommendation)
   - Use fs.cpSync for recursive directory copies
   - Print summary of files copied
   - Add "bundle-content" script to package.json: "tsx scripts/bundle-content.ts"

2. Run the bundle script: `npx tsx scripts/bundle-content.ts`

3. Verify the bundled content structure mirrors the source:
   - src/content/instruments/evolver/ has overview.md, signal-flow.md, basic-patch.md, modules.md
   - src/content/sessions/evolver/ has all 35 session files
   - src/content/patches/evolver/ has at least README.md
   - src/content/references/ has the PDF files (or symlinks to them)

4. Create src/content/patches/evolver/README.md with frontmatter-free explanation that patches will be created during sessions

5. Run validate-content against bundled content: `npx tsx scripts/validate-content.ts src/content`
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx tsx scripts/bundle-content.ts && ls src/content/sessions/evolver/ | wc -l && ls src/content/instruments/evolver/ | wc -l && ls src/content/references/ 2>&1 | head -5 && npx tsx scripts/validate-content.ts src/content</automated>
  </verify>
  <acceptance_criteria>
    - scripts/bundle-content.ts exists and runs without errors
    - src/content/sessions/evolver/ contains 35 session markdown files
    - src/content/instruments/evolver/ contains 4 instrument doc files
    - src/content/references/ contains reference PDFs (or symlinks)
    - src/content/patches/evolver/README.md exists
    - validate-content passes against src/content/
    - package.json contains "bundle-content" script
  </acceptance_criteria>
  <done>Bundle script creates src/content/ with complete curriculum, instrument docs, and reference PDFs. Validate-content passes against bundled content. Content reader can serve all content in demo mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curriculum integration tests</name>
  <files>src/lib/content/__tests__/curriculum.test.ts</files>
  <read_first>
    src/lib/content/reader.ts
    src/lib/content/schemas.ts
    instruments/evolver/modules.md
    framework/adhd-design.md
    .planning/phases/01-content-pipeline-curriculum/01-VALIDATION.md
  </read_first>
  <behavior>
    - "session count": at least 30 session files exist in src/content/sessions/evolver/
    - "session structure": each session contains ## Warm-Up, ## Setup, ## Exercises, ## Output Checklist sections
    - "adhd": all session durations are between 5 and 30 (from frontmatter), every exercise has parameter values (grep for backtick-wrapped values)
    - "basic-patch": src/content/instruments/evolver/basic-patch.md exists and has valid InstrumentFileSchema frontmatter with type: basic-patch
    - "instrument-docs": all 4 instrument docs exist (overview, signal-flow, basic-patch, modules) with valid frontmatter
    - "pdf-access": src/content/references/ directory exists and contains at least 1 PDF file
  </behavior>
  <action>
1. Create src/lib/content/__tests__/curriculum.test.ts:

   describe("session count"):
   - Read src/content/sessions/evolver/ directory
   - Assert at least 30 .md files exist (CONTEXT.md: "Target ~35 sessions but quality over hitting exact count")

   describe("session structure"):
   - For each session file, read content and verify it contains required sections:
     - "## Warm-Up" or "## Warm-up"
     - "## Setup"
     - "## Exercises" or "### Exercise"
     - "## Output Checklist"
   - Assert all sessions have these sections

   describe("adhd"):
   - For each session, parse frontmatter with gray-matter
   - Assert duration >= 5 and duration <= 30
   - Assert output_type is one of: patch, technique, recording, composition

   describe("basic-patch"):
   - Read src/content/instruments/evolver/basic-patch.md
   - Parse frontmatter and validate against InstrumentFileSchema
   - Assert type is "basic-patch"
   - Assert content contains at least 20 parameter table rows (| ... | ... |)

   describe("instrument-docs"):
   - Assert all 4 files exist: overview.md, signal-flow.md, basic-patch.md, modules.md
   - Each passes InstrumentFileSchema validation
   - Each has the correct type field

   describe("pdf-access"):
   - Assert src/content/references/ directory exists
   - Assert it contains at least 1 file ending in .pdf

2. Run: `npx vitest run src/lib/content/__tests__/curriculum.test.ts`
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx vitest run src/lib/content/__tests__/curriculum.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/content/__tests__/curriculum.test.ts exists
    - Contains describe blocks for: "session count", "session structure", "adhd", "basic-patch", "instrument-docs", "pdf-access"
    - All tests pass
    - Tests validate against actual bundled content in src/content/
  </acceptance_criteria>
  <done>Curriculum integration tests verify: session count >= 30, session structure includes all required sections, ADHD compliance (duration, output types), basic patch completeness, instrument doc validity, and PDF accessibility. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/content/__tests__/curriculum.test.ts` -- all integration tests pass
- `npx tsx scripts/validate-content.ts src/content` -- all content validates
- Content reader in demo mode (no vaultPath) reads from src/content/ successfully
- Reference PDFs accessible at src/content/references/
</verification>

<success_criteria>
- PIPE-02 strengthened: bundled content directory populated, content reader has content to serve
- PIPE-05 addressed: reference PDFs accessible at src/content/references/
- Curriculum integration tests verify all CURR requirements
- Bundle script enables repeatable content sync
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-pipeline-curriculum/01-06-SUMMARY.md`
</output>
