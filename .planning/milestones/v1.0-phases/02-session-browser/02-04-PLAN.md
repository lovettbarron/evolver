---
phase: 02-session-browser
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mermaid-renderer.tsx
  - src/components/instrument-overview.tsx
autonomous: true
requirements: [INST-02]
gap_closure: true

must_haves:
  truths:
    - "Mermaid diagrams render as SVG on session detail pages"
    - "Mermaid diagrams render as SVG on the instrument overview page"
  artifacts:
    - path: "src/components/mermaid-renderer.tsx"
      provides: "Client-side Mermaid diagram rendering"
      contains: "data-chart"
    - path: "src/components/instrument-overview.tsx"
      provides: "Instrument overview with Mermaid support"
      contains: "MermaidRenderer"
  key_links:
    - from: "src/components/mermaid-renderer.tsx"
      to: "mermaid-placeholder.ts output"
      via: "data-chart attribute match"
      pattern: "getAttribute\\('data-chart'\\)"
    - from: "src/components/instrument-overview.tsx"
      to: "src/components/mermaid-renderer.tsx"
      via: "dynamic import"
      pattern: "dynamic.*mermaid-renderer"
---

<objective>
Fix Mermaid diagram rendering across all pages.

Purpose: The mermaid-placeholder rehype plugin emits `data-chart` on placeholder divs, but MermaidRenderer reads `data-diagram`. This one-word mismatch silently prevents all Mermaid diagrams from rendering. Additionally, the instrument overview page is missing the MermaidRenderer component entirely.

Output: Mermaid diagrams render as SVG on both session detail and instrument overview pages.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-session-browser/02-VERIFICATION.md

<interfaces>
<!-- The rehype plugin emits this attribute on placeholder divs -->
From src/lib/markdown/plugins/mermaid-placeholder.ts:
```typescript
// Line 39: the attribute name emitted by the processor
'data-chart': chartContent,
```

<!-- session-detail.tsx already wires MermaidRenderer correctly via dynamic import -->
From src/components/session-detail.tsx:
```typescript
const MermaidRenderer = dynamic(
  () => import('@/components/mermaid-renderer').then((m) => ({ default: m.MermaidRenderer })),
  { ssr: false },
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Mermaid data attribute mismatch and add MermaidRenderer to instrument overview</name>
  <files>src/components/mermaid-renderer.tsx, src/components/instrument-overview.tsx</files>
  <read_first>
    - src/components/mermaid-renderer.tsx (current code reads data-diagram on line 20)
    - src/lib/markdown/plugins/mermaid-placeholder.ts (emits data-chart on line 39)
    - src/components/instrument-overview.tsx (missing MermaidRenderer)
    - src/components/session-detail.tsx (reference implementation of MermaidRenderer wiring)
  </read_first>
  <action>
Two changes in this task:

**Fix 1: mermaid-renderer.tsx line 20**
Change `el.getAttribute('data-diagram')` to `el.getAttribute('data-chart')`. This aligns the renderer with what the rehype plugin actually emits. The full line becomes:
```typescript
const diagram = el.getAttribute('data-chart');
```

**Fix 2: instrument-overview.tsx**
This component renders `signalFlowHtml` via `dangerouslySetInnerHTML` but does not include the MermaidRenderer client component, so Mermaid placeholder divs in signal flow content are never hydrated.

Add these changes to instrument-overview.tsx:
1. Add `'use client';` directive at the top of the file (required for `next/dynamic` with `ssr: false`)
2. Add dynamic import of MermaidRenderer, matching the pattern from session-detail.tsx:
```typescript
import dynamic from 'next/dynamic';

const MermaidRenderer = dynamic(
  () => import('@/components/mermaid-renderer').then((m) => ({ default: m.MermaidRenderer })),
  { ssr: false },
);
```
3. Render `<MermaidRenderer />` immediately after the signal flow `div` that uses `dangerouslySetInnerHTML={{ __html: signalFlowHtml }}`, inside the `{signalFlowHtml && (...)}` conditional block.

Note: Adding `'use client'` to instrument-overview.tsx means its parent (the instrument page server component at `src/app/instruments/[slug]/page.tsx`) will still be a server component that passes serializable props down. This matches the session-detail.tsx pattern exactly.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && grep -n "data-chart" src/components/mermaid-renderer.tsx && grep -n "MermaidRenderer" src/components/instrument-overview.tsx && grep -n "'use client'" src/components/instrument-overview.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mermaid-renderer.tsx contains `el.getAttribute('data-chart')` and does NOT contain `data-diagram`
    - instrument-overview.tsx contains `'use client'` as the first line
    - instrument-overview.tsx contains `import dynamic from 'next/dynamic'`
    - instrument-overview.tsx contains `const MermaidRenderer = dynamic(`
    - instrument-overview.tsx contains `<MermaidRenderer />` inside the signalFlowHtml conditional section
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Mermaid diagrams can render on all pages: the data attribute matches what the rehype plugin emits, and both session-detail and instrument-overview include the MermaidRenderer client component.</done>
</task>

</tasks>

<verification>
1. `grep 'data-chart' src/components/mermaid-renderer.tsx` returns a match
2. `grep 'data-diagram' src/components/mermaid-renderer.tsx` returns NO match
3. `grep 'MermaidRenderer' src/components/instrument-overview.tsx` returns a match
4. `npx next build` succeeds
</verification>

<success_criteria>
- The data attribute in MermaidRenderer matches `data-chart` from the rehype plugin
- Instrument overview page includes MermaidRenderer via dynamic import with ssr:false
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-browser/02-04-SUMMARY.md`
</output>
