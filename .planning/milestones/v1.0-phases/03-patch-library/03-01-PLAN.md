---
phase: 03-patch-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/patches/evolver/sub-bass.md
  - src/content/patches/evolver/acid-bass.md
  - src/content/patches/evolver/fm-bass.md
  - src/content/patches/evolver/sync-lead.md
  - src/content/patches/evolver/fm-lead.md
  - src/content/patches/evolver/filtered-lead.md
  - src/content/patches/evolver/pwm-pad.md
  - src/content/patches/evolver/waveshape-pad.md
  - src/content/patches/evolver/modulated-texture.md
  - src/content/patches/evolver/analog-kick.md
  - src/content/patches/evolver/noise-snare.md
  - src/content/patches/evolver/fm-hat.md
  - src/content/patches/evolver/drum-beat.md
  - src/content/patches/evolver/pwm-sweep.md
  - src/content/patches/evolver/hard-sync-sweep.md
  - src/content/patches/evolver/self-osc-drone.md
  - src/lib/patches.ts
autonomous: true
requirements:
  - PTCH-01
  - PTCH-02

must_haves:
  truths:
    - "At least 15 patch markdown files exist with valid PatchSchema frontmatter"
    - "Patches cover all 6 types: bass, lead, pad, drum, texture, sequence"
    - "Every patch has a session_origin linking to an existing session number"
    - "groupByType and getAvailableTypes functions exist for filtering"
  artifacts:
    - path: "src/content/patches/evolver/sub-bass.md"
      provides: "Demo bass patch"
      contains: "type: bass"
    - path: "src/content/patches/evolver/sync-lead.md"
      provides: "Demo lead patch"
      contains: "type: lead"
    - path: "src/content/patches/evolver/pwm-pad.md"
      provides: "Demo pad patch"
      contains: "type: pad"
    - path: "src/content/patches/evolver/analog-kick.md"
      provides: "Demo drum patch"
      contains: "type: drum"
    - path: "src/content/patches/evolver/modulated-texture.md"
      provides: "Demo texture patch"
      contains: "type: texture"
    - path: "src/content/patches/evolver/drum-beat.md"
      provides: "Demo sequence patch"
      contains: "type: sequence"
    - path: "src/lib/patches.ts"
      provides: "Patch grouping and filtering utilities"
      exports: ["groupByType", "getAvailableTypes", "PatchWithMeta", "PatchTypeGroup"]
  key_links:
    - from: "src/content/patches/evolver/*.md"
      to: "src/lib/content/schemas.ts"
      via: "PatchSchema validation at parse time"
      pattern: "type: (bass|lead|pad|drum|texture|sequence)"
    - from: "src/lib/patches.ts"
      to: "src/lib/content/types.ts"
      via: "Patch type import"
      pattern: "import.*Patch.*from.*types"
---

<objective>
Create 16 demo patch content files with curriculum-sourced parameter values and build the patch helper utility library for grouping/filtering.

Purpose: Populate the patch library with authentic content from sessions 4, 5, 13, 27-30, and provide the data utilities the UI needs for grouping and filtering patches by type.
Output: 16 patch markdown files in src/content/patches/evolver/, plus src/lib/patches.ts utility module.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-patch-library/03-CONTEXT.md
@.planning/phases/03-patch-library/03-RESEARCH.md
@.planning/phases/03-patch-library/03-UI-SPEC.md

<interfaces>
<!-- PatchSchema from src/lib/content/schemas.ts -->
```typescript
export const PatchSchema = z.object({
  name: z.string(),
  type: z.enum(['bass', 'lead', 'pad', 'drum', 'texture', 'sequence']),
  session_origin: z.union([z.number(), z.null()]),
  description: z.string(),
  tags: z.array(z.string()),
  instrument: z.string(),
  created: z.string(),
}).passthrough();

export type Patch = z.infer<typeof PatchSchema>;
```

<!-- Patch type from src/lib/content/types.ts -->
```typescript
export type { Patch } from './schemas.js';
```

<!-- listPatches from src/lib/content/reader.ts -->
```typescript
export async function listPatches(
  instrument: string,
  config: AppConfig,
): Promise<Array<{ data: Patch; content: string; slug: string }>>;
```

<!-- SessionWithMeta pattern from src/lib/sessions.ts -->
```typescript
export interface SessionWithMeta {
  data: Session;
  content: string;
  slug: string;
}

export interface ModuleGroup {
  module: string;
  sessions: SessionWithMeta[];
}

export function groupByModule(sessions: SessionWithMeta[]): ModuleGroup[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 16 demo patch content files</name>
  <files>
    src/content/patches/evolver/sub-bass.md,
    src/content/patches/evolver/acid-bass.md,
    src/content/patches/evolver/fm-bass.md,
    src/content/patches/evolver/sync-lead.md,
    src/content/patches/evolver/fm-lead.md,
    src/content/patches/evolver/filtered-lead.md,
    src/content/patches/evolver/pwm-pad.md,
    src/content/patches/evolver/waveshape-pad.md,
    src/content/patches/evolver/modulated-texture.md,
    src/content/patches/evolver/analog-kick.md,
    src/content/patches/evolver/noise-snare.md,
    src/content/patches/evolver/fm-hat.md,
    src/content/patches/evolver/drum-beat.md,
    src/content/patches/evolver/pwm-sweep.md,
    src/content/patches/evolver/hard-sync-sweep.md,
    src/content/patches/evolver/self-osc-drone.md
  </files>
  <read_first>
    src/lib/content/schemas.ts,
    src/content/patches/evolver/README.md,
    sessions/evolver/27-recipes-bass.md,
    sessions/evolver/28-recipes-leads.md,
    sessions/evolver/29-recipes-pads.md,
    sessions/evolver/30-recipes-drums.md,
    sessions/evolver/04-analog-osc-pwm.md,
    sessions/evolver/05-analog-osc-hard-sync.md,
    sessions/evolver/13-filters-self-oscillation.md,
    instruments/evolver/basic-patch.md
  </read_first>
  <action>
Create 16 patch markdown files in src/content/patches/evolver/. Each file has YAML frontmatter matching PatchSchema and a markdown body with key parameters (diff from basic patch), playing tips as `> [!tip]` callouts, and technique notes as `> [!note]` callouts.

**Source parameter values from the session exercise files listed in read_first.** Do NOT invent parameter values. Each patch corresponds to a specific session exercise recipe.

**Patch manifest (16 files):**

From Session 27 (recipes-bass):
1. `sub-bass.md` — type: bass, session_origin: 27. Osc 1 Saw Level 55, Osc 2 Saw Level 50 Fine +2, LPF 35 Res 0 4P, Key Amount 72, Env Amount 25.
2. `acid-bass.md` — type: bass, session_origin: 27. Osc 1 Saw Level 55, Osc 2 Pulse 50 Level 50 Fine +3, Slop 3, LPF 45 Res 70 4P, Env Amount 80, Decay 45, Sustain 5, Glide 30.
3. `fm-bass.md` — type: bass, session_origin: 27. Uses digital oscillator FM for metallic bass. Source values from session 27 Recipe 3.

From Session 28 (recipes-leads):
4. `sync-lead.md` — type: lead, session_origin: 28. Sync 2->1 ON, Osc 1 Pulse 50 Level 55 Freq C2, Osc 2 Saw Level 0, ENV 3 Dest O1F Amount 67 Attack 15 Decay 87, Mod Wheel Dest FiL Amount -60.
5. `fm-lead.md` — type: lead, session_origin: 28. Digital oscillator FM for bell-like lead. Source from session 28 Recipe 2.
6. `filtered-lead.md` — type: lead, session_origin: 28. Warm filtered lead. Source from session 28 Recipe 3.

From Session 29 (recipes-pads):
7. `pwm-pad.md` — type: pad, session_origin: 29. Osc 1 Pulse 50 Level 50, Osc 2 Pulse 50 Level 45 Fine +5, Slop 3, LFO 1 Tri Freq 6 Amount 35 Dest PW1, LFO 2 Tri Freq 5 Amount 30 Dest PW2, ENV 2 Attack 40 Release 45.
8. `waveshape-pad.md` — type: pad, session_origin: 29. Source from session 29 Recipe 2.
9. `modulated-texture.md` — type: texture, session_origin: 29. Source from session 29 Recipe 3.

From Session 30 (recipes-drums):
10. `analog-kick.md` — type: drum, session_origin: 30. All Osc Levels 0, LPF 40 Res 90 4P, Env Amount 99, ENV 1 Attack 0 Decay 25 Sustain 0, ENV 2 Attack 0 Decay 20 Sustain 0.
11. `noise-snare.md` — type: drum, session_origin: 30. Source from session 30 Recipe 2.
12. `fm-hat.md` — type: drum, session_origin: 30. Source from session 30 Recipe 3.
13. `drum-beat.md` — type: sequence, session_origin: 30. Sequencer-based drum pattern using all 4 tracks. Source from session 30 Exercise 4.

From earlier sessions (simpler patches with fewer parameter changes):
14. `pwm-sweep.md` — type: texture, session_origin: 4. Simple PWM sweep from Session 04 (analog-osc-pwm). Osc 1 Pulse 50, LFO modulating pulse width.
15. `hard-sync-sweep.md` — type: lead, session_origin: 5. Hard sync sweep from Session 05 (analog-osc-hard-sync). Sync 2->1 ON, sweep Osc 1 frequency.
16. `self-osc-drone.md` — type: texture, session_origin: 13. Self-oscillating filter drone from Session 13 (filters-self-oscillation). High resonance drone patch.

**Each file format:**

```markdown
---
name: "Patch Name"
type: bass
session_origin: 27
description: "One-sentence description of the sound and use case."
tags: [tag1, tag2, tag3]
instrument: evolver
created: "2026-01-15"
---

# Patch Name

> [!tip] Playing Tips
> How to play expressively. Mod wheel, aftertouch, velocity tips.

## Key Parameters

### Oscillators
| Parameter | Value |
|-----------|-------|
| Param Name | value |

### Filter
| Parameter | Value |
|-----------|-------|
| Param Name | value |

### Envelopes
| Parameter | Value |
|-----------|-------|
| Param Name | value |

> [!note] Technique Notes
> Why this patch works. Technical explanation of the sound design approach.
```

**Dates:** Use `created` dates spread across Jan-Mar 2026 for realistic demo data (e.g., session 27 patches get "2026-02-10", session 28 gets "2026-02-12", etc.).

**Tags:** Include the type, plus descriptive tags from the session (e.g., [bass, sub, foundation, low-end]).

**Parameter sections:** Group by Oscillators, Filter, Envelopes, LFOs, Sequencer, Effects as applicable. Only include sections with changed parameters. Use the section groupings that match the Evolver's panel layout.

**Type distribution:** 3 bass, 4 lead, 2 pad, 3 drum, 3 texture, 1 sequence = 16 total covering all 6 types.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && ls src/content/patches/evolver/*.md | grep -v README | wc -l | grep -q "16" && echo "16 patch files exist" && npx tsx scripts/validate-content.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/content/patches/evolver/ contains exactly 16 .md files (excluding README.md)
    - Every patch file contains `type:` with one of: bass, lead, pad, drum, texture, sequence
    - Every patch file contains `session_origin:` with a valid session number (4, 5, 13, 27, 28, 29, or 30)
    - Every patch file contains `instrument: evolver`
    - Every patch file contains at least one `> [!tip]` callout
    - Every patch file contains at least one parameter table with `| Parameter | Value |` header
    - npx tsx scripts/validate-content.ts passes without PatchSchema validation errors
    - Type coverage: at least 1 file each with type bass, lead, pad, drum, texture, sequence
  </acceptance_criteria>
  <done>16 demo patch files exist in src/content/patches/evolver/ with valid PatchSchema frontmatter, curriculum-sourced parameter values, playing tips, and technique notes covering all 6 patch types.</done>
</task>

<task type="auto">
  <name>Task 2: Create patch helper utility module</name>
  <files>src/lib/patches.ts</files>
  <read_first>
    src/lib/sessions.ts,
    src/lib/content/types.ts,
    src/lib/content/schemas.ts
  </read_first>
  <action>
Create `src/lib/patches.ts` following the exact pattern of `src/lib/sessions.ts` (SessionWithMeta, ModuleGroup, groupByModule).

Define:

```typescript
import type { Patch } from '@/lib/content/types';

export interface PatchWithMeta {
  data: Patch;
  content: string;
  slug: string;
}

export interface PatchTypeGroup {
  type: string;
  patches: PatchWithMeta[];
}

const TYPE_ORDER = ['bass', 'lead', 'pad', 'drum', 'texture', 'sequence'] as const;

export function groupByType(patches: PatchWithMeta[]): PatchTypeGroup[] {
  // Group patches by type using a Map
  // Sort patches within each group by session_origin (ascending, nulls last)
  // Return groups in TYPE_ORDER order, skipping types with no patches
}

export function getAvailableTypes(patches: PatchWithMeta[]): string[] {
  // Return only types from TYPE_ORDER that have at least one patch
  // Preserves TYPE_ORDER ordering
}
```

Implementation details:
- `groupByType`: Create Map<string, PatchWithMeta[]>, iterate patches adding to map by data.type, sort each group by (a.data.session_origin ?? Infinity) - (b.data.session_origin ?? Infinity), return TYPE_ORDER.filter(has patches).map to PatchTypeGroup.
- `getAvailableTypes`: Create Set from patches.map(p => p.data.type), return TYPE_ORDER.filter(t => set.has(t)).
- Handle empty input array: groupByType returns [], getAvailableTypes returns [].
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx vitest run src/lib/__tests__/patches.test.ts --reporter=verbose 2>&1 || echo "Test file not yet created -- verify file exists and exports: grep -n 'export' src/lib/patches.ts"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/patches.ts exists
    - File exports PatchWithMeta interface: `grep 'export interface PatchWithMeta' src/lib/patches.ts`
    - File exports PatchTypeGroup interface: `grep 'export interface PatchTypeGroup' src/lib/patches.ts`
    - File exports groupByType function: `grep 'export function groupByType' src/lib/patches.ts`
    - File exports getAvailableTypes function: `grep 'export function getAvailableTypes' src/lib/patches.ts`
    - TYPE_ORDER contains exactly ['bass', 'lead', 'pad', 'drum', 'texture', 'sequence']
    - Import uses `@/lib/content/types` for Patch type (not schemas)
  </acceptance_criteria>
  <done>src/lib/patches.ts exports PatchWithMeta, PatchTypeGroup, groupByType, and getAvailableTypes, mirroring the sessions.ts pattern for modules.</done>
</task>

</tasks>

<verification>
- `ls src/content/patches/evolver/*.md | grep -v README | wc -l` returns 16
- `npx tsx scripts/validate-content.ts` passes (all patch frontmatter validates against PatchSchema)
- `grep -l 'type: bass' src/content/patches/evolver/*.md | wc -l` returns at least 3
- `grep -l 'type: lead' src/content/patches/evolver/*.md | wc -l` returns at least 3
- `grep -l 'type: pad' src/content/patches/evolver/*.md | wc -l` returns at least 2
- `grep -l 'type: drum' src/content/patches/evolver/*.md | wc -l` returns at least 3
- `grep -l 'type: texture' src/content/patches/evolver/*.md | wc -l` returns at least 2
- `grep -l 'type: sequence' src/content/patches/evolver/*.md | wc -l` returns at least 1
- `grep -c 'export function' src/lib/patches.ts` returns 2
</verification>

<success_criteria>
16 demo patches exist with valid frontmatter covering all 6 types, and the patch utility module provides groupByType/getAvailableTypes for the UI layer.
</success_criteria>

<output>
After completion, create `.planning/phases/03-patch-library/03-01-SUMMARY.md`
</output>
