---
phase: 23-panel-progress-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/__tests__/practice-heatmap.test.tsx
  - src/lib/__tests__/practice-metrics.test.ts
  - src/app/__tests__/tokens.test.ts
  - src/components/__tests__/evolver-panel.test.tsx
  - src/components/__tests__/cascadia-panel.test.tsx
autonomous: true
requirements: [SPEC-01, SPEC-02, TOKEN-06]

must_haves:
  truths:
    - "Heatmap test file exists with failing stubs for grid rendering, empty state, intensity levels, and 12-week date range"
    - "practice-metrics test file has failing stubs for getHeatmapData utility"
    - "tokens test file has extended assertions for hardcoded lime in shadows and keyframes"
    - "Panel test files have failing stubs for motion.svg viewBox animation"
  artifacts:
    - path: "src/components/__tests__/practice-heatmap.test.tsx"
      provides: "Heatmap component test stubs"
      contains: "describe.*PracticeHeatmap"
    - path: "src/lib/__tests__/practice-metrics.test.ts"
      provides: "getHeatmapData test stubs"
      contains: "getHeatmapData"
    - path: "src/app/__tests__/tokens.test.ts"
      provides: "Extended accent audit assertions"
      contains: "pulse-glow"
    - path: "src/components/__tests__/evolver-panel.test.tsx"
      provides: "motion.svg viewBox test stubs"
      contains: "motion.svg"
    - path: "src/components/__tests__/cascadia-panel.test.tsx"
      provides: "motion.svg viewBox test stubs"
      contains: "motion.svg"
  key_links: []
---

<objective>
Create all Wave 0 test stubs for Phase 23, ensuring Nyquist compliance -- every implementation task has a failing test to target.

Purpose: Test-first scaffolding so Wave 1 implementation has red tests to turn green.
Output: 5 test files (2 new, 3 extended) with failing stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-panel-progress-polish/23-CONTEXT.md
@.planning/phases/23-panel-progress-polish/23-RESEARCH.md
@.planning/phases/23-panel-progress-polish/23-UI-SPEC.md
@.planning/phases/23-panel-progress-polish/23-VALIDATION.md

<interfaces>
<!-- Existing prop contract from CumulativeMetrics (heatmap inherits this) -->
From src/components/cumulative-metrics.tsx:
```typescript
interface CumulativeMetricsProps {
  completionDates: string[]; // Array of ISO date strings (YYYY-MM-DD)
}
```

From src/lib/practice-metrics.ts:
```typescript
export function getSessionsThisMonth(completionDates: Date[], currentDate?: Date): number;
export function getActiveWeeks(completionDates: Date[]): number;
export function getSyntheticCompletionDates(instrument?: string): Map<number, string>;
```

From src/components/evolver-panel.tsx:
```typescript
interface EvolverPanelProps {
  knobValues?: Record<string, number>;
  highlights?: Array<{ controlId: string; color: 'blue' | 'amber' }>;
  activeSections?: string[];
  zoomSections?: string[];
  onKnobChange?: (controlId: string, value: number) => void;
  className?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create heatmap test stubs + extend practice-metrics tests</name>
  <files>src/components/__tests__/practice-heatmap.test.tsx, src/lib/__tests__/practice-metrics.test.ts</files>
  <read_first>
    - src/components/__tests__/practice-heatmap.test.tsx (will not exist yet -- confirm)
    - src/lib/__tests__/practice-metrics.test.ts (existing file -- read current tests)
    - src/components/cumulative-metrics.tsx (prop contract to inherit)
    - src/lib/practice-metrics.ts (existing utilities, getISOWeekKey pattern)
    - .planning/phases/23-panel-progress-polish/23-UI-SPEC.md (heatmap spec: intensity scale, empty state copy, day labels)
  </read_first>
  <action>
Create `src/components/__tests__/practice-heatmap.test.tsx` with these failing test stubs (using `it.todo` or `it('...', () => { throw new Error('not implemented') })`):

1. `describe('PracticeHeatmap')`:
   - `it('renders 12 weeks of grid cells')` -- expects ~84 cells (12 weeks x 7 days) rendered as divs with `data-date` attributes
   - `it('renders empty state when completionDates is empty')` -- expects text "Your practice heatmap appears here after your first completed session."
   - `it('applies intensity levels based on session count')` -- expects cells with count=0 to have `var(--color-surface)` bg, count=1 at 25%, count>=4 at 100%
   - `it('renders day labels Mon, Wed, Fri')` -- expects exactly 3 day label elements
   - `it('renders legend with Less/More labels')` -- expects "Less" and "More" text plus 5 legend squares
   - `it('renders section heading "Practice Activity"')` -- expects h2 with text "Practice Activity"

Extend `src/lib/__tests__/practice-metrics.test.ts` with a new `describe('getHeatmapData')` block containing:
   - `it('returns cells aligned to Monday start')` -- expects first cell dayOfWeek=0 (Monday)
   - `it('returns 12 weeks of data by default')` -- expects ~84 cells
   - `it('counts sessions per date correctly')` -- pass duplicate dates, expect count > 1
   - `it('returns count 0 for dates with no sessions')` -- expect zero-count cells

All stubs should be `it.todo(...)` so they register as pending, not failing. This keeps the test suite green while signaling what needs implementation.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/components/__tests__/practice-heatmap.test.tsx src/lib/__tests__/practice-metrics.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/__tests__/practice-heatmap.test.tsx exists and contains `describe('PracticeHeatmap'`
    - src/components/__tests__/practice-heatmap.test.tsx contains at least 6 `it.todo` calls
    - src/lib/__tests__/practice-metrics.test.ts contains `describe('getHeatmapData'`
    - src/lib/__tests__/practice-metrics.test.ts contains at least 4 `it.todo` calls for getHeatmapData
    - `npx vitest run` exits 0 (all stubs are todo, not failing)
  </acceptance_criteria>
  <done>Heatmap and getHeatmapData test stubs exist as pending todos; test suite stays green</done>
</task>

<task type="auto">
  <name>Task 2: Extend panel tests + tokens tests for Phase 23 assertions</name>
  <files>src/components/__tests__/evolver-panel.test.tsx, src/components/__tests__/cascadia-panel.test.tsx, src/app/__tests__/tokens.test.ts</files>
  <read_first>
    - src/components/__tests__/evolver-panel.test.tsx (existing tests)
    - src/components/__tests__/cascadia-panel.test.tsx (existing tests)
    - src/app/__tests__/tokens.test.ts (existing accent assertions)
    - src/app/globals.css (lines 57, 401-403 -- hardcoded lime values to assert against)
  </read_first>
  <action>
Extend `src/components/__tests__/evolver-panel.test.tsx` with a new `describe('viewBox animation')` block:
   - `it.todo('renders motion.svg element instead of plain svg')` -- will check for motion.svg usage
   - `it.todo('sets initial viewBox to full panel dimensions 0 0 1200 520')` -- checks initial attr
   - `it.todo('animates viewBox to zoom target when zoomSections provided')` -- checks animate prop

Extend `src/components/__tests__/cascadia-panel.test.tsx` with identical `describe('viewBox animation')` block:
   - Same 3 stubs but with Cascadia full viewBox `0 0 1000 580`

Extend `src/app/__tests__/tokens.test.ts` with a new `describe('accent color audit')` block:
   - `it.todo('--shadow-card-hover does not contain hardcoded lime oklch(0.85 0.18 105')` -- will grep globals.css
   - `it.todo('@keyframes pulse-glow does not contain hardcoded lime')` -- will grep globals.css
   - `it.todo('panel glow circles use var(--color-accent) not #c8ff00')` -- will grep panel files

All stubs as `it.todo(...)` to keep suite green.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/components/__tests__/evolver-panel.test.tsx src/components/__tests__/cascadia-panel.test.tsx src/app/__tests__/tokens.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/__tests__/evolver-panel.test.tsx contains `describe('viewBox animation'`
    - src/components/__tests__/cascadia-panel.test.tsx contains `describe('viewBox animation'`
    - src/app/__tests__/tokens.test.ts contains `describe('accent color audit'`
    - src/app/__tests__/tokens.test.ts contains `pulse-glow` in a test description or body
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Panel viewBox and accent audit test stubs registered as pending todos; full suite green</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` exits 0
- All new test stubs appear as "todo" in output
- No existing tests broken
</verification>

<success_criteria>
- 5 test files have Phase 23 stubs (2 new, 3 extended)
- Test suite remains green (all new items are `it.todo`)
- Stubs cover: heatmap rendering, heatmap data utility, panel viewBox animation, accent color audit
</success_criteria>

<output>
After completion, create `.planning/phases/23-panel-progress-polish/23-00-SUMMARY.md`
</output>
