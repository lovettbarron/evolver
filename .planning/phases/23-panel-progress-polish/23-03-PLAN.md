---
phase: 23-panel-progress-polish
plan: 03
type: execute
wave: 2
depends_on: [23-01, 23-02]
files_modified: []
autonomous: false
requirements: [SPEC-01, SPEC-02, TOKEN-06]

must_haves:
  truths:
    - "All pages pass the ADHD 5-second test: single focal point, no competing animations, same or fewer clicks"
    - "Panel zoom tween looks and feels like a smooth camera glide"
    - "Heatmap motivates practice with visible streaks"
    - "Cascadia pages show teal accents throughout (shadows, glow, pulse, heatmap)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 23 work: panel zoom tween, practice heatmap, accent color identity, and ADHD 5-second test across all routes.

Purpose: Human confirmation that the automated work produces the intended visual and interactive quality per D-11.
Output: Verification sign-off or issues list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-panel-progress-polish/23-CONTEXT.md
@.planning/phases/23-panel-progress-polish/23-UI-SPEC.md
@.planning/phases/23-panel-progress-polish/23-01-SUMMARY.md
@.planning/phases/23-panel-progress-polish/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite + build verification</name>
  <files></files>
  <read_first>
    - .planning/phases/23-panel-progress-polish/23-01-SUMMARY.md
    - .planning/phases/23-panel-progress-polish/23-02-SUMMARY.md
  </read_first>
  <action>
Run the full verification battery:
1. `npx vitest run --reporter=verbose` -- all tests must pass
2. `npx next build` -- production build must succeed with no errors
3. Verify no hardcoded accent: `grep -rn 'oklch(0.85 0.18 105' src/app/globals.css` should return ONLY the primitive definition line (~line 16)
4. Verify no hardcoded glow: `grep -rn 'fill="#c8ff00"' src/components/` should return zero matches
5. Verify motion.svg: `grep -n 'motion.svg' src/components/evolver-panel.tsx src/components/cascadia-panel.tsx` should return matches in both files
6. Start dev server for human verification: `npx next dev`
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `npx next build` exits 0 with no errors
    - grep for hardcoded accent outside primitives returns no matches
    - grep for #c8ff00 in components returns no matches
  </acceptance_criteria>
  <done>Full test suite green, production build succeeds, no hardcoded accent values remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification -- panel zoom, heatmap, accent identity, ADHD test</name>
  <files></files>
  <action>
Human visual verification of Phase 23 deliverables. Dev server must be running (`npx next dev`). Walk through the verification checklist below covering all 4 areas: panel zoom tween, practice heatmap, accent identity, and ADHD 5-second test.
  </action>
  <what-built>
Phase 23 complete: panel viewBox zoom tween, practice heatmap on progress page, per-instrument accent color cascade (lime for Evolver, teal for Cascadia), ADHD 5-second test compliance.
  </what-built>
  <how-to-verify>
Start dev server if not running: `npx next dev`

**1. Panel Zoom Tween (SPEC-01):**
- Visit http://localhost:3000/instruments/evolver/sessions/01-oscillators-introduction
- Watch the panel: it should show full panel briefly (~200ms) then smoothly tween (glide) to zoom on the relevant section
- The zoom should feel like a camera moving, not a cut
- Visit a Cascadia session with panel sections for same behavior
- Visit a session with NO zoomSections -- panel should stay at full view, no animation

**2. Practice Heatmap (SPEC-02):**
- Visit http://localhost:3000/instruments/evolver/progress
- Below the 4 count cards and Module Journey, see a "Practice Activity" section
- See a 12-week grid of small colored squares (GitHub contribution graph style)
- Cells with practice sessions should be lime-green at varying intensities
- See "Mon", "Wed", "Fri" day labels on the left
- See "Less" / "More" legend below the grid
- Hover over a colored cell to see tooltip: "{date}: {N} session(s)"

**3. Accent Color Identity (TOKEN-06):**
- Visit http://localhost:3000/instruments/cascadia/progress
- Heatmap cells should be TEAL (not lime)
- Hover over a card -- shadow glow should have teal tint (not lime)
- Check ModuleJourney current-module dot -- pulse glow should be teal
- Visit http://localhost:3000/instruments/evolver/progress -- everything lime
- Compare: Evolver = lime, Cascadia = teal across all accented elements

**4. ADHD 5-Second Test (D-11):**
For each page below, verify: one clear focal point, no competing auto-animations, information scannable in <5 seconds
- / (home)
- /instruments/evolver (instrument home)
- /instruments/evolver/sessions (session list)
- /instruments/evolver/sessions/01-oscillators-introduction (session detail)
- /instruments/evolver/patches (patch grid)
- /instruments/evolver/progress (progress page with heatmap)
- /instruments/cascadia (repeat key pages)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human visual verification"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms panel zoom tween is smooth spatial glide (not a cut)
    - Human confirms heatmap renders 12-week grid with accent-colored cells
    - Human confirms Cascadia pages use teal accent (not lime) for heatmap, shadows, pulse-glow
    - Human confirms each page has single focal point and no competing auto-animations
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
  <done>Human approves visual quality of panel zoom, heatmap, accent identity, and ADHD compliance across all routes</done>
</task>

</tasks>

<verification>
- Full test suite green
- Production build succeeds
- Human visual sign-off on all 4 verification areas
</verification>

<success_criteria>
- Tests pass, build succeeds, no hardcoded accents remain
- Human confirms: zoom feels smooth, heatmap looks motivating, accent colors are correct per instrument, ADHD test passes on all pages
</success_criteria>

<output>
After completion, create `.planning/phases/23-panel-progress-polish/23-03-SUMMARY.md`
</output>
