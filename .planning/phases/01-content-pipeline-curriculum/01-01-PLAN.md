---
phase: 01-content-pipeline-curriculum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - evolver.config.json
  - src/lib/config.ts
  - src/lib/content/schemas.ts
  - src/lib/content/types.ts
  - src/lib/content/reader.ts
  - scripts/validate-content.ts
  - src/lib/content/__tests__/schemas.test.ts
  - src/lib/content/__tests__/reader.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - INST-04

must_haves:
  truths:
    - "Content reader reads markdown + YAML frontmatter from a configured vault path"
    - "Content reader falls back to bundled content when no vault path is configured"
    - "Zod schemas validate all frontmatter at parse time and reject invalid data"
    - "Instruments are discovered from filesystem directories"
    - "npm run validate-content runs scripts/validate-content.ts and reports validation errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "gray-matter"
    - path: "src/lib/content/schemas.ts"
      provides: "Zod schemas for session, patch, instrument"
      exports: ["SessionSchema", "PatchSchema", "InstrumentFileSchema"]
    - path: "src/lib/content/reader.ts"
      provides: "Dual-source content reader"
      exports: ["readContentFile", "discoverInstruments", "listSessions", "listPatches"]
    - path: "src/lib/config.ts"
      provides: "Config file reader"
      exports: ["loadConfig", "AppConfig"]
    - path: "scripts/validate-content.ts"
      provides: "Content validation CLI script"
      contains: "safeParse"
  key_links:
    - from: "src/lib/content/reader.ts"
      to: "src/lib/content/schemas.ts"
      via: "schema.parse(data)"
      pattern: "Schema\\.parse"
    - from: "src/lib/content/reader.ts"
      to: "src/lib/config.ts"
      via: "loadConfig to determine content root"
      pattern: "getContentRoot|loadConfig"
    - from: "scripts/validate-content.ts"
      to: "src/lib/content/schemas.ts"
      via: "imports schemas for safeParse"
      pattern: "SessionSchema|PatchSchema|InstrumentFileSchema"
---

<objective>
Set up the project foundation and build the content pipeline core: Zod schemas for all content types, a dual-source content reader (vault vs bundled), config file, instrument discovery from filesystem, and the validate-content script.

Purpose: This is the invisible data layer that every other phase builds on. Without validated schemas and a working content reader, nothing else can function.
Output: Working content reader with tests, Zod schemas, config system, validate-content script.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-pipeline-curriculum/01-CONTEXT.md
@.planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding + Zod schemas + types</name>
  <files>package.json, tsconfig.json, vitest.config.ts, evolver.config.json, src/lib/config.ts, src/lib/content/schemas.ts, src/lib/content/types.ts, src/lib/content/__tests__/schemas.test.ts</files>
  <read_first>
    .planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
    .planning/phases/01-content-pipeline-curriculum/01-CONTEXT.md
    instruments/evolver/basic-patch.md
    instruments/evolver/overview.md
  </read_first>
  <behavior>
    - SessionSchema.parse({title: "Session 01: Navigation", module: "Foundations", session_number: 1, duration: 20, prerequisite: null, output_type: "patch", difficulty: "beginner", tags: ["navigation"], instrument: "evolver"}) returns valid data
    - SessionSchema.parse({title: "Bad", duration: 60}) throws (duration > 30)
    - SessionSchema.parse({title: "Bad"}) throws (missing required fields: module, session_number, duration, output_type, difficulty, tags, instrument)
    - PatchSchema.parse({name: "Fat Bass", type: "bass", session_origin: 6, description: "Deep sub", tags: ["bass"], instrument: "evolver", created: "2026-01-15"}) returns valid data
    - PatchSchema.parse({name: "Bad", type: "invalid_type"}) throws (invalid enum value)
    - InstrumentFileSchema.parse({type: "signal-flow", instrument: "evolver", title: "Evolver Signal Flow", manufacturer: "Dave Smith Instruments"}) returns valid data
    - InstrumentFileSchema.parse({type: "invalid"}) throws (invalid enum value)
    - All schemas allow extra fields via .passthrough() -- {title: "...", ..., obsidian_custom: "foo"} does NOT throw
    - ConfigSchema with no vaultPath returns demo mode (vaultPath undefined)
    - ConfigSchema with vaultPath returns local mode
  </behavior>
  <action>
1. Create package.json:
   - name: "evolver"
   - type: "module"
   - scripts: {"validate-content": "tsx scripts/validate-content.ts", "test": "vitest run", "test:watch": "vitest"}
   - dependencies: gray-matter, zod, unified, remark-parse, remark-gfm, remark-rehype, remark-frontmatter, remark-wiki-link, rehype-stringify, rehype-raw, rehype-callouts, rehype-highlight, rehype-slug, rehype-autolink-headings, mermaid, glob
   - devDependencies: vitest, typescript, tsx, @types/node

2. Create tsconfig.json with target ES2022, module NodeNext, moduleResolution NodeNext, strict true, outDir dist, rootDir src, paths: {"@/*": ["./src/*"]}

3. Create vitest.config.ts:
   ```typescript
   import { defineConfig } from 'vitest/config';
   export default defineConfig({
     test: { globals: true },
     resolve: { alias: { '@': './src' } },
   });
   ```

4. Create evolver.config.json:
   ```json
   { "vaultPath": "/Users/andrew/song" }
   ```

5. Create src/lib/config.ts implementing loadConfig():
   - Read evolver.config.json from process.cwd()
   - ConfigSchema: z.object({ vaultPath: z.string().optional(), instrument: z.string().default('evolver') }).passthrough()
   - If file missing or parse fails, return defaults (demo mode: vaultPath undefined)
   - Export AppConfig type and loadConfig function

6. Create src/lib/content/schemas.ts with three schemas:
   - SessionSchema: z.object({ title: z.string(), module: z.string(), session_number: z.number().int().positive(), duration: z.number().int().min(5).max(30), prerequisite: z.union([z.number(), z.null()]), output_type: z.enum(['patch', 'technique', 'recording', 'composition']), difficulty: z.enum(['beginner', 'intermediate', 'advanced']), tags: z.array(z.string()), instrument: z.string() }).passthrough()
   - PatchSchema: z.object({ name: z.string(), type: z.enum(['bass', 'lead', 'pad', 'drum', 'texture', 'sequence']), session_origin: z.union([z.number(), z.null()]), description: z.string(), tags: z.array(z.string()), instrument: z.string(), created: z.string() }).passthrough()
   - InstrumentFileSchema: z.object({ type: z.enum(['overview', 'signal-flow', 'basic-patch', 'modules']), instrument: z.string(), title: z.string(), manufacturer: z.string() }).passthrough()
   - Export all schemas and inferred types (Session, Patch, InstrumentFile)

7. Create src/lib/content/types.ts re-exporting the inferred types from schemas.ts for convenience

8. Create src/lib/content/__tests__/schemas.test.ts with tests for each behavior above

9. Run `npm install` then `npx vitest run src/lib/content/__tests__/schemas.test.ts`
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx vitest run src/lib/content/__tests__/schemas.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "gray-matter" and "zod" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "validate-content" in scripts
    - tsconfig.json contains "strict": true
    - src/lib/content/schemas.ts contains "export const SessionSchema"
    - src/lib/content/schemas.ts contains "export const PatchSchema"
    - src/lib/content/schemas.ts contains "export const InstrumentFileSchema"
    - src/lib/content/schemas.ts contains ".passthrough()" (3 occurrences)
    - src/lib/config.ts contains "export async function loadConfig"
    - src/lib/config.ts contains "export type AppConfig"
    - vitest run schemas.test.ts exits 0
  </acceptance_criteria>
  <done>All three Zod schemas validate correct frontmatter and reject invalid data. Passthrough allows unknown fields. Config system reads evolver.config.json with demo-mode fallback. All schema tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dual-source content reader + instrument discovery</name>
  <files>src/lib/content/reader.ts, src/lib/content/__tests__/reader.test.ts</files>
  <read_first>
    src/lib/content/schemas.ts
    src/lib/config.ts
    .planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
    instruments/evolver/overview.md
    instruments/evolver/basic-patch.md
  </read_first>
  <behavior>
    - readContentFile("instruments/evolver/overview.md", InstrumentFileSchema, {vaultPath: undefined}) reads from src/content/ (bundled)
    - readContentFile("instruments/evolver/overview.md", InstrumentFileSchema, {vaultPath: "/path/to/vault"}) reads from /path/to/vault/
    - readContentFile returns { data: validated_frontmatter, content: markdown_body }
    - readContentFile throws ZodError for invalid frontmatter
    - discoverInstruments(contentRoot) returns ["evolver"] when instruments/evolver/ exists
    - discoverInstruments skips dotfiles and non-directory entries
    - listSessions("evolver", config) returns array of Session objects from sessions/evolver/*.md
    - listPatches("evolver", config) returns array of Patch objects from patches/evolver/*.md
    - getContentRoot({vaultPath: undefined}) returns path.join(process.cwd(), 'src/content')
    - getContentRoot({vaultPath: "/foo"}) returns "/foo"
  </behavior>
  <action>
1. Create src/lib/content/reader.ts:
   - Import gray-matter, fs/promises, path, glob, all schemas from schemas.ts, AppConfig from config.ts
   - getContentRoot(config: AppConfig): string -- returns config.vaultPath ?? path.join(process.cwd(), 'src/content')
   - readContentFile<T extends z.ZodType>(filePath: string, schema: T, config: AppConfig): Promise<{data: z.infer<T>, content: string}> -- uses gray-matter to extract frontmatter, validates with schema.parse(data), returns {data, content}
   - discoverInstruments(config: AppConfig): Promise<string[]> -- reads instruments/ directory under content root, returns directory names (filtering out dotfiles and non-directories)
   - listSessions(instrument: string, config: AppConfig): Promise<Array<{data: Session, content: string, slug: string}>> -- globs sessions/{instrument}/*.md, reads each with readContentFile + SessionSchema, returns array sorted by session_number
   - listPatches(instrument: string, config: AppConfig): Promise<Array<{data: Patch, content: string, slug: string}>> -- globs patches/{instrument}/*.md (skipping README.md), reads each with readContentFile + PatchSchema
   - listInstrumentFiles(instrument: string, config: AppConfig): Promise<Array<{data: InstrumentFile, content: string, slug: string}>> -- globs instruments/{instrument}/*.md, reads each with InstrumentFileSchema
   - Export all functions

2. Create src/lib/content/__tests__/reader.test.ts:
   - Create test fixtures in a __fixtures__/ directory: a valid session .md, a valid instrument .md, an invalid .md
   - Test readContentFile with vault path pointing to fixtures
   - Test readContentFile with no vault path (bundled mode) -- needs src/content/ test fixture
   - Test discoverInstruments against fixtures directory
   - Test listSessions, listPatches against fixtures
   - Test that invalid frontmatter throws ZodError

3. Run tests: `npx vitest run src/lib/content/__tests__/reader.test.ts`
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx vitest run src/lib/content/__tests__/reader.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/content/reader.ts contains "export async function readContentFile"
    - src/lib/content/reader.ts contains "export async function discoverInstruments"
    - src/lib/content/reader.ts contains "export async function listSessions"
    - src/lib/content/reader.ts contains "export async function listPatches"
    - src/lib/content/reader.ts contains "matter(" (gray-matter usage)
    - src/lib/content/reader.ts contains "schema.parse" or "Schema.parse"
    - src/lib/content/reader.ts contains "getContentRoot"
    - vitest run reader.test.ts exits 0
  </acceptance_criteria>
  <done>Content reader reads from vault or bundled content based on config. Instrument discovery returns directory names. listSessions and listPatches return validated, sorted arrays. All reader tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create validate-content script</name>
  <files>scripts/validate-content.ts</files>
  <read_first>
    src/lib/content/schemas.ts
    src/lib/content/reader.ts
    .planning/phases/01-content-pipeline-curriculum/01-RESEARCH.md
  </read_first>
  <action>
1. Create scripts/validate-content.ts following the pattern from RESEARCH.md:
   - Accept optional CLI argument for content root path (default: src/content)
   - Import SessionSchema, PatchSchema, InstrumentFileSchema from src/lib/content/schemas.ts
   - Import gray-matter and glob
   - Validate all sessions (sessions/**/*.md) against SessionSchema using safeParse
   - Validate all instrument files (instruments/**/*.md) against InstrumentFileSchema using safeParse
   - Validate all patch files (patches/**/*.md, skipping README.md) against PatchSchema using safeParse
   - Print each validation failure with file path and formatted Zod error
   - Exit with code 1 if any failures, code 0 if all pass
   - Print summary: "N files validated, M failures" at the end

2. Verify the script runs: `npx tsx scripts/validate-content.ts`
   - It should either pass (if no content yet) or report specific validation errors
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && npx tsx scripts/validate-content.ts --help 2>&1 || npx tsx scripts/validate-content.ts 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate-content.ts exists
    - scripts/validate-content.ts contains "SessionSchema" and "PatchSchema" and "InstrumentFileSchema"
    - scripts/validate-content.ts contains "safeParse"
    - scripts/validate-content.ts contains "process.exit"
    - Running `npm run validate-content` invokes the script via tsx
  </acceptance_criteria>
  <done>validate-content script exists, imports all schemas, validates session/instrument/patch files from a content root, reports errors with file paths, and exits with appropriate code.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all schema and reader tests pass
- `npm run validate-content` -- runs without crashing (may report errors if no content yet)
- Content reader returns identical data shapes from vault path and bundled content
- Zod schemas reject invalid frontmatter with clear error messages
- discoverInstruments finds instruments from filesystem directories
</verification>

<success_criteria>
- All 4 requirements addressed: PIPE-01 (vault reading), PIPE-02 (bundled fallback), PIPE-03 (Zod validation), INST-04 (instrument discovery)
- Schema tests pass for valid and invalid frontmatter for all 3 content types
- Content reader tests pass for both vault and bundled modes
- Package.json exists with all required dependencies
- scripts/validate-content.ts exists and is runnable via `npm run validate-content`
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-pipeline-curriculum/01-01-SUMMARY.md`
</output>
