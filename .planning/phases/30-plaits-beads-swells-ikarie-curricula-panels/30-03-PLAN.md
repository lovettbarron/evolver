---
phase: 30-plaits-beads-swells-ikarie-curricula-panels
plan: 03
type: execute
wave: 3
depends_on: [30-02]
files_modified:
  - src/lib/swells-panel-data.ts
  - src/lib/__tests__/swells-panel-data.test.ts
  - src/components/swells-panel.tsx
  - src/components/__tests__/swells-panel.test.tsx
  - src/components/session-detail.tsx
  - "src/app/modules/[slug]/panel/page.tsx"
  - src/components/quick-ref-panel.tsx
  - src/components/patch-detail.tsx
  - modules/swells/overview.md
  - src/content/modules/swells/overview.md
  - modules/swells/module.json
  - references/swells-manual.pdf
  - sessions/swells/01-foundations-controls-routing.md
  - sessions/swells/02-plate-hall-fog-blur-shadow.md
  - sessions/swells/03-shimmer-cloud-velvet-asterion-deadspace.md
  - sessions/swells/04-modulated-resonant-buckets-ritual-gaze.md
  - sessions/swells/05-swell-generator.md
  - sessions/swells/06-freeze-reverse-performance.md
  - src/content/sessions/swells/01-foundations-controls-routing.md
  - src/content/sessions/swells/02-plate-hall-fog-blur-shadow.md
  - src/content/sessions/swells/03-shimmer-cloud-velvet-asterion-deadspace.md
  - src/content/sessions/swells/04-modulated-resonant-buckets-ritual-gaze.md
  - src/content/sessions/swells/05-swell-generator.md
  - src/content/sessions/swells/06-freeze-reverse-performance.md
autonomous: false
requirements: [CURR-06, PANEL-02]
user_setup:
  - service: intellijel
    why: "Swells PDF manual needed for accurate panel layout"
    dashboard_config:
      - task: "Download Swells manual PDF if automated download fails"
        location: "https://intellijel.com/shop/eurorack/swells/ (Manual tab)"

must_haves:
  truths:
    - "Swells panel data file exports ~40-45 control entries matching the physical module"
    - "Every jack has a signalType, every control has id/name/module/type"
    - "Panel data test suite passes with all entries validated"
    - "Swells panel component renders without crashing with all 35+ controls visible"
    - "Swells overview page has architecture, controls reference, and init state"
    - "6 Swells sessions exist covering foundations, 3 algorithm groups, Swell Generator, and Freeze/Reverse"
    - "All sessions are triple-written to sessions/, src/content/, and ~/song/"
    - "Session markdown files include data-swells-panel markers for inline panel rendering"
  artifacts:
    - path: "src/lib/swells-panel-data.ts"
      provides: "Complete Swells panel control metadata, positions (placeholder), section bounds"
      exports: ["CONTROL_METADATA", "CONTROL_POSITIONS", "SECTION_BOUNDS", "SwellsControlMeta", "midiToRotation"]
    - path: "src/lib/__tests__/swells-panel-data.test.ts"
      provides: "Validation tests for panel data completeness and correctness"
    - path: "src/components/swells-panel.tsx"
      provides: "React SVG panel component rendering all Swells controls"
      exports: ["SwellsPanel"]
    - path: "src/components/__tests__/swells-panel.test.tsx"
      provides: "Component render test for SwellsPanel"
    - path: "modules/swells/overview.md"
      provides: "Full overview with architecture, controls reference, init state"
  key_links:
    - from: "src/lib/__tests__/swells-panel-data.test.ts"
      to: "src/lib/swells-panel-data.ts"
      via: "import CONTROL_METADATA, SECTION_BOUNDS, midiToRotation"
      pattern: "import.*from.*swells-panel-data"
    - from: "src/components/swells-panel.tsx"
      to: "src/lib/swells-panel-data.ts"
      via: "import CONTROL_METADATA, CONTROL_POSITIONS, SECTION_BOUNDS"
      pattern: "import.*from.*swells-panel-data"
    - from: "src/components/session-detail.tsx"
      to: "src/components/swells-panel.tsx"
      via: "data-swells-panel marker regex + dynamic import"
      pattern: "data-swells-panel|SwellsPanel"
---

<objective>
Create the complete Swells curriculum (panel data + panel component + overview + 6 sessions), including a Swells manual PDF download attempt.

Purpose: Swells is the largest module in this phase (20HP, ~40-45 controls) with 9 reverb algorithms plus a Swell Generator. The curriculum per D-03 groups algorithms into thematic sessions (plate/hall, shimmer/cloud, modulated/resonant) plus dedicated sessions for the Swell Generator and Freeze/Reverse performance features.

Output: Panel data file with tests, panel SVG component with tests, integration updates, expanded overview page, 6 sessions triple-written to all locations with data-swells-panel markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-CONTEXT.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-UI-SPEC.md

@src/lib/maths-panel-data.ts
@src/lib/__tests__/maths-panel-data.test.ts
@src/components/maths-panel.tsx
@src/components/__tests__/maths-panel.test.tsx
@src/components/session-detail.tsx
@src/app/modules/[slug]/panel/page.tsx
@src/components/quick-ref-panel.tsx
@src/components/patch-detail.tsx
@modules/maths/overview.md
@sessions/maths/01-foundations-rise-fall-envelope.md
@.claude/skills/synth-panel-builder/SKILL.md
@references/swells-manual.txt

@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Download Swells manual PDF and create panel data file with test suite</name>
  <files>references/swells-manual.pdf, references/swells-manual.txt, src/lib/swells-panel-data.ts, src/lib/__tests__/swells-panel-data.test.ts</files>
  <read_first>
    - references/swells-manual.txt (current placeholder — confirms PDF not yet downloaded)
    - src/lib/maths-panel-data.ts (reference pattern for data file structure)
    - src/lib/__tests__/maths-panel-data.test.ts (reference pattern for test structure)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md (Swells control inventory from web sources — MEDIUM confidence, ~40-45 controls)
  </read_first>
  <action>
**Part A: Attempt Swells manual PDF download per D-08.**

Try these approaches in order:
1. `curl -L -o references/swells-manual.pdf "https://intellijel.com/wp-content/uploads/2024/01/Swells-Manual.pdf"` (common Intellijel URL pattern)
2. `curl -L -o references/swells-manual.pdf "https://intellijel.com/downloads/manuals/swells_manual.pdf"`
3. Fetch the product page to find the PDF link: `curl -s "https://intellijel.com/shop/eurorack/swells/" | grep -i "pdf\|manual\|download"`
4. Try ModularGrid's manual link if available

After each attempt, verify with `file references/swells-manual.pdf` (must report "PDF document") and `wc -c` (must be > 100KB).

If ALL automated downloads fail, update `references/swells-manual.txt` with:
```
Intellijel Swells Manual
Status: PDF not available via automated download
Action needed: Visit https://intellijel.com/shop/eurorack/swells/ (Manual tab) and download manually
Save as: references/swells-manual.pdf
Fallback: Control names verified from ModularGrid + SYNTH ANATOMY. Positions use placeholder values.
```

Per D-09: If PDF is downloaded, cross-reference its panel diagram with manufacturer product photos on the Intellijel product page for accuracy verification.

**Part B: Create panel data file and tests.**

Proceed with panel data regardless of PDF availability — use RESEARCH.md control inventory (verified from web sources).

**Interface:**
```typescript
export interface SwellsControlMeta {
  id: string;
  name: string;
  module: string;  // always 'swells'
  type: 'knob' | 'slider' | 'switch' | 'button' | 'jack-in' | 'jack-out' | 'led';
  signalType?: 'audio' | 'cv' | 'gate' | 'modulation';
}
```

Note: Swells uses `slider` type for its faders (vertical faders, not rotary knobs) and `switch` for the 3-position toggles.

**CONTROL_METADATA entries (from RESEARCH.md):**

Faders/sliders (8, type: 'slider'):
- `slider-swells-pre-delay` — PRE-DELAY
- `slider-swells-size` — SIZE
- `slider-swells-decay` — DECAY
- `slider-swells-hi-damp` — HI DAMP
- `slider-swells-lo-damp` — LO DAMP
- `slider-swells-eq` — EQ
- `slider-swells-ebb` — EBB (model-specific)
- `slider-swells-flow` — FLOW (model-specific)

Level faders (2, type: 'slider'):
- `slider-swells-input` — INPUT level
- `slider-swells-mix` — MIX level

Knobs (~6, type: 'knob'):
- `knob-swells-drive` — DRIVE
- `knob-swells-trim` — TRIM
- `knob-swells-rise` — RISE (Swell Generator)
- `knob-swells-fall` — FALL (Swell Generator)
- `knob-swells-threshold` — THRESHOLD (Swell Generator)
- `knob-swells-ef-gain` — EF GAIN (envelope follower)
- `knob-swells-ef-high-cut` — EF HIGH CUT

Buttons (6, type: 'button'):
- `button-swells-swell` — SWELL
- `button-swells-freeze` — FREEZE
- `button-swells-burst` — BURST
- `button-swells-reverse` — REVERSE
- `button-swells-model-up` — MODEL UP (cycle algorithm)
- `button-swells-model-down` — MODEL DOWN (cycle algorithm)

Switches (2, type: 'switch'):
- `switch-swells-lo-fi` — LO-FI (3-position: OFF/MIN/MAX)
- `switch-swells-ef-source` — EF SOURCE (3-position: IN/SC/OUT)

CV input jacks (~9, type: 'jack-in', signalType: 'cv'):
- Attenuverter-paired CV inputs for major reverb parameters (exact jack names from PDF or best-guess from web: PRE-DELAY CV, SIZE CV, DECAY CV, HI DAMP CV, LO DAMP CV, EBB CV, FLOW CV, TRIG IN, EF IN). Adjust names if PDF provides corrections.

Audio I/O (5 jacks):
- `jack-swells-in-l` — IN L, jack-in, audio
- `jack-swells-in-r` — IN R, jack-in, audio
- `jack-swells-out-l` — L, jack-out, audio
- `jack-swells-out-r` — R, jack-out, audio
- `jack-swells-sidechain` — SIDECHAIN, jack-in, audio

Other jacks (2):
- `jack-swells-swell-cv` — SWELL CV, jack-out, cv (Swell Generator output)
- `jack-swells-trig` — TRIG, jack-in, gate (assignable trigger input)

**Total: ~40-45 entries.** The exact count depends on how many CV inputs + attenuverters the manual reveals. Set the test minimum to 35 (conservative) with a note that count may increase after PDF review.

**CONTROL_POSITIONS** — All entries with `{ x: 0, y: 0 }` placeholder.
**SECTION_BOUNDS** — `'swells': { x: 0, y: 0, width: 100, height: 100 }` placeholder.
**midiToRotation** — Same formula. Also add `midiToSliderPosition(value: number): number` returning `value / 127` (0 to 1 range for slider thumb position).

Also update `modules/swells/module.json` reference_pdfs to point to `swells-manual.pdf` (instead of `swells-manual.txt`) if the PDF was successfully downloaded.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/lib/__tests__/swells-panel-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/swells-panel-data.ts contains `export interface SwellsControlMeta`
    - src/lib/swells-panel-data.ts contains `export const CONTROL_METADATA: Record<string, SwellsControlMeta>`
    - src/lib/swells-panel-data.ts contains `slider-swells-decay`
    - src/lib/swells-panel-data.ts contains `button-swells-freeze`
    - src/lib/swells-panel-data.ts contains `jack-swells-out-l`
    - src/lib/swells-panel-data.ts contains `switch-swells-lo-fi`
    - src/lib/swells-panel-data.ts contains `knob-swells-rise`
    - src/lib/__tests__/swells-panel-data.test.ts exits 0
    - references/swells-manual.pdf exists as PDF OR references/swells-manual.txt documents manual download instructions
  </acceptance_criteria>
  <done>Swells panel data file exports 35+ controls with tests passing. Manual PDF downloaded or fallback instructions documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create Swells overview and 6 curriculum sessions with triple-write</name>
  <files>modules/swells/overview.md, src/content/modules/swells/overview.md, sessions/swells/01-foundations-controls-routing.md, sessions/swells/02-plate-hall-fog-blur-shadow.md, sessions/swells/03-shimmer-cloud-velvet-asterion-deadspace.md, sessions/swells/04-modulated-resonant-buckets-ritual-gaze.md, sessions/swells/05-swell-generator.md, sessions/swells/06-freeze-reverse-performance.md</files>
  <read_first>
    - modules/swells/overview.md (current state — likely placeholder, needs creating from scratch if it doesn't exist)
    - modules/maths/overview.md (reference pattern)
    - sessions/maths/01-foundations-rise-fall-envelope.md (reference session format)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md (Swells algorithms, Swell Generator, control descriptions)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-CONTEXT.md (locked decision D-03)
    - references/swells-manual.pdf (if downloaded — read for architecture and algorithm details)
  </read_first>
  <action>
**Step 1: Create/expand Swells overview page** (`modules/swells/overview.md`)

1. **Frontmatter**: `type: overview`, `instrument: swells`, `title: "Swells Overview"`, `manufacturer: "Intellijel"`
2. **Heading**: `# Intellijel Swells` with tagline `*Multi-algorithm stereo reverb with swell generator*` and `**20HP** | Effects`
3. **Architecture**: Describe the multi-algorithm stereo reverb — 9 algorithms (Fog, Blur, Shadow, Velvet, Asterion, Deadspace, Buckets, Ritual, Gaze) selected via buttons. Stereo audio I/O with sidechain input. 8 parameter faders control reverb character (PRE-DELAY, SIZE, DECAY, HI DAMP, LO DAMP, EQ, EBB, FLOW — last 2 are algorithm-specific). Built-in Swell Generator (envelope follower + AR envelope) that can generate CV from input dynamics. FREEZE captures and holds the reverb tail. REVERSE plays reverb tail backwards. BURST triggers a burst effect. LO-FI mode adds bit-crush/sample-rate reduction.
4. **Controls Reference**: Table grouped by section (Reverb Parameters, Level, Swell Generator, Effects, Switches, Audio I/O, CV).
5. **Basic Patch (Init State)**: All faders at noon, DRIVE at min, MIX at 50%, Fog algorithm selected, LO-FI OFF, EF SOURCE at IN. Patch audio to IN L, OUT L to mixer.
6. **9 Algorithms Reference**: Brief table with algorithm name and character description.
7. **Further Reading**: Swells Manual if PDF exists, Intellijel website.

Triple-write to `src/content/modules/swells/overview.md` and `~/song/modules/swells/overview.md`.

**Step 2: Create 6 sessions per D-03:**

All sessions use frontmatter with `instrument: swells`, `instrument_type: eurorack_module`.

**Session 01 — Foundations: Controls, Routing & Your First Reverb** (section: Foundations, difficulty: beginner, duration: 25, output_type: technique, prerequisite: null)
- Set init state with Fog algorithm. Explore the 8 parameter faders systematically. Understand stereo routing (mono in L, stereo from L+R). Explore INPUT and MIX level controls. Explore DRIVE for saturation.
- Output: Documented first impressions of Fog algorithm with annotated fader positions.
- Reference: Swells Manual pp. TBD
- Include `data-swells-panel` markers highlighting faders as they are explored

**Session 02 — Traditional Spaces: Fog, Blur & Shadow** (section: Algorithms, difficulty: beginner, duration: 20)
- Plate/Hall algorithms per RESEARCH.md grouping. Compare Fog (diffuse), Blur (smeared), Shadow (dark/cavernous). Focus on how EBB and FLOW behave differently per algorithm.
- Output: Saved settings for each algorithm with comparison notes.

**Session 03 — Ethereal Textures: Velvet, Asterion & Deadspace** (section: Algorithms, difficulty: intermediate, duration: 20)
- Shimmer/cloud algorithms. Velvet (lush shimmer), Asterion (spectral), Deadspace (void). More experimental territory.
- Output: A layered ethereal texture patch.

**Session 04 — Character Effects: Buckets, Ritual & Gaze** (section: Algorithms, difficulty: intermediate, duration: 25)
- Modulated/resonant algorithms. Buckets (bucket-brigade character), Ritual (ritualistic resonance), Gaze (staring into the void). Explore how EBB and FLOW create distinct modulation per algorithm.
- Output: A patch exploiting algorithm-specific modulation character.

**Session 05 — Swell Generator Deep Dive** (section: Advanced, difficulty: intermediate, duration: 25)
- Focus on RISE, FALL, THRESHOLD knobs. Envelope follower as modulation source. SWELL CV output jack for controlling other modules. Combine with reverb algorithms for dynamics-responsive effects.
- Output: A dynamics-responsive reverb patch using Swell Generator CV.

**Session 06 — Freeze, Reverse & Performance** (section: Creative, difficulty: advanced, duration: 25)
- Creative culmination per D-05. FREEZE captures reverb tail, REVERSE plays it backwards, BURST triggers burst effect. LO-FI mode for degradation. Combine multiple performance features. Build a performance-ready effects patch.
- Output: A performance patch using freeze/reverse/burst documented with settings.

**Include `data-swells-panel` markers** in all sessions at exercise steps where specific controls are being used. Use the format:
```html
<div data-swells-panel
  data-sliders="slider-swells-decay:100,slider-swells-size:80"
  data-highlights="button-swells-freeze:amber"
></div>
```

**Triple-write all 6 sessions** to sessions/swells/, src/content/sessions/swells/, ~/song/sessions/swells/. Create directories if needed.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && ls sessions/swells/*.md | wc -l && ls src/content/sessions/swells/*.md | wc -l && ls ~/song/sessions/swells/*.md | wc -l && head -15 sessions/swells/01-foundations-controls-routing.md</automated>
  </verify>
  <acceptance_criteria>
    - modules/swells/overview.md contains `## Architecture`
    - modules/swells/overview.md contains `## Controls Reference`
    - modules/swells/overview.md contains `## Basic Patch`
    - modules/swells/overview.md contains `instrument: swells`
    - src/content/modules/swells/overview.md is identical to modules/swells/overview.md
    - 6 session files exist in sessions/swells/ (01 through 06)
    - 6 session files exist in src/content/sessions/swells/
    - 6 session files exist in ~/song/sessions/swells/
    - sessions/swells/01-foundations-controls-routing.md contains `instrument_type: eurorack_module`
    - sessions/swells/01-foundations-controls-routing.md contains `instrument: swells`
    - sessions/swells/01-foundations-controls-routing.md contains `data-swells-panel`
    - sessions/swells/06-freeze-reverse-performance.md contains `session_number: 6`
    - All session files contain `instrument_type: eurorack_module`
  </acceptance_criteria>
  <done>Swells overview expanded. 6 sessions covering foundations, 3 algorithm groups, Swell Generator, and Freeze/Reverse per D-03, triple-written with data-swells-panel markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Swells panel component, component test, and integration updates</name>
  <files>src/components/swells-panel.tsx, src/components/__tests__/swells-panel.test.tsx, src/components/session-detail.tsx, src/app/modules/[slug]/panel/page.tsx, src/components/quick-ref-panel.tsx, src/components/patch-detail.tsx</files>
  <read_first>
    - src/components/maths-panel.tsx (reference pattern for panel component structure)
    - src/components/__tests__/maths-panel.test.tsx (reference pattern for component test)
    - src/components/session-detail.tsx (existing data-*-panel regex patterns — extend with data-swells-panel)
    - "src/app/modules/[slug]/panel/page.tsx" (existing panel conditionals — add swells case)
    - src/components/quick-ref-panel.tsx (existing panel conditionals — add swells case)
    - src/components/patch-detail.tsx (existing panel conditionals — add swells case)
    - references/swells-manual.pdf (if downloaded — cross-reference with Intellijel product photos per D-09 for control placement)
  </read_first>
  <behavior>
    - Test 1: SwellsPanel renders without crashing (container has SVG element)
    - Test 2: SVG has a viewBox attribute with reasonable dimensions for 20HP
    - Test 3: SwellsPanel component is exported as named export
  </behavior>
  <action>
**Part A: Create SwellsPanel component** (`src/components/swells-panel.tsx`)

Follow the MathsPanel pattern:
1. Import CONTROL_METADATA, CONTROL_POSITIONS, SECTION_BOUNDS, midiToRotation, midiToSliderPosition from `@/lib/swells-panel-data`
2. Define SwellsPanel props interface matching MathsPanel
3. Render an SVG with viewBox based on Swells 20HP dimensions (widest panel in this phase)
4. Render loop over CONTROL_METADATA entries — note Swells uses slider type for faders and switch type for toggles, in addition to knobs/buttons/jacks
5. Render sliders as vertical fader elements, switches as 3-position toggles, knobs, buttons, jacks
6. Export SwellsPanel as named export

Per D-09: Cross-reference manual diagram (if PDF available) with Intellijel product photos for accuracy.

**Part B: Create component test** (`src/components/__tests__/swells-panel.test.tsx`)

Follow maths-panel.test.tsx pattern. Test: renders, viewBox, export.

**Part C: Update integration files** for Swells panel

1. **session-detail.tsx**: Add `data-swells-panel` regex pattern and parser. IMPORTANT: Use the cable-safe regex `/<div data-swells-panel((?:[^>"]|"[^"]*")*)>\s*<\/div>/g` (not naive `[^>]*`) per synth-panel-builder SKILL.md.
2. **panel/page.tsx**: Add slug === 'swells' conditional rendering SwellsPanel.
3. **quick-ref-panel.tsx**: Add instrument === 'swells' conditional.
4. **patch-detail.tsx**: Add instrument === 'swells' conditional.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/components/__tests__/swells-panel.test.tsx && npx vitest run src/lib/__tests__/swells-panel-data.test.ts</automated>
  </verify>
  <done>SwellsPanel component renders all controls including sliders and switches, component test passes, session-detail.tsx parses data-swells-panel markers, panel/page.tsx renders Swells panel, quick-ref-panel.tsx and patch-detail.tsx include Swells conditionals.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Swells manual PDF download was attempted. If automated download failed, the user needs to download manually before panel positions can be hand-placed in Phase 28.</what-built>
  <how-to-verify>
    1. Run: `file references/swells-manual.pdf` — should report "PDF document"
    2. If it does NOT exist or is not a PDF: visit https://intellijel.com/shop/eurorack/swells/ and download the manual
    3. Save as `references/swells-manual.pdf`
    4. If the PDF is available: type "approved"
    5. If you cannot get the PDF right now: type "skip" (panel positions will remain placeholder)
  </how-to-verify>
  <resume-signal>Type "approved" if PDF exists, "skip" to continue without it</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/swells-panel-data.test.ts` passes
- `npx vitest run src/components/__tests__/swells-panel.test.tsx` passes
- 6 session files in each of the 3 locations
- Session files include `data-swells-panel` markers
- Overview page has Architecture, Controls Reference, Basic Patch, and 9 Algorithms Reference
- All session frontmatter has instrument_type: eurorack_module, instrument: swells
- Swells manual PDF exists or download instructions documented
- session-detail.tsx handles data-swells-panel markers
- panel/page.tsx renders SwellsPanel for slug 'swells'
</verification>

<success_criteria>
- Swells panel data file complete with 35+ controls and passing test suite
- SwellsPanel component renders without crashing, test passes
- Integration files updated for Swells
- 6 sessions cover foundations, 3 algorithm groups, Swell Generator, and Freeze/Reverse per D-03
- All content triple-written with data-swells-panel markers and follows ADHD-friendly format
- Manual PDF downloaded or clear fallback instructions
</success_criteria>

<output>
After completion, create `.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-03-SUMMARY.md`
</output>
