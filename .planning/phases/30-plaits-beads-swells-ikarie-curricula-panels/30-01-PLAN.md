---
phase: 30-plaits-beads-swells-ikarie-curricula-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/plaits-panel-data.ts
  - src/lib/__tests__/plaits-panel-data.test.ts
  - src/components/plaits-panel.tsx
  - src/components/__tests__/plaits-panel.test.tsx
  - src/components/session-detail.tsx
  - "src/app/modules/[slug]/panel/page.tsx"
  - src/components/quick-ref-panel.tsx
  - src/components/patch-detail.tsx
  - modules/plaits/overview.md
  - src/content/modules/plaits/overview.md
  - sessions/plaits/01-foundations-controls-model-selection.md
  - sessions/plaits/02-classic-waveforms-filtered-noise.md
  - sessions/plaits/03-waveshaping-granular-cloud.md
  - sessions/plaits/04-fm-synthesis-particle-noise.md
  - sessions/plaits/05-formant-vowel-speech.md
  - sessions/plaits/06-harmonic-inharmonic-string.md
  - sessions/plaits/07-wavetable-modal-resonator.md
  - sessions/plaits/08-chords-analog-drums.md
  - src/content/sessions/plaits/01-foundations-controls-model-selection.md
  - src/content/sessions/plaits/02-classic-waveforms-filtered-noise.md
  - src/content/sessions/plaits/03-waveshaping-granular-cloud.md
  - src/content/sessions/plaits/04-fm-synthesis-particle-noise.md
  - src/content/sessions/plaits/05-formant-vowel-speech.md
  - src/content/sessions/plaits/06-harmonic-inharmonic-string.md
  - src/content/sessions/plaits/07-wavetable-modal-resonator.md
  - src/content/sessions/plaits/08-chords-analog-drums.md
autonomous: true
requirements: [CURR-02, PANEL-03]

must_haves:
  truths:
    - "Plaits panel data file exports ~19 functional control entries plus LED array matching the physical module"
    - "Every jack has a signalType, every control has id/name/module/type"
    - "Control IDs follow plaits convention (knob-plaits-frequency, jack-plaits-out, etc.)"
    - "Panel data test suite passes with all entries validated"
    - "Plaits panel component renders without crashing with all 19+ controls visible"
    - "Plaits overview page has architecture, controls reference, and init state"
    - "8 Plaits sessions exist covering all 16 synthesis modes in mode-pair structure"
    - "All sessions are triple-written to sessions/, src/content/, and ~/song/"
    - "All sessions follow 15-30 min ADHD format with tangible output"
    - "Session markdown files include data-plaits-panel markers for inline panel rendering"
  artifacts:
    - path: "src/lib/plaits-panel-data.ts"
      provides: "Complete Plaits panel control metadata, positions (placeholder), section bounds"
      exports: ["CONTROL_METADATA", "CONTROL_POSITIONS", "SECTION_BOUNDS", "PlaitsControlMeta", "midiToRotation"]
    - path: "src/lib/__tests__/plaits-panel-data.test.ts"
      provides: "Validation tests for panel data completeness and correctness"
    - path: "src/components/plaits-panel.tsx"
      provides: "React SVG panel component rendering all Plaits controls"
      exports: ["PlaitsPanel"]
    - path: "src/components/__tests__/plaits-panel.test.tsx"
      provides: "Component render test for PlaitsPanel"
    - path: "modules/plaits/overview.md"
      provides: "Full overview with architecture, controls reference, init state"
    - path: "sessions/plaits/01-foundations-controls-model-selection.md"
      provides: "Session 1 of 8 in Plaits curriculum"
  key_links:
    - from: "src/lib/__tests__/plaits-panel-data.test.ts"
      to: "src/lib/plaits-panel-data.ts"
      via: "import CONTROL_METADATA, SECTION_BOUNDS, midiToRotation"
      pattern: "import.*from.*plaits-panel-data"
    - from: "src/components/plaits-panel.tsx"
      to: "src/lib/plaits-panel-data.ts"
      via: "import CONTROL_METADATA, CONTROL_POSITIONS, SECTION_BOUNDS"
      pattern: "import.*from.*plaits-panel-data"
    - from: "src/components/session-detail.tsx"
      to: "src/components/plaits-panel.tsx"
      via: "data-plaits-panel marker regex + dynamic import"
      pattern: "data-plaits-panel|PlaitsPanel"
---

<objective>
Create the complete Plaits curriculum (panel data + panel component + overview + 8 sessions) following the pattern proven by Maths in Phase 29.

Purpose: Plaits is the first of four modules in this phase. Its 16 synthesis modes across 2 banks provide the richest curriculum. The mode-pair structure (per D-01) covers 2 related synthesis modes per session, making all 16 modes accessible in 8 focused sessions.

Output: Panel data file with tests, panel SVG component with tests, integration updates, expanded overview page, 8 sessions triple-written to all locations with data-plaits-panel markers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-CONTEXT.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md
@.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-UI-SPEC.md

@src/lib/maths-panel-data.ts
@src/lib/__tests__/maths-panel-data.test.ts
@src/components/maths-panel.tsx
@src/components/__tests__/maths-panel.test.tsx
@src/components/session-detail.tsx
@src/app/modules/[slug]/panel/page.tsx
@src/components/quick-ref-panel.tsx
@src/components/patch-detail.tsx
@modules/maths/overview.md
@sessions/maths/01-foundations-rise-fall-envelope.md
@.claude/skills/synth-panel-builder/SKILL.md
@references/plaits-manual.pdf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Plaits panel data file and test suite</name>
  <files>src/lib/plaits-panel-data.ts, src/lib/__tests__/plaits-panel-data.test.ts</files>
  <read_first>
    - src/lib/maths-panel-data.ts (reference pattern for CONTROL_METADATA, SECTION_BOUNDS, midiToRotation — follow this exact structure)
    - src/lib/__tests__/maths-panel-data.test.ts (reference pattern for test structure)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md (verified control inventory — section "Plaits (12HP) -- Verified from Manual PDF pp. 3-5")
    - references/plaits-manual.pdf (pages 3-5 for front plate diagram and control lettering A-F, 1-7). Cross-reference with manufacturer product photos per D-09
  </read_first>
  <behavior>
    - Test 1: CONTROL_METADATA has at least 19 entries (2 buttons, 5 knobs, 3 attenuverters, 7 input jacks, 2 output jacks; LEDs optional as decorative)
    - Test 2: Every entry has non-empty id, name, module, and type fields
    - Test 3: Every entry type is one of: knob, button, jack-in, jack-out, led
    - Test 4: Every jack (jack-in or jack-out) has a valid signalType (audio, cv, gate, modulation)
    - Test 5: Every entry module is 'plaits' (single-module panel)
    - Test 6: SECTION_BOUNDS has entry for 'plaits' with positive width and height
    - Test 7: midiToRotation(0) returns -135, midiToRotation(127) returns 135, midiToRotation(63) returns approximately 0
    - Test 8: CONTROL_POSITIONS has an entry for every key in CONTROL_METADATA
  </behavior>
  <action>
Create the test file first, then the data file.

**Interface:**
```typescript
export interface PlaitsControlMeta {
  id: string;
  name: string;
  module: string;  // always 'plaits'
  type: 'knob' | 'button' | 'jack-in' | 'jack-out' | 'led';
  signalType?: 'audio' | 'cv' | 'gate' | 'modulation';
}
```

**CONTROL_METADATA entries (from RESEARCH.md verified inventory, manual pp. 3-5):**

Knobs (5):
- `knob-plaits-frequency` — FREQUENCY, large coarse tuning knob (control B in manual)
- `knob-plaits-harmonics` — HARMONICS, large model-dependent tone (control C)
- `knob-plaits-timbre` — TIMBRE, medium model-dependent (control D)
- `knob-plaits-morph` — MORPH, medium model-dependent (control E)

Attenuverters (3, type: 'knob'):
- `knob-plaits-timbre-attenuverter` — TIMBRE ATT, small attenuverter for TIMBRE CV
- `knob-plaits-fm-attenuverter` — FM ATT, small attenuverter for FM CV
- `knob-plaits-morph-attenuverter` — MORPH ATT, small attenuverter for MORPH CV

Note on attenuverter dual-function (RESEARCH Pitfall 9): When CV input is unpatched and TRIG is patched, attenuverter controls internal decaying envelope modulation amount. Document this in session 01.

Buttons (2):
- `button-plaits-model-1` — MODEL 1, cycles through Bank 1 (8 pitched modes)
- `button-plaits-model-2` — MODEL 2, cycles through Bank 2 (8 noise/percussion modes)

Input jacks (7):
- `jack-plaits-model-cv` — MODEL, cv
- `jack-plaits-timbre-cv` — TIMBRE, cv
- `jack-plaits-fm-cv` — FM, cv
- `jack-plaits-morph-cv` — MORPH/HARMO, cv
- `jack-plaits-trig` — TRIG, gate (triggers internal envelope, excites percussive models)
- `jack-plaits-level` — LEVEL, cv (opens internal LPG)
- `jack-plaits-v-oct` — V/OCT, cv

Output jacks (2):
- `jack-plaits-out` — OUT, audio (main output)
- `jack-plaits-aux` — AUX, audio (variant/sidekick output)

LEDs (16, optional decorative — include as 2 entries for the 2 columns, or individual entries):
Include at minimum 2 LED entries: `led-plaits-bank-1` and `led-plaits-bank-2` representing the two 8-LED columns. Or include all 16 as individual LEDs if following granular approach.

**CONTROL_POSITIONS** — All entries with `{ x: 0, y: 0 }` placeholder. Comment: `// Placeholder — hand-place from Plaits manual pp. 3-5 when Phase 28 panel renderer is ready`

**SECTION_BOUNDS** — Single entry `'plaits': { x: 0, y: 0, width: 100, height: 100 }` placeholder.

**midiToRotation** — `return -135 + (value / 127) * 270;`
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/lib/__tests__/plaits-panel-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/plaits-panel-data.ts contains `export interface PlaitsControlMeta`
    - src/lib/plaits-panel-data.ts contains `export const CONTROL_METADATA: Record<string, PlaitsControlMeta>`
    - src/lib/plaits-panel-data.ts contains `export const CONTROL_POSITIONS`
    - src/lib/plaits-panel-data.ts contains `export const SECTION_BOUNDS`
    - src/lib/plaits-panel-data.ts contains `export function midiToRotation`
    - src/lib/plaits-panel-data.ts contains `knob-plaits-frequency`
    - src/lib/plaits-panel-data.ts contains `jack-plaits-out`
    - src/lib/plaits-panel-data.ts contains `jack-plaits-v-oct`
    - src/lib/plaits-panel-data.ts contains `button-plaits-model-1`
    - src/lib/__tests__/plaits-panel-data.test.ts exits 0
  </acceptance_criteria>
  <done>Panel data file exports 19+ controls with all metadata fields, test suite passes with 8+ test cases</done>
</task>

<task type="auto">
  <name>Task 2: Create Plaits overview and 8 curriculum sessions with triple-write</name>
  <files>modules/plaits/overview.md, src/content/modules/plaits/overview.md, sessions/plaits/01-foundations-controls-model-selection.md, sessions/plaits/02-classic-waveforms-filtered-noise.md, sessions/plaits/03-waveshaping-granular-cloud.md, sessions/plaits/04-fm-synthesis-particle-noise.md, sessions/plaits/05-formant-vowel-speech.md, sessions/plaits/06-harmonic-inharmonic-string.md, sessions/plaits/07-wavetable-modal-resonator.md, sessions/plaits/08-chords-analog-drums.md</files>
  <read_first>
    - modules/plaits/overview.md (current placeholder — needs full expansion)
    - modules/maths/overview.md (reference pattern for architecture, controls reference, init state, further reading)
    - sessions/maths/01-foundations-rise-fall-envelope.md (reference session format — frontmatter, warm-up, exercises, output)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-RESEARCH.md (16 synthesis modes list, mode pairings table, control descriptions)
    - .planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-CONTEXT.md (locked decisions D-01 through D-07)
    - references/plaits-manual.pdf (pp. 3-11 for controls and mode descriptions)
  </read_first>
  <action>
**Step 1: Expand Plaits overview page** (`modules/plaits/overview.md`)

Follow the Maths overview pattern exactly. Include:

1. **Frontmatter**: `type: overview`, `instrument: plaits`, `title: "Plaits Overview"`, `manufacturer: "Mutable Instruments"`
2. **Heading**: `# Mutable Instruments Plaits` with tagline `*Macro oscillator with 16 synthesis models*` and `**12HP** | VCO`
3. **Architecture section**: Describe the macro oscillator concept — 16 models across 2 banks (Bank 1: pitched, Bank 2: noise/percussion), model selection via buttons+LEDs, built-in LPG (low-pass gate) with LEVEL and TRIG inputs, internal decay envelope that auto-modulates parameters through attenuverters when CV is unpatched. Explain the AUX output provides a complementary/variant signal (different per model).
4. **Controls Reference**: Table with columns `Control | Type | Range | Function` for all controls (FREQUENCY, HARMONICS, TIMBRE, MORPH, 3 attenuverters, 2 model buttons, 7 input jacks, 2 output jacks). Group by section.
5. **Basic Patch (Init State)**: Recommend: FREQUENCY at noon (C3), HARMONICS at noon, TIMBRE at noon, MORPH at noon, all attenuverters at noon (12 o'clock), Model 1 selected (first LED in bank 1 — VA oscillator pair). Patch V/OCT from keyboard/sequencer, OUT to mixer/VCA.
6. **Further Reading**: Plaits Manual (references/plaits-manual.pdf), Mutable Instruments website archive.

Triple-write: copy to `src/content/modules/plaits/overview.md` and `~/song/modules/plaits/overview.md`.

**Step 2: Create 8 sessions** per D-01 mode-pair structure (from RESEARCH.md "Recommended Plaits Mode Pairings"):

All sessions use this frontmatter pattern:
```yaml
  ---
title: 'Session NN: Title'
session_number: N
duration: 20-25
prerequisite: N-1  # null for session 1
output_type: patch  # or technique
difficulty: beginner/intermediate/advanced
tags: [relevant, tags]
instrument: plaits
instrument_type: eurorack_module
reference: 'Plaits Manual pp. X-Y'
section: Section Name
  ---
```

**Session 01 — Foundations: Controls, Model Selection & Internal Voice** (section: Foundations, difficulty: beginner, duration: 25, output_type: technique, prerequisite: null)
- Warm-up: None (first session). Describe the physical panel layout.
- Exercises: (1) Set init state, patch V/OCT+OUT, play first model. (2) Cycle through Bank 1 models with button, listen to each. (3) Explore HARMONICS and TIMBRE on VA oscillator. (4) Patch TRIG and observe internal envelope + LPG. (5) Demonstrate attenuverter dual-function — with TRIG patched and CV unpatched, attenuverter controls internal envelope depth (RESEARCH Pitfall 9).
- Output: Documented first impressions of 3 favorite Bank 1 models.
- Reference: Plaits Manual pp. 3-6
- Tags: foundations, model-selection, internal-lpg, controls-overview
- Include `data-plaits-panel` markers at relevant exercise steps to highlight active controls

**Session 02 — Classic Waveforms & Filtered Noise** (section: Mode Pairs, difficulty: beginner, duration: 20)
- Bank 1 Mode 1: Pair of classic waveforms (VA synthesis) — HARMONICS sweeps sub/pulse/saw/square mix
- Bank 2 Mode 2: Filtered noise — HARMONICS controls filter type, TIMBRE controls brightness
- Exercises: Compare tonal (VA) vs textural (noise), explore MORPH on each, create a patch layering both via AUX
- Output: Patch using VA oscillator with filtered noise on AUX
- Reference: Plaits Manual pp. 7, 9
- Include `data-plaits-panel` markers highlighting relevant knobs per mode

**Session 03 — Waveshaping & Granular Cloud** (section: Mode Pairs, difficulty: intermediate, duration: 20)
- Bank 1 Mode 2: Waveshaping oscillator
- Bank 2 Mode 1: Granular cloud
- Reference: Plaits Manual pp. 7, 9

**Session 04 — FM Synthesis & Particle Noise** (section: Mode Pairs, difficulty: intermediate, duration: 25)
- Bank 1 Mode 3: Two-operator FM
- Bank 2 Mode 3: Particle noise
- Reference: Plaits Manual pp. 8, 10

**Session 05 — Formant Oscillator & Vowel/Speech** (section: Mode Pairs, difficulty: intermediate, duration: 20)
- Bank 1 Mode 4: Granular formant oscillator
- Bank 2 Mode 8: Vowel and speech synthesis
- Both are formant/voice-related — natural pairing
- Reference: Plaits Manual pp. 8, 11

**Session 06 — Harmonic & Inharmonic String** (section: Mode Pairs, difficulty: intermediate, duration: 25)
- Bank 1 Mode 5: Harmonic oscillator
- Bank 2 Mode 4: Inharmonic string modeling
- Harmonic vs inharmonic contrast
- Reference: Plaits Manual pp. 8, 10

**Session 07 — Wavetable & Modal Resonator** (section: Mode Pairs, difficulty: advanced, duration: 25)
- Bank 1 Mode 6: Wavetable oscillator
- Bank 2 Mode 5: Modal resonator (mini-Rings)
- Both are lookup/model-based synthesis
- Reference: Plaits Manual pp. 9, 10

**Session 08 — Chords & Analog Drums** (section: Mode Pairs, difficulty: advanced, duration: 25)
- Bank 1 Mode 7-8: Chords (4-note) — HARMONICS selects chord type
- Bank 2 Mode 6-8: Analog bass drum, snare drum, hi-hat models
- Both produce complete musical elements — "instant instruments"
- This is the creative culmination session per D-05
- Output: A short rhythmic composition using chord stabs and drum models
- Reference: Plaits Manual pp. 9, 10-11

Each session follows ADHD format: objective up front, 5-min warm-up tip box, 3-4 exercises max with clear steps, hard 20-25 min time box, session output documented at end.

**Include `data-plaits-panel` markers** in all sessions at exercise steps where specific controls are being used. Use the format from RESEARCH.md:
```html
<div data-plaits-panel
  data-knobs="knob-plaits-frequency:64,knob-plaits-timbre:100"
  data-highlights="jack-plaits-v-oct:blue,jack-plaits-out:amber"
></div>
```

**Triple-write all 8 sessions**: For each session file, write to:
1. `sessions/plaits/NN-*.md`
2. `src/content/sessions/plaits/NN-*.md`
3. `~/song/sessions/plaits/NN-*.md`

Create the `sessions/plaits/`, `src/content/sessions/plaits/`, and `~/song/sessions/plaits/` directories if they don't exist.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && ls sessions/plaits/*.md | wc -l && ls src/content/sessions/plaits/*.md | wc -l && ls ~/song/sessions/plaits/*.md | wc -l && head -15 sessions/plaits/01-foundations-controls-model-selection.md</automated>
  </verify>
  <acceptance_criteria>
    - modules/plaits/overview.md contains `## Architecture`
    - modules/plaits/overview.md contains `## Controls Reference`
    - modules/plaits/overview.md contains `## Basic Patch`
    - modules/plaits/overview.md contains `instrument: plaits`
    - src/content/modules/plaits/overview.md is identical to modules/plaits/overview.md
    - 8 session files exist in sessions/plaits/ (01 through 08)
    - 8 session files exist in src/content/sessions/plaits/
    - 8 session files exist in ~/song/sessions/plaits/
    - sessions/plaits/01-foundations-controls-model-selection.md contains `instrument_type: eurorack_module`
    - sessions/plaits/01-foundations-controls-model-selection.md contains `instrument: plaits`
    - sessions/plaits/01-foundations-controls-model-selection.md contains `section: Foundations`
    - sessions/plaits/01-foundations-controls-model-selection.md contains `data-plaits-panel`
    - sessions/plaits/08-chords-analog-drums.md contains `session_number: 8`
    - All session files contain `instrument_type: eurorack_module`
    - All session files contain `duration:` with value between 15 and 30
  </acceptance_criteria>
  <done>Plaits overview expanded with architecture/controls/init-state. 8 sessions covering all 16 modes in mode-pair structure, triple-written to all 3 locations, with data-plaits-panel markers, following ADHD-friendly format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Plaits panel component, component test, and integration updates</name>
  <files>src/components/plaits-panel.tsx, src/components/__tests__/plaits-panel.test.tsx, src/components/session-detail.tsx, src/app/modules/[slug]/panel/page.tsx, src/components/quick-ref-panel.tsx, src/components/patch-detail.tsx</files>
  <read_first>
    - src/components/maths-panel.tsx (reference pattern for panel component — props interface, SVG structure, control rendering loop)
    - src/components/__tests__/maths-panel.test.tsx (reference pattern for component test — motion mock, render test, viewBox test)
    - src/components/session-detail.tsx (find existing data-*-panel regex patterns to extend with data-plaits-panel)
    - "src/app/modules/[slug]/panel/page.tsx" (find existing panel conditional renders to add plaits case)
    - src/components/quick-ref-panel.tsx (find existing panel conditional renders to add plaits case)
    - src/components/patch-detail.tsx (find existing panel conditional renders to add plaits case)
    - references/plaits-manual.pdf (pages 3-5 for front plate diagram — cross-reference with Mutable Instruments product photos per D-09 for control placement verification)
  </read_first>
  <behavior>
    - Test 1: PlaitsPanel renders without crashing (container has SVG element)
    - Test 2: SVG has a viewBox attribute with reasonable dimensions
    - Test 3: PlaitsPanel component is exported as named export
  </behavior>
  <action>
**Part A: Create PlaitsPanel component** (`src/components/plaits-panel.tsx`)

Follow the MathsPanel pattern exactly:
1. Import CONTROL_METADATA, CONTROL_POSITIONS, SECTION_BOUNDS, midiToRotation from `@/lib/plaits-panel-data`
2. Define PlaitsPanel props interface matching MathsPanel (knobValues, highlights, activeSections, zoomSections, cables, onKnobChange, className)
3. Render an SVG with viewBox based on Plaits 12HP dimensions (narrower than Maths)
4. Render loop over CONTROL_METADATA entries, placing each control at its CONTROL_POSITIONS coordinates
5. Render knobs (large for FREQUENCY/HARMONICS, medium for TIMBRE/MORPH, small for attenuverters), buttons, jacks (input dark, output filled white per CLAUDE.md conventions), and LED columns
6. Export PlaitsPanel as named export

Per D-09: Cross-reference the Plaits manual front plate diagram (pp. 3-5) with manufacturer product photos to verify control layout accuracy before finalizing positions.

**Part B: Create component test** (`src/components/__tests__/plaits-panel.test.tsx`)

Follow the maths-panel.test.tsx pattern:
1. Mock motion/react with the same Proxy pattern
2. Import PlaitsPanel after mock
3. Test: renders without crashing (querySelector('svg') is truthy)
4. Test: SVG viewBox contains expected dimensions
5. Test: PlaitsPanel is a valid export

**Part C: Update integration files** to recognize Plaits panel

1. **session-detail.tsx**: Add regex pattern for `data-plaits-panel` markers alongside existing patterns (data-cascadia-panel, data-maths-panel, etc.). Add parser to extract knob values, highlights, and cables from the marker attributes. Render PlaitsPanel inline at the marker location. IMPORTANT: Use the cable-safe regex `/<div data-plaits-panel((?:[^>"]|"[^"]*")*)>\s*<\/div>/g` (not naive `[^>]*`) per synth-panel-builder SKILL.md -- the `>` in cable syntax breaks naive attribute patterns.

2. **panel/page.tsx** (`src/app/modules/[slug]/panel/page.tsx`): Add a conditional case for slug === 'plaits' that renders PlaitsPanel instead of "coming soon".

3. **quick-ref-panel.tsx**: Add conditional render for instrument === 'plaits' that renders PlaitsPanel.

4. **patch-detail.tsx**: Add conditional render for instrument === 'plaits' that renders PlaitsPanel with knob values from patch data.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/components/__tests__/plaits-panel.test.tsx && npx vitest run src/lib/__tests__/plaits-panel-data.test.ts</automated>
  </verify>
  <done>PlaitsPanel component renders all controls, component test passes, session-detail.tsx parses data-plaits-panel markers, panel/page.tsx renders Plaits panel, quick-ref-panel.tsx and patch-detail.tsx include Plaits conditionals.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/plaits-panel-data.test.ts` passes (all tests green)
- `npx vitest run src/components/__tests__/plaits-panel.test.tsx` passes (component renders)
- 8 session files in each of the 3 locations (sessions/, src/content/sessions/, ~/song/sessions/)
- Session files include `data-plaits-panel` markers
- Overview page has Architecture, Controls Reference, and Basic Patch sections
- All session frontmatter has instrument_type: eurorack_module, instrument: plaits
- Session 01 explains attenuverter dual-function per Pitfall 9
- session-detail.tsx handles data-plaits-panel markers
- panel/page.tsx renders PlaitsPanel for slug 'plaits'
</verification>

<success_criteria>
- Plaits panel data file complete with 19+ controls and passing test suite
- PlaitsPanel component renders without crashing, test passes
- Integration files updated (session-detail, panel/page, quick-ref-panel, patch-detail)
- 8 sessions cover all 16 synthesis modes via mode-pair structure per D-01
- All content triple-written and follows ADHD-friendly format per D-05
- Overview page matches Maths overview pattern
</success_criteria>

<output>
After completion, create `.planning/phases/30-plaits-beads-swells-ikarie-curricula-panels/30-01-SUMMARY.md`
</output>
