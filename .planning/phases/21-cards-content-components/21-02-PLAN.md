---
phase: 21-cards-content-components
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/components/hero-card.tsx
  - src/components/patch-card.tsx
  - src/components/module-card.tsx
  - src/components/instrument-card.tsx
  - src/components/count-card.tsx
  - src/components/session-row.tsx
  - src/components/__tests__/count-card.test.tsx
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - CONTENT-02

must_haves:
  truths:
    - "All 5 card components (HeroCard, PatchCard, ModuleCard, InstrumentCard, CountCard) use the .card CSS class"
    - "HeroCard additionally uses .card-hero for its distinct accent-left-bar treatment"
    - "CountCard no longer has inline focus-visible Tailwind utilities (inherits global)"
    - "SessionRow does NOT use .card class -- keeps its list-item identity with bg-surface hover"
    - "Card unification tests pass for all 6 components"
    - "All existing tests pass after migration"
  artifacts:
    - path: "src/components/hero-card.tsx"
      provides: "HeroCard with .card .card-hero classes"
      contains: "card card-hero"
    - path: "src/components/patch-card.tsx"
      provides: "PatchCard with .card class"
      contains: "card block"
    - path: "src/components/module-card.tsx"
      provides: "ModuleCard with .card class"
      contains: "card"
    - path: "src/components/instrument-card.tsx"
      provides: "InstrumentCard with .card class"
      contains: "card block"
    - path: "src/components/count-card.tsx"
      provides: "CountCard with .card class, no inline focus-visible"
    - path: "src/components/session-row.tsx"
      provides: "SessionRow unchanged (no .card class)"
  key_links:
    - from: "src/components/hero-card.tsx"
      to: "src/app/globals.css"
      via: ".card and .card-hero CSS classes"
      pattern: "card card-hero"
    - from: "src/components/patch-card.tsx"
      to: "src/app/globals.css"
      via: ".card CSS class"
      pattern: 'className="card'
    - from: "src/components/__tests__/card-unification.test.tsx"
      to: "all card components"
      via: "render + className assertion"
      pattern: 'toContain.*card'
---

<objective>
Migrate all card component classNames to use the shared .card base class from globals.css and verify unification.

Purpose: Replace inconsistent per-component border-radius, padding, border, and hover styles with the unified .card class. Remove redundant inline focus-visible from CountCard. Verify with automated tests and visual checkpoint.
Output: All 5 card components using .card class, SessionRow unchanged, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cards-content-components/21-CONTEXT.md
@.planning/phases/21-cards-content-components/21-RESEARCH.md
@.planning/phases/21-cards-content-components/21-UI-SPEC.md
@.planning/phases/21-cards-content-components/21-01-SUMMARY.md

<interfaces>
<!-- CSS classes created by Plan 01 in globals.css -->
```css
.card {
  background-color: var(--color-surface);
  border: 1px solid var(--color-border-subtle);
  border-radius: 8px;
  padding: var(--spacing-lg);
  transition: border-color 150ms, transform 150ms, box-shadow 150ms;
}
.card:hover {
  border-color: var(--color-accent);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px oklch(0.85 0.18 105 / 0.06);
}
.card-hero {
  border-left: 3px solid var(--color-accent);
  border-radius: 2px 8px 8px 2px;
  padding: var(--spacing-2xl);
}
:where(:focus-visible) {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

<!-- Current component classNames (BEFORE migration) -->
HeroCard outer div: className="bg-surface max-w-[480px] w-full p-2xl rounded-lg"
PatchCard Link: className="block bg-surface rounded-[6px] p-lg border border-transparent hover:border-accent transition-colors"
ModuleCard Link: className="bg-surface rounded-[6px] p-lg border border-transparent hover:border-accent transition-colors block"
InstrumentCard Link: className="block bg-surface p-2xl rounded-md border border-surface hover:border-accent transition-colors duration-150"
CountCard Link: className="bg-surface p-lg rounded-lg flex flex-col items-center cursor-pointer transition-[transform,box-shadow] duration-150 ease-out hover:-translate-y-[2px] hover:shadow-[0_4px_12px_rgba(200,255,0,0.08)] focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
SessionRow Link: className="group flex items-start justify-between py-md px-sm rounded transition-colors hover:bg-surface"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate card component classNames to .card base class</name>
  <files>
    src/components/hero-card.tsx
    src/components/patch-card.tsx
    src/components/module-card.tsx
    src/components/instrument-card.tsx
    src/components/count-card.tsx
    src/components/session-row.tsx
    src/components/__tests__/count-card.test.tsx
  </files>
  <read_first>
    - src/components/hero-card.tsx
    - src/components/patch-card.tsx
    - src/components/module-card.tsx
    - src/components/instrument-card.tsx
    - src/components/count-card.tsx
    - src/components/session-row.tsx
    - src/components/__tests__/count-card.test.tsx
    - src/app/globals.css (verify Plan 01 CSS rules are in place)
  </read_first>
  <action>
Update each component's className to use the `.card` CSS class from globals.css. Remove all properties now handled by `.card` (bg-surface, border, border-radius, padding, hover border, hover translate, hover shadow, transition). Keep only layout-specific utilities (flex, gap, block, cursor, width constraints).

**HeroCard** (src/components/hero-card.tsx) -- per D-01, D-03:
Change outer div className from:
```
"bg-surface max-w-[480px] w-full p-2xl rounded-lg"
```
To:
```
"card card-hero max-w-[480px] w-full"
```
Removed: bg-surface (in .card), p-2xl (in .card-hero as --spacing-2xl), rounded-lg (in .card-hero as 2px 8px 8px 2px).
Kept: max-w-[480px] w-full (layout constraint, not visual envelope).

Also update the Link button inside HeroCard -- it currently has `focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-bg` which uses `:focus` not `:focus-visible`. Change to just rely on the global `:focus-visible` for keyboard focus. The existing styles use `focus:` prefix (triggers on mouse click too). Remove the focus ring utilities and let the global `:where(:focus-visible)` handle it. Keep the `hover:brightness-110` and other non-focus styles. Change:
```
"inline-flex items-center justify-center bg-accent text-bg font-bold py-md px-xl rounded min-h-[48px] hover:brightness-110 focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-bg transition-[filter]"
```
To:
```
"inline-flex items-center justify-center bg-accent text-bg font-bold py-md px-xl rounded min-h-[48px] hover:brightness-110 transition-[filter]"
```

**PatchCard** (src/components/patch-card.tsx) -- per D-01, D-02, D-10:
Change Link className from:
```
"block bg-surface rounded-[6px] p-lg border border-transparent hover:border-accent transition-colors"
```
To:
```
"card block"
```
Removed: bg-surface, rounded-[6px], p-lg, border, border-transparent, hover:border-accent, transition-colors (all in .card).
Kept: block (display utility).

**ModuleCard** (src/components/module-card.tsx) -- per D-01, D-02, D-10:
Change Link className from:
```
"bg-surface rounded-[6px] p-lg border border-transparent hover:border-accent transition-colors block"
```
To:
```
"card block"
```

**InstrumentCard** (src/components/instrument-card.tsx) -- per D-01, D-02, D-10:
Change Link className from:
```
"block bg-surface p-2xl rounded-md border border-surface hover:border-accent transition-colors duration-150"
```
To:
```
"card block"
```
Note: InstrumentCard previously had p-2xl (48px) padding, but the unified .card uses --spacing-lg (24px). Per D-01, all cards share the same padding. The content layout (flex flex-col gap-sm) handles internal spacing adequately at 24px.

**CountCard** (src/components/count-card.tsx) -- per D-01, D-06, D-10:
Change Link className from:
```
"bg-surface p-lg rounded-lg flex flex-col items-center cursor-pointer
 transition-[transform,box-shadow] duration-150 ease-out
 hover:-translate-y-[2px] hover:shadow-[0_4px_12px_rgba(200,255,0,0.08)]
 focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
```
To:
```
"card flex flex-col items-center cursor-pointer"
```
Removed: bg-surface, p-lg, rounded-lg (in .card), transition-*, hover:-translate-y-*, hover:shadow-* (in .card:hover), ALL focus-visible:* utilities (per D-06, inherits global :where(:focus-visible)).
Kept: flex flex-col items-center cursor-pointer (layout utilities).

**SessionRow** (src/components/session-row.tsx) -- per D-04, D-11:
NO CHANGES to className. SessionRow keeps its current:
```
"group flex items-start justify-between py-md px-sm rounded transition-colors hover:bg-surface"
```
SessionRow is NOT a card. It inherits global :focus-visible for keyboard navigation (which it gets automatically from the :where(:focus-visible) rule). No border, no lift, no .card class.

**CountCard test** (src/components/__tests__/count-card.test.tsx):
The existing test at line 44 checks `link.className.toContain('hover:-translate-y-')` and line 50 checks `link.className.toContain('focus-visible:outline')`. Both of these will fail after migration since those Tailwind utilities are removed. Update:
- Change line 44 test "includes hover translate-y classes for lift affordance" to check `link.className.toContain('card')` instead (the .card CSS class handles hover lift)
- Change line 50 test "includes focus-visible outline classes for keyboard accessibility" to check that the global CSS handles it -- remove this test entirely OR change assertion to verify that inline focus-visible classes are NOT present: `expect(link.className).not.toContain('focus-visible:outline')`
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/card-unification.test.tsx src/components/__tests__/count-card.test.tsx -x --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - hero-card.tsx outer div className contains "card card-hero" and "max-w-[480px] w-full"
    - hero-card.tsx outer div className does NOT contain "bg-surface", "p-2xl", "rounded-lg"
    - hero-card.tsx Link className does NOT contain "focus:ring"
    - patch-card.tsx Link className is exactly "card block"
    - module-card.tsx Link className is exactly "card block"
    - instrument-card.tsx Link className is exactly "card block"
    - count-card.tsx Link className contains "card" and "flex flex-col items-center cursor-pointer"
    - count-card.tsx Link className does NOT contain "focus-visible:", "hover:-translate-y", "hover:shadow-", "rounded-lg", "bg-surface"
    - session-row.tsx Link className is UNCHANGED (still "group flex items-start justify-between py-md px-sm rounded transition-colors hover:bg-surface")
    - `npx vitest run src/components/__tests__/card-unification.test.tsx -x` passes (all 6 assertions green)
    - `npx vitest run src/components/__tests__/count-card.test.tsx -x` passes (updated assertions)
    - `npx vitest run` -- full test suite passes
  </acceptance_criteria>
  <done>All 5 card components use .card class, HeroCard uses .card-hero, CountCard has no inline focus-visible, SessionRow is unchanged, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of card unification and editorial styles</name>
  <files>none</files>
  <action>
Human visually verifies the complete phase output: card unification (COMP-01), focus states (COMP-02), and editorial session content (CONTENT-02). No code changes in this task -- verification only.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <what-built>
    Unified card visual language across all card types (COMP-01), global keyboard focus states (COMP-02), and editorial session content styling (CONTENT-02):
    - All cards now have consistent 8px border-radius, visible border at rest, and combined border-accent + 2px lift + accent-tinted shadow on hover
    - HeroCard has a distinct 3px accent left bar
    - Keyboard Tab navigation shows accent-colored focus outlines on all interactive elements
    - Session content has accent-colored ordered list step markers, subtle hr section dividers, and inline code parameter pills with accent left border
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` if not already running
    2. **Card unification** (COMP-01):
       - Visit http://localhost:3000 -- check InstrumentCards have visible border at rest, accent border + lift on hover
       - Visit http://localhost:3000/instruments/evolver -- check HeroCard has accent left bar, CountCards lift on hover
       - Visit http://localhost:3000/instruments/evolver/patches -- check PatchCards have consistent 8px radius, accent hover
       - Visit http://localhost:3000/instruments/evolver/sessions -- check SessionRows have bg-surface hover only (no border, no lift)
       - Visit http://localhost:3000/instruments/cascadia/modules -- check ModuleCards use teal accent on hover (per data-instrument)
    3. **Focus states** (COMP-02):
       - On any page, press Tab key repeatedly -- all links, buttons, cards should show 2px accent outline with 2px offset
       - Click a card with mouse -- no focus outline should appear (focus-visible only)
       - Tab to the search bar (if visible) -- it should show its own border-accent focus, NOT a double outline
    4. **Editorial session content** (CONTENT-02):
       - Visit http://localhost:3000/instruments/evolver/sessions/01-oscillators-basic-waveforms -- check:
         a. Ordered list numerals are accent-colored and bold (not default muted gray)
         b. Horizontal rules between sections are thin subtle lines with generous vertical spacing
         c. Inline code like `C0`, `Saw`, `127` appear as accent-left-bordered pills with raised surface background
    5. Verify no visual regression on other pages (modules list, patch detail)
  </how-to-verify>
  <acceptance_criteria>
    - User confirms cards have consistent visual language (borders, radius, hover states)
    - User confirms HeroCard has distinct accent left bar treatment
    - User confirms keyboard Tab shows focus outlines, mouse click does not
    - User confirms session ordered lists have accent-colored bold numerals
    - User confirms inline code has pill treatment with accent left border
    - User confirms no visual regressions
  </acceptance_criteria>
  <done>User has visually approved card unification, focus states, and editorial session styling</done>
  <resume-signal>Type "approved" or describe any issues with card styling, focus rings, or editorial elements</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass including card-unification and updated count-card
- Visual: all 5 card types have consistent border, radius, hover behavior
- Visual: keyboard Tab shows accent focus rings, mouse click does not
- Visual: session content has editorial step markers, dividers, and code pills
</verification>

<success_criteria>
All card components share a unified visual language via .card CSS class. Focus states are globally consistent via :focus-visible. Session content has editorial styling. All tests pass. User has visually verified the result.
</success_criteria>

<output>
After completion, create `.planning/phases/21-cards-content-components/21-02-SUMMARY.md`
</output>
