---
phase: 29
slug: maths-curriculum-panel
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-18
---

# Phase 29 — UI Design Contract

> Visual and interaction contract for the Maths curriculum, overview page, and panel data file. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — custom Tailwind v4 design system) |
| Preset | not applicable |
| Component library | none (custom components) |
| Icon library | none needed for this phase |
| Font | Inter (body/labels), Space Grotesk (display headings), JetBrains Mono (code/parameters) |

**Source:** globals.css `@theme` block, established in Phase 18.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing, prose pre padding |
| lg | 24px | Card padding, section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks, prose hr margin |
| 3xl | 64px | Page-level spacing |

Exceptions: none — this phase uses only existing spacing tokens.

**Source:** globals.css `--spacing-*` tokens.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (1rem) | 400 (Inter) | 1.6 |
| Label / Muted | 13px (0.8125rem) | 400 (Inter) | 1.5 |
| Subheading (h3/h4) | 20px (1.25rem) | 700 (Inter) | 1.3 |
| Display (h1/h2) | clamp(1.5rem, ..., 2.4375rem) | 700 (Space Grotesk) | 1.15 |

Four sizes total: 13px, 16px, 20px, Display clamp (24px-39px).

**Source:** globals.css `.prose` rules, Phase 19.

### Phase-Specific Typography Notes

- **Controls reference table:** Use `.param-table` class for parameter/control listings — 13px body text, 13px headers, mono values, accent left border. The 13px Label/Muted size serves both table body and header text for compact data-dense tables.
- **Session frontmatter metadata:** 13px muted text (`.obsidian-tag` pattern).
- **Subheadings (h3 and h4):** Both use 20px at weight 700. Differentiation between h3 and h4 is structural (nesting depth), not size-based — consistent with the prose pipeline's semantic hierarchy.
- **Panel control labels:** Handled by SVG text elements within the panel data file — not governed by CSS typography tokens. Use 10-12px sans-serif within SVG viewBox.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(0.12 0.025 85)` | `--color-bg` — Maths page background via `[data-instrument="maths"]` cascade |
| Secondary (30%) | `oklch(0.16 0.025 85)` / `oklch(0.20 0.025 85)` | `--color-surface` / `--color-surface-raised` — cards, tables, code blocks |
| Accent (10%) | `oklch(0.72 0.14 85)` | `--color-accent` — Maths warm gold identity |
| Destructive | `oklch(0.55 0.2 25)` | `--color-destructive` — not used in this phase |

Accent reserved for: session step markers (ordered list numerals), callout borders, param-table left border, card hover border, focus outline, link hover color, inline code left border.

**Source:** globals.css `[data-instrument="maths"]` block, Phase 27.

### Panel SVG Colors (within SVG, not CSS tokens)

| Element | Color | Notes |
|---------|-------|-------|
| Panel background | `#1a1a1a` to `#222` | Dark panel matching Make Noise aesthetic |
| Knob body | `#333` stroke, `#444` fill | Standard knob rendering |
| Input jacks | `#1a1a1a` fill, `#555` stroke | Dark circles per convention |
| Output jacks | `#e8e8e8` fill, `#fff` stroke | White-filled per convention |
| Section labels | `#e8e8e8` | Silk-screen text color |
| Highlight glow | Per-instrument accent via CSS filter | Glow filter for session annotations |
| Cable: audio | `#ff6644` | Orange-red |
| Cable: cv | `#3388ff` | Blue |
| Cable: modulation | `#ffaa33` | Amber |

**Source:** synth-panel-builder SKILL.md, cascadia-panel.tsx patterns.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Start Session" (links to session 1 from overview) |
| Overview heading | "Make Noise Maths" |
| Overview tagline | "Analog computer for control voltage" (from module.json) |
| Architecture section heading | "Architecture" |
| Controls reference heading | "Controls Reference" |
| Init state heading | "Basic Patch (Init State)" |
| Init state body | "All sessions start from this known state. Set these positions before beginning any session." |
| Empty state (sessions list) | "No sessions yet. Curriculum is being developed." |
| Empty state (patches) | "No patches documented yet. Complete sessions to build your patch library." |
| Session warm-up label | "Warm-Up" (consistent with Evolver/Cascadia pattern) |
| Session output label | "Session Output" |
| Session format callout | "15-30 min" badge on each session card |
| Error state | "Session content could not be loaded. Check that your vault path is configured in evolver.config.json." |
| Destructive confirmation | None — no destructive actions in this phase |

### Session Title Pattern

Sessions follow the naming convention: `{NN}-{section}-{topic-kebab}.md`

Example titles (Claude's discretion per D-01 through D-04):
- "01 - Rise & Fall: Your First Envelope"
- "02 - Shaping VCA and VCF with Maths"
- "03 - Audio-Rate Maths: Listen to This!"
- "10 - Integration: Maths into Cascadia"

### Overview Page Sections (per D-05, D-06)

Primary focal point: the module name + tagline in the Hero section draws the eye first; the panel thumbnail (when available) serves as secondary visual anchor.

1. **Hero** — Module name, manufacturer, tagline, HP width badge, category badges
2. **Architecture** — 4-channel structure description, OR/SUM/INV bus, EOC/EOR outputs
3. **Controls Reference** — `.param-table` with all ~30 controls, grouped by channel (Ch1, Ch2/3, Ch4, Buses)
4. **Basic Patch (Init State)** — Specific knob positions table producing a known envelope on Ch1
5. **Sessions** — Ordered session list linking to individual sessions

---

## Component Inventory

This phase creates NO new React components. All content renders through existing components and routes.

| Existing Component | How This Phase Uses It |
|-------------------|----------------------|
| Module overview page (`/modules/[slug]/`) | Maths overview content fills the existing layout |
| Session list (`/modules/[slug]/sessions/`) | Maths sessions appear via existing session-list component |
| Session detail (`session-detail.tsx`) | Each Maths session renders through existing prose/markdown pipeline |
| Module header (`module-header.tsx`) | Displays Maths identity (name, manufacturer, categories) |
| Module sub-nav (`module-sub-nav.tsx`) | Overview / Sessions / Patches / Panel tabs |
| Param table (`.param-table` CSS class) | Controls reference table on overview page |
| Callout blocks (`.callout` CSS class) | Tips, challenges, warnings within sessions |

### Panel Data File (new file, not a component)

- **File:** `src/lib/maths-panel-data.ts`
- **Pattern:** Data-driven (Cascadia pattern) — ~30 controls with repeating knob/jack types across 4 channels
- **Approach:** `CONTROL_METADATA` record + `CONTROL_POSITIONS` map + `SECTION_BOUNDS`
- **Control ID convention:** `{type}-{channel}-{name}` e.g., `knob-ch1-rise`, `knob-ch1-fall`, `jack-ch1-trig`, `jack-or`, `jack-sum` (per D-08)
- **HP width:** 20HP — panel renders at same width as Swells (per PANEL-09 HP-aware sizing from Phase 28)
- **Note:** The generic `ModulePanel` renderer (Phase 28) must ship before this data file produces visible output. Data file is authored now so it is ready.

---

## Panel Data Contract (Maths-Specific)

### Channel Structure

| Channel | Controls | Notes |
|---------|----------|-------|
| Ch1 (left) | Rise knob, Fall knob, Rise CV attenuverter, Fall CV attenuverter, Cycle switch, Response curve switch, Trig input jack, Rise CV input jack, Fall CV input jack, Signal input jack, Unity output jack, Channel output jack, EOC output jack | Full-featured channel |
| Ch2/Ch3 (center) | Attenuverter knob each, Signal input jack each, Channel output jack each | Simple utility channels |
| Ch4 (right) | Rise knob, Fall knob, Rise CV attenuverter, Fall CV attenuverter, Cycle switch, Response curve switch, Trig input jack, Rise CV input jack, Fall CV input jack, Signal input jack, Unity output jack, Channel output jack, EOR output jack | Mirror of Ch1, with EOR instead of EOC |
| Buses | OR output jack, SUM output jack, INV output jack | Combined outputs |

### Approximate Control Count: ~30

Exact count must be verified against the downloaded Maths manual PDF before implementation (per STATE.md blocker note).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No component registry usage in this phase. All components are existing custom components.

---

## Interaction States

This phase is content-focused. No new interactive patterns are introduced. Existing interactions apply:

| Interaction | Existing Pattern | Notes |
|-------------|-----------------|-------|
| Session card hover | SpringCard lift + accent border | From Phase 22 |
| Session navigation | Prev/Next nav at session bottom | From Phase 15 |
| Tab switching | Module sub-nav (Overview/Sessions/Patches/Panel) | From Phase 27 |
| Panel control hover | Tooltip with control name | From Phase 12/13 |
| Panel session annotations | Glow filter on highlighted controls | From Phase 13 |
| Keyboard focus | 2px accent outline, 2px offset | Global `:focus-visible` rule |

---

## Accessibility Notes

- All session content renders as semantic HTML via the prose pipeline (headings, lists, tables)
- Controls reference table uses `<th>` for column headers
- Panel SVG includes `role="img"` and `aria-label` per existing panel convention
- Session step markers use ordered lists (`<ol>`) with accent-colored `::marker`
- Reduced motion: `prefers-reduced-motion` disables pulse-glow animation (existing rule)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
