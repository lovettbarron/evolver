---
phase: 29-maths-curriculum-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/maths-panel-data.ts
  - src/lib/__tests__/maths-panel-data.test.ts
  - references/maths-manual.pdf
autonomous: true
requirements: [PANEL-07]

must_haves:
  truths:
    - "Maths panel data file exports 45 control entries matching the physical module"
    - "Every jack has a signalType, every control has id/name/module/type"
    - "Control IDs follow channel-based convention (knob-ch1-rise, jack-ch4-eoc-out, etc.)"
    - "Panel data test suite passes with all 45 entries validated"
  artifacts:
    - path: "src/lib/maths-panel-data.ts"
      provides: "Complete Maths panel control metadata, positions (placeholder), section bounds"
      exports: ["CONTROL_METADATA", "CONTROL_POSITIONS", "SECTION_BOUNDS", "MathsControlMeta", "midiToRotation"]
    - path: "src/lib/__tests__/maths-panel-data.test.ts"
      provides: "Validation tests for panel data completeness and correctness"
    - path: "references/maths-manual.pdf"
      provides: "Official Make Noise Maths manual for panel reference"
  key_links:
    - from: "src/lib/__tests__/maths-panel-data.test.ts"
      to: "src/lib/maths-panel-data.ts"
      via: "import CONTROL_METADATA, SECTION_BOUNDS, midiToRotation"
      pattern: "import.*from.*maths-panel-data"
---

<objective>
Create the Maths panel data file with all 45 control entries and a test suite, plus download the official manual PDF.

Purpose: The panel data file is the foundation for Phase 28's generic ModulePanel renderer to display the Maths front plate. Getting the complete control inventory correct now (per the verified research) means Phase 28 can wire it up without re-doing the data work. The manual PDF is needed for accurate panel position hand-placement (a later task) and session page references.

Output: `src/lib/maths-panel-data.ts` with 45 entries, passing test suite, and `references/maths-manual.pdf`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-maths-curriculum-panel/29-CONTEXT.md
@.planning/phases/29-maths-curriculum-panel/29-RESEARCH.md

@src/lib/cascadia-panel-data.ts
@src/lib/__tests__/cascadia-panel-data.test.ts
@.claude/skills/synth-panel-builder/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Maths panel data file and test suite</name>
  <files>src/lib/maths-panel-data.ts, src/lib/__tests__/maths-panel-data.test.ts</files>
  <read_first>
    - src/lib/cascadia-panel-data.ts (reference pattern for CONTROL_METADATA, SECTION_BOUNDS, midiToRotation)
    - src/lib/__tests__/cascadia-panel-data.test.ts (reference pattern for test structure)
    - .planning/phases/29-maths-curriculum-panel/29-RESEARCH.md (verified control inventory — 45 elements across 5 channel groups)
  </read_first>
  <behavior>
    - Test 1: CONTROL_METADATA has exactly 45 entries
    - Test 2: Every entry has non-empty id, name, module, and type fields
    - Test 3: Every entry type is one of: knob, button, jack-in, jack-out, led
    - Test 4: Every jack (jack-in or jack-out) has a valid signalType (audio, cv, gate, modulation)
    - Test 5: Every entry module is one of: ch1, ch2, ch3, ch4, buses
    - Test 6: SECTION_BOUNDS has entries for ch1, ch2, ch3, ch4, buses — all with positive width and height
    - Test 7: midiToRotation(0) returns -135, midiToRotation(127) returns 135, midiToRotation(63) returns approximately 0
    - Test 8: CONTROL_POSITIONS has an entry for every key in CONTROL_METADATA (45 entries)
    - Test 9: Per-module counts: ch1 has 17 entries, ch2 has 3, ch3 has 3, ch4 has 17, buses has 5
  </behavior>
  <action>
Create the test file first (`src/lib/__tests__/maths-panel-data.test.ts`) following the Cascadia test pattern. Then create the data file (`src/lib/maths-panel-data.ts`).

**Interface** (per D-08 channel-based IDs):
```typescript
export interface MathsControlMeta {
  id: string;
  name: string;
  module: string;  // 'ch1' | 'ch2' | 'ch3' | 'ch4' | 'buses'
  type: 'knob' | 'button' | 'jack-in' | 'jack-out' | 'led';
  signalType?: 'audio' | 'cv' | 'gate' | 'modulation';
}
```

**CONTROL_METADATA** — 45 entries exactly as listed in RESEARCH.md "Verified Maths Control Inventory":

Channel 1 (17): knob-ch1-rise, knob-ch1-fall, knob-ch1-vari-response, knob-ch1-attenuverter, button-ch1-cycle, led-ch1-cycle, led-ch1-unity, led-ch1-eor, jack-ch1-signal-in (cv), jack-ch1-trig-in (gate), jack-ch1-rise-cv-in (cv), jack-ch1-fall-cv-in (cv), jack-ch1-both-cv-in (cv), jack-ch1-cycle-in (gate), jack-ch1-unity-out (cv), jack-ch1-eor-out (gate), jack-ch1-var-out (cv)

Channel 2 (3): knob-ch2-attenuverter, jack-ch2-signal-in (cv), jack-ch2-var-out (cv)

Channel 3 (3): knob-ch3-attenuverter, jack-ch3-signal-in (cv), jack-ch3-var-out (cv)

Channel 4 (17): knob-ch4-rise, knob-ch4-fall, knob-ch4-vari-response, knob-ch4-attenuverter, button-ch4-cycle, led-ch4-cycle, led-ch4-unity, led-ch4-eoc, jack-ch4-signal-in (cv), jack-ch4-trig-in (gate), jack-ch4-rise-cv-in (cv), jack-ch4-fall-cv-in (cv), jack-ch4-both-cv-in (cv), jack-ch4-cycle-in (gate), jack-ch4-unity-out (cv), jack-ch4-eoc-out (gate), jack-ch4-var-out (cv)

Buses (5): jack-or-out (cv), jack-sum-out (cv), jack-inv-out (cv), led-sum-pos, led-sum-neg

**CONTROL_POSITIONS** — All 45 entries with `{ x: 0, y: 0 }` placeholder values. Add a comment: `// Placeholder — hand-place from manual reference image when Phase 28 panel renderer is ready`

**SECTION_BOUNDS** — 5 entries (ch1, ch2, ch3, ch4, buses) with `{ x: 0, y: 0, width: 100, height: 100 }` placeholder values. Use width/height of 100 (not 0) so the "positive dimensions" test passes.

**midiToRotation** — `return -135 + (value / 127) * 270;` (same formula as Cascadia)

Note on `type: 'button'`: Maths Cycle controls are push buttons, NOT toggle switches (see RESEARCH.md Pitfall 6). The Cascadia pattern uses 'switch' but Maths needs 'button'. Add 'button' to the valid types in the test.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/lib/__tests__/maths-panel-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/maths-panel-data.ts contains `export interface MathsControlMeta`
    - src/lib/maths-panel-data.ts contains `export const CONTROL_METADATA: Record<string, MathsControlMeta>`
    - src/lib/maths-panel-data.ts contains `export const CONTROL_POSITIONS`
    - src/lib/maths-panel-data.ts contains `export const SECTION_BOUNDS`
    - src/lib/maths-panel-data.ts contains `export function midiToRotation`
    - src/lib/maths-panel-data.ts contains exactly 45 entries in CONTROL_METADATA (grep for `': {` count)
    - src/lib/maths-panel-data.ts contains `type: 'button'` for cycle controls (not 'switch')
    - src/lib/__tests__/maths-panel-data.test.ts exits 0
    - Test file checks for 45 total entries, per-module counts (17, 3, 3, 17, 5), valid types including 'button', signalType on jacks
  </acceptance_criteria>
  <done>Panel data file exports 45 controls across 5 channel groups with all metadata fields, test suite passes with 9+ test cases</done>
</task>

<task type="auto">
  <name>Task 2: Download Maths manual PDF</name>
  <files>references/maths-manual.pdf, references/maths-manual.txt</files>
  <read_first>
    - references/maths-manual.txt (current placeholder — 4 lines)
    - .planning/phases/29-maths-curriculum-panel/29-RESEARCH.md (download source info — UCSB mirror for 2013 version, Make Noise website for 2020)
  </read_first>
  <action>
Download the Maths manual PDF per D-09. Try these sources in order:

1. **UCSB mirror** (2013 version, direct PDF): `curl -L -o references/maths-manual.pdf "http://w2.mat.ucsb.edu/200C/Maths_2013.pdf"` — this is a direct PDF link that should work without browser interaction
2. **Make Noise website**: `curl -L -o references/maths-manual.pdf "https://www.makenoisemusic.com/content/manuals/MATHSmanual2020.pdf"` — try common URL pattern for Make Noise manuals
3. If neither direct download works, try fetching the Make Noise Maths page to find the actual PDF URL: `curl -s https://www.makenoisemusic.com/modules/maths | grep -i "pdf\|manual\|download"`

After successful download, verify it is a real PDF (not an HTML error page):
- `file references/maths-manual.pdf` should output "PDF document"
- `wc -c references/maths-manual.pdf` should be > 100KB (manuals are typically 1-5MB)

If the download succeeds, update `references/maths-manual.txt` to note the PDF is now available:
```
Maths Manual
Source: [actual URL used]
Downloaded: [date]
See: references/maths-manual.pdf
```

If NO direct download works (all return HTML), leave the placeholder and create a checkpoint note in the summary that the user needs to manually download from https://www.makenoisemusic.com/modules/maths — but do NOT block the rest of the phase on this.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && file references/maths-manual.pdf 2>/dev/null | grep -q "PDF" && echo "PASS: PDF exists" || echo "WARN: PDF not downloaded — manual browser download needed"</automated>
  </verify>
  <acceptance_criteria>
    - references/maths-manual.pdf exists AND `file` reports it as PDF document, OR references/maths-manual.txt documents why manual download is needed
    - references/maths-manual.txt is updated with download status (either success with source URL, or instructions for manual download)
  </acceptance_criteria>
  <done>Maths manual PDF is available in references/ for panel position hand-placement and session page references (or clear instructions for manual download if automated download fails)</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/maths-panel-data.test.ts` passes (all 9+ tests green)
- `file references/maths-manual.pdf` reports PDF (or fallback documented)
- Panel data file has exactly 45 CONTROL_METADATA entries
- All control IDs follow D-08 convention: `{type}-{channel}-{name-kebab}`
</verification>

<success_criteria>
- Maths panel data file is complete with 45 controls, correct types (including 'button' for Cycle), and channel-based IDs
- Test suite validates control count, per-module counts, required fields, and signalType on jacks
- Maths manual PDF downloaded or download instructions documented
</success_criteria>

<output>
After completion, create `.planning/phases/29-maths-curriculum-panel/29-01-SUMMARY.md`
</output>
