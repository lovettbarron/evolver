---
phase: 06-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pdf-viewer.tsx
  - src/components/source-ref.tsx
  - src/components/session-detail.tsx
  - src/components/instrument-overview.tsx
  - src/app/instruments/[slug]/page.tsx
  - package.json
autonomous: true
requirements: [DEPL-04]
must_haves:
  truths:
    - "Session reference citations link to an embedded PDF viewer opening at the cited page"
    - "Instrument overview page shows links to reference PDFs"
    - "PDFs render via react-pdf in an embedded viewer (not browser iframe)"
    - "PDF viewer supports page navigation and deep page linking via URL hash"
  artifacts:
    - path: "src/components/pdf-viewer.tsx"
      provides: "react-pdf based PDF viewer with page nav and hash-based page linking"
      min_lines: 60
    - path: "src/components/source-ref.tsx"
      provides: "Updated source reference component with PDF link"
  key_links:
    - from: "src/components/source-ref.tsx"
      to: "src/components/pdf-viewer.tsx"
      via: "onClick opens PDF viewer overlay at cited page"
      pattern: "PdfViewer|pdf-viewer"
    - from: "src/components/instrument-overview.tsx"
      to: "PDF files in src/content/references/"
      via: "links to open PDF viewer"
      pattern: "pdf-viewer|PdfViewer"
---

<objective>
Create an embedded PDF viewer using react-pdf and wire it into session reference citations and the instrument overview page, so reference PDFs are accessible with deep page linking.

Purpose: Reference PDFs (DSI Manual, Anu Kirk guide) are core learning resources. Making them accessible directly from session citations ("See Anu Kirk p.42" opens viewer at page 42) keeps learners in flow.
Output: PdfViewer component, updated SourceRef with PDF links, instrument overview references section.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment/06-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/source-ref.tsx:
```typescript
interface SourceRefProps {
  reference: string;
  detail?: string;
}
export function SourceRef({ reference, detail }: SourceRefProps)
```

From src/components/session-detail.tsx:
```typescript
interface SessionDetailProps {
  session: Session;
  html: string;
  prev: { slug: string; title: string } | null;
  next: { slug: string; title: string } | null;
  instrumentSlug: string;
  quickRefContent: { label: string; html: string }[];
  reference: string | null;
}
```

From src/components/instrument-overview.tsx:
```typescript
// props type needs to be inspected at read_first time
```

Bundled PDF files at:
- src/content/references/Evo_Key_Manual_1.3.pdf
- src/content/references/evolverguide.pdf

Design system colors (globals.css):
- --color-bg: #0a0a0a
- --color-surface: #161616
- --color-text: #e8e8e8
- --color-muted: #737373
- --color-accent: #c8ff00
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-pdf and create PdfViewer component</name>
  <files>package.json, src/components/pdf-viewer.tsx, next.config.ts</files>
  <read_first>
    - /Users/andrewlovettbarron/src/evolver/package.json
    - /Users/andrewlovettbarron/src/evolver/next.config.ts
    - /Users/andrewlovettbarron/src/evolver/src/app/globals.css
  </read_first>
  <action>
1. Install react-pdf: `npm install react-pdf`

2. Create `src/components/pdf-viewer.tsx` as a 'use client' component:
   - Props: `{ src: string; initialPage?: number; onClose: () => void }`
   - Uses `Document` and `Page` from `react-pdf` with `pdfjs.GlobalWorkerOptions.workerSrc` set to the pdf.js worker CDN URL (from react-pdf docs: `new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url)` or cdnjs fallback)
   - Full-screen overlay with fixed positioning: `fixed inset-0 z-50 bg-bg/95 flex flex-col`
   - Top bar: page indicator "Page X of Y", previous/next page buttons using Lucide `ChevronLeft`/`ChevronRight` icons, close button using Lucide `X` icon
   - Buttons use `text-muted hover:text-text` styling
   - Page display area: centered, scrollable overflow, `max-w-[800px] mx-auto`
   - State: `pageNumber` (default from `initialPage` prop or 1), `numPages` (from `onDocumentLoadSuccess`)
   - `onDocumentLoadSuccess` callback sets numPages from `{ numPages }` param
   - Page navigation clamps between 1 and numPages
   - Keyboard support: Escape calls onClose, ArrowLeft/ArrowRight for page nav (useEffect with keydown listener)

3. Update `next.config.ts` to add webpack config for react-pdf's canvas dependency:
   ```typescript
   const nextConfig: NextConfig = {
     webpack: (config) => {
       config.resolve.alias.canvas = false;
       return config;
     },
   };
   ```
   Keep any existing config properties (currently empty object body).

4. Run `npm run build` to verify no build errors from react-pdf integration.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && grep -q "react-pdf" package.json && test -f src/components/pdf-viewer.tsx && grep -q "canvas" next.config.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react-pdf" in dependencies
    - src/components/pdf-viewer.tsx exists and contains `'use client'`
    - src/components/pdf-viewer.tsx contains `from 'react-pdf'`
    - src/components/pdf-viewer.tsx contains `onClose` in props interface
    - src/components/pdf-viewer.tsx contains `initialPage` in props interface
    - src/components/pdf-viewer.tsx contains `pageNumber` state
    - src/components/pdf-viewer.tsx contains `numPages` state
    - src/components/pdf-viewer.tsx contains `Escape` keyboard handler
    - next.config.ts contains `canvas = false` alias for webpack
  </acceptance_criteria>
  <done>PdfViewer component renders PDFs with page navigation, deep page linking via initialPage prop, and keyboard controls. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PDF links into SourceRef and instrument overview</name>
  <files>src/components/source-ref.tsx, src/components/instrument-overview.tsx, src/app/instruments/[slug]/page.tsx</files>
  <read_first>
    - /Users/andrewlovettbarron/src/evolver/src/components/source-ref.tsx
    - /Users/andrewlovettbarron/src/evolver/src/components/instrument-overview.tsx
    - /Users/andrewlovettbarron/src/evolver/src/components/pdf-viewer.tsx
    - /Users/andrewlovettbarron/src/evolver/src/app/instruments/[slug]/page.tsx
    - /Users/andrewlovettbarron/src/evolver/src/components/session-detail.tsx
  </read_first>
  <action>
1. Update `src/components/source-ref.tsx`:
   - Add state: `const [showPdf, setShowPdf] = useState(false)`
   - Parse the `reference` string to extract PDF source and page number. Pattern: references like "Anu Kirk p.42" or "DSI Manual p.14". Use a mapping object:
     ```typescript
     const PDF_MAP: Record<string, string> = {
       'anu kirk': '/content/references/evolverguide.pdf',
       'dsi manual': '/content/references/Evo_Key_Manual_1.3.pdf',
       'evolver guide': '/content/references/evolverguide.pdf',
     };
     ```
   - Extract page number with regex: `/p\.?\s*(\d+)/i` from the reference string
   - Match PDF by checking if `reference.toLowerCase()` includes any PDF_MAP key
   - If a PDF match is found, the button onClick sets `setShowPdf(true)` instead of just toggling the tooltip
   - Keep the existing tooltip behavior as fallback when no PDF match found
   - When `showPdf` is true, render `<PdfViewer src={pdfPath} initialPage={pageNum} onClose={() => setShowPdf(false)} />`
   - Import PdfViewer: `import { PdfViewer } from './pdf-viewer'`

2. Add a references section to `src/components/instrument-overview.tsx`:
   - Add a new prop: `references?: Array<{ label: string; pdfPath: string }>`
   - Below the existing content, render a "References" section with `<h2>` heading
   - Each reference is a button that opens the PdfViewer overlay
   - Use state to track which PDF is open: `const [openPdf, setOpenPdf] = useState<string | null>(null)`
   - Reference items styled as clickable cards: `bg-surface p-md rounded hover:border-accent border border-surface transition-colors cursor-pointer`
   - Add Lucide `FileText` icon next to each label

3. Update `src/app/instruments/[slug]/page.tsx`:
   - Pass references prop to InstrumentOverview:
     ```typescript
     const references = [
       { label: 'DSI Evolver Manual (v1.3)', pdfPath: '/content/references/Evo_Key_Manual_1.3.pdf' },
       { label: 'The Definitive Guide to Evolver — Anu Kirk', pdfPath: '/content/references/evolverguide.pdf' },
     ];
     ```
   - Add `references={references}` to the InstrumentOverview JSX

4. Ensure PDFs are served statically: create a `public/content/references/` directory and copy the 3 PDF files from `src/content/references/` OR add a Next.js API route at `src/app/api/references/[...path]/route.ts` that reads from `src/content/references/` using fs. The API route approach is better since content can come from vault or bundled source:
   - Create `src/app/api/references/[...path]/route.ts`
   - GET handler: read `path` param, resolve to content references dir using `getContentRoot(config)`, stream the file with appropriate `Content-Type: application/pdf` header
   - Update PDF_MAP paths in source-ref.tsx and references array in page.tsx to use `/api/references/evolverguide.pdf` pattern

5. Run `npm run build` to verify.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && grep -q "PdfViewer" src/components/source-ref.tsx && grep -q "references" src/components/instrument-overview.tsx && test -f src/app/api/references/\\[...path\\]/route.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/source-ref.tsx contains `import` of PdfViewer
    - src/components/source-ref.tsx contains `PDF_MAP` object with 'anu kirk' and 'dsi manual' keys
    - src/components/source-ref.tsx contains `showPdf` state
    - src/components/instrument-overview.tsx contains `references` prop
    - src/components/instrument-overview.tsx contains `FileText` icon import from lucide-react
    - src/app/instruments/[slug]/page.tsx contains `references={` prop pass
    - src/app/api/references/[...path]/route.ts exists and contains `Content-Type` header set to `application/pdf`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Session citations with PDF references (e.g., "Anu Kirk p.42") open the PDF viewer at the cited page. Instrument overview shows reference PDFs as clickable cards. PDFs served via API route from content directory.</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- PdfViewer component exists with page navigation and keyboard controls
- SourceRef opens PDF viewer when reference matches a known PDF
- Instrument overview page shows reference PDF links
- API route serves PDF files from content directory
</verification>

<success_criteria>
- Reference PDFs are accessible from session citations with deep page linking
- Instrument overview page has a references section with clickable PDF cards
- PDF viewer renders with page navigation, close button, and keyboard shortcuts
- Both local vault and bundled demo content serve PDFs correctly via the API route
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment/06-01-SUMMARY.md`
</output>
