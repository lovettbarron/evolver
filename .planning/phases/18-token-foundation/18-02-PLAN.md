---
phase: 18-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/about/page.tsx
  - src/app/instruments/[slug]/progress/page.tsx
  - src/app/instruments/[slug]/midi/page.tsx
  - src/app/instruments/[slug]/patches/page.tsx
  - src/app/instruments/[slug]/troubleshooting/page.tsx
  - src/app/instruments/[slug]/sessions/page.tsx
  - src/app/instruments/[slug]/modules/page.tsx
  - src/app/page.tsx
  - src/components/midi-page.tsx
  - src/components/module-detail.tsx
  - src/components/session-list.tsx
  - src/components/instrument-overview.tsx
  - src/components/patch-card.tsx
  - src/components/diff-view.tsx
  - src/components/patch-grid.tsx
  - src/components/module-index.tsx
  - src/components/session-detail.tsx
  - src/components/search-bar.tsx
  - src/components/sticky-header.tsx
  - src/components/patch-detail.tsx
  - src/components/module-card.tsx
  - src/components/instrument-card.tsx
  - src/components/no-sysex-page.tsx
  - src/components/completion-toggle.tsx
  - src/components/patch-filter-bar.tsx
  - src/components/hero-card.tsx
  - src/components/empty-progress.tsx
  - src/components/session-list-client.tsx
  - src/app/__tests__/spacing-migration.test.ts
autonomous: true
requirements: [TOKEN-03]
must_haves:
  truths:
    - "No component file contains hardcoded numeric spacing classes (p-1, p-2, p-4, gap-6, etc.)"
    - "All spacing uses token references (p-xs, p-sm, p-md, p-lg, p-xl, p-2xl, p-3xl)"
    - "Visual layout is unchanged — token values match the pixel values they replace"
  artifacts:
    - path: "src/app/__tests__/spacing-migration.test.ts"
      provides: "Grep-based test ensuring no hardcoded numeric spacing in components"
    - path: "src/components/hero-card.tsx"
      provides: "Example of migrated component using token spacing"
      contains: "p-md"
  key_links:
    - from: "all 29 component files"
      to: "src/app/globals.css @theme"
      via: "Tailwind spacing token classes"
      pattern: "p-md|gap-lg|p-sm"
---

<objective>
Migrate all 29 component/page files from hardcoded Tailwind numeric spacing classes (p-4, gap-6, m-2, etc.) to the 7-token spacing scale (p-md, gap-lg, m-sm, etc.) defined in @theme. Visual layout is pixel-identical since token values match the numeric values they replace.

Purpose: Consistent spacing vocabulary across the entire codebase — every component speaks the same spacing language, making future rhythm changes a single-token edit.
Output: 29 migrated files, passing spacing migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-token-foundation/18-CONTEXT.md
@.planning/phases/18-token-foundation/18-UI-SPEC.md
@.planning/phases/18-token-foundation/18-RESEARCH.md
@src/app/globals.css

<interfaces>
<!-- Spacing tokens available after Plan 01 in @theme -->
--spacing-xs: 4px;   /* replaces p-1, gap-1, m-1 */
--spacing-sm: 8px;   /* replaces p-2, gap-2, m-2 */
--spacing-md: 16px;  /* replaces p-4, gap-4, m-4 */
--spacing-lg: 24px;  /* replaces p-6, gap-6, m-6 */
--spacing-xl: 32px;  /* replaces p-8, gap-8, m-8 */
--spacing-2xl: 48px; /* replaces p-12, gap-12 (if any) */
--spacing-3xl: 64px; /* replaces p-16 (if any) */
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spacing migration test (Wave 0) + full component migration</name>
  <files>
    src/app/__tests__/spacing-migration.test.ts
    src/app/about/page.tsx
    src/app/instruments/[slug]/progress/page.tsx
    src/app/instruments/[slug]/midi/page.tsx
    src/app/instruments/[slug]/patches/page.tsx
    src/app/instruments/[slug]/troubleshooting/page.tsx
    src/app/instruments/[slug]/sessions/page.tsx
    src/app/instruments/[slug]/modules/page.tsx
    src/app/page.tsx
    src/components/midi-page.tsx
    src/components/module-detail.tsx
    src/components/session-list.tsx
    src/components/instrument-overview.tsx
    src/components/patch-card.tsx
    src/components/diff-view.tsx
    src/components/patch-grid.tsx
    src/components/module-index.tsx
    src/components/session-detail.tsx
    src/components/search-bar.tsx
    src/components/sticky-header.tsx
    src/components/patch-detail.tsx
    src/components/module-card.tsx
    src/components/instrument-card.tsx
    src/components/no-sysex-page.tsx
    src/components/completion-toggle.tsx
    src/components/patch-filter-bar.tsx
    src/components/hero-card.tsx
    src/components/empty-progress.tsx
    src/components/session-list-client.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/components/hero-card.tsx
    src/components/patch-card.tsx
    src/components/module-card.tsx
    src/components/session-detail.tsx
    src/components/sticky-header.tsx
    src/app/page.tsx
    .planning/phases/18-token-foundation/18-RESEARCH.md
    .planning/phases/18-token-foundation/18-UI-SPEC.md
  </read_first>
  <behavior>
    spacing-migration.test.ts:
    - Test: no .tsx file in src/components/ or src/app/ contains Tailwind numeric spacing like p-1, p-2, p-4, p-6, p-8, gap-1, gap-2, gap-4, gap-6, gap-8, m-1, m-2, m-4, m-6, m-8, px-1, px-2, px-4, px-6, px-8, py-1, py-2, py-4, py-6, py-8, mx-1, mx-2, mx-4, mx-6, mx-8, my-1, my-2, my-4, my-6, my-8, mt-1, mt-2, mt-4, mt-6, mt-8, mb-1, mb-2, mb-4, mb-6, mb-8, ml-1, ml-2, ml-4, ml-6, ml-8, mr-1, mr-2, mr-4, mr-6, mr-8, pt-1, pt-2, pt-4, pt-6, pt-8, pb-1, pb-2, pb-4, pb-6, pb-8, pl-1, pl-2, pl-4, pl-6, pl-8, pr-1, pr-2, pr-4, pr-6, pr-8 (word-boundary matched)
    - Test: EXCLUDES panel files (evolver-panel.tsx, cascadia-panel.tsx) from the check — they are intentionally isolated
    - Test: EXCLUDES files under __tests__/ and node_modules/
    - Test: EXCLUDES .claude/worktrees/ directory
    - Test: src/lib/cascadia-cable-lookup.ts is excluded (data file, not a component)
  </behavior>
  <action>
    **Step 1: Write the spacing migration test (RED)**

    Create `src/app/__tests__/spacing-migration.test.ts`:
    - Use Node.js `fs` and `path` to glob all .tsx files in src/components/ and src/app/ (recursively)
    - Exclude: evolver-panel.tsx, cascadia-panel.tsx, __tests__/, node_modules/, .claude/worktrees/
    - For each file, read contents and check for regex pattern matching hardcoded numeric spacing classes:
      Pattern: `\b(p|px|py|pt|pb|pl|pr|m|mx|my|mt|mb|ml|mr|gap)-[0-9]+\b`
      This catches p-1, p-2, p-4, p-6, p-8, gap-2, gap-4, gap-6, mt-4, mb-8, etc.
    - Report which files and which classes fail
    - Test MUST pass when no hardcoded numeric spacing remains

    Run test — it should FAIL (RED) because components still have hardcoded values.

    **Step 2: Migrate all 29 files (GREEN)**

    Apply this mapping systematically to every file listed above:

    | Find (word-boundary) | Replace with |
    |----------------------|--------------|
    | `p-1` | `p-xs` |
    | `p-2` | `p-sm` |
    | `p-4` | `p-md` |
    | `p-6` | `p-lg` |
    | `p-8` | `p-xl` |
    | `px-1` | `px-xs` |
    | `px-2` | `px-sm` |
    | `px-4` | `px-md` |
    | `px-6` | `px-lg` |
    | `px-8` | `px-xl` |
    | `py-1` | `py-xs` |
    | `py-2` | `py-sm` |
    | `py-4` | `py-md` |
    | `py-6` | `py-lg` |
    | `py-8` | `py-xl` |
    | `gap-1` | `gap-xs` |
    | `gap-2` | `gap-sm` |
    | `gap-4` | `gap-md` |
    | `gap-6` | `gap-lg` |
    | `gap-8` | `gap-xl` |
    | Same pattern for m-, mx-, my-, mt-, mb-, ml-, mr-, pt-, pb-, pl-, pr- variants |

    **CRITICAL: Word-boundary matching only.** Do NOT replace `p-10` with `p-1xs0` or break compound class names. Use regex `\bp-4\b` not string replace.

    **CRITICAL: Do NOT touch panel files** (evolver-panel.tsx, cascadia-panel.tsx) — they use hardcoded styles intentionally.

    **CRITICAL: Do NOT touch src/lib/cascadia-cable-lookup.ts** — it's a data file, not a component.

    **Edge cases:**
    - If any file uses p-3 (12px), p-5 (20px), p-1.5 (6px), or similar non-standard values: keep them as-is (no token equivalent). The test should not flag these.
    - If a class like `p-0` exists: keep as-is (0 has no token).
    - Values like `space-y-4` should be mapped to `space-y-md`, same pattern.

    Run test — should now PASS (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/app/__tests__/spacing-migration.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/app/__tests__/spacing-migration.test.ts exists and contains regex pattern for numeric spacing detection
    - `grep -rn '\bp-4\b' src/components/ --include='*.tsx' | grep -v panel` returns no matches
    - `grep -rn '\bgap-6\b' src/components/ --include='*.tsx' | grep -v panel` returns no matches
    - `grep -rn '\bp-2\b' src/components/ --include='*.tsx' | grep -v panel` returns no matches
    - `grep -rn '\bp-md\b' src/components/ --include='*.tsx'` returns multiple matches (tokens in use)
    - `grep -rn '\bgap-lg\b' src/components/ --include='*.tsx'` returns multiple matches
    - `npx vitest run src/app/__tests__/spacing-migration.test.ts` exits 0
    - `npx next build` succeeds (no broken class references)
  </acceptance_criteria>
  <done>All 29 component/page files use token-based spacing. No hardcoded numeric Tailwind spacing remains outside panels. Spacing migration test passes. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/app/__tests__/spacing-migration.test.ts --reporter=verbose` — zero hardcoded numeric spacing in components
2. `grep -rn '\bp-[0-9]\b\|gap-[0-9]\b' src/components/ --include='*.tsx' | grep -v panel | wc -l` — returns 0
3. `npx next build` — build succeeds
</verification>

<success_criteria>
- Zero hardcoded numeric spacing classes in component files (excluding panels)
- All spacing references use token names (xs, sm, md, lg, xl, 2xl, 3xl)
- Spacing migration test passes
- Build succeeds with no errors
- Visual layout is pixel-identical (token values match replaced numeric values)
</success_criteria>

<output>
After completion, create `.planning/phases/18-token-foundation/18-02-SUMMARY.md`
</output>
