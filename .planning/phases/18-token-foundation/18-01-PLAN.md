---
phase: 18-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - scripts/validate-contrast.mjs
  - scripts/__tests__/validate-contrast.test.ts
  - src/app/__tests__/tokens.test.ts
  - public/textures/grain.webp
  - package.json
autonomous: true
requirements: [TOKEN-01, TOKEN-04, TOKEN-05]
must_haves:
  truths:
    - "All surface tokens (bg, sunken, surface, surface-raised, overlay) exist in @theme with OKLCH values at hue ~85"
    - "All foreground/background pairings pass WCAG AA contrast ratio (4.5:1 minimum)"
    - "Existing color token names (bg, surface, text, muted, accent, param) are preserved so components cascade automatically"
    - "Grain texture overlay is visible at low opacity on page background"
    - "pulse-glow keyframe uses OKLCH accent value, not hardcoded RGBA"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete OKLCH token system with 5 surface elevations, semantic color tokens, border tokens, shadows, radii, z-index"
      contains: "oklch(0.12 0.01 85)"
    - path: "scripts/validate-contrast.mjs"
      provides: "Automated WCAG AA contrast validation for all token pairings"
      exports: ["default script execution"]
    - path: "scripts/__tests__/validate-contrast.test.ts"
      provides: "Test coverage for contrast validation script"
    - path: "src/app/__tests__/tokens.test.ts"
      provides: "Test coverage for token existence, OKLCH format, and hue angle"
    - path: "public/textures/grain.webp"
      provides: "64x64 grain texture tile for subtle overlay"
  key_links:
    - from: "src/app/globals.css"
      to: "all 51 components"
      via: "Tailwind @theme cascade"
      pattern: "bg-bg|bg-surface|text-text|text-muted"
    - from: "scripts/validate-contrast.mjs"
      to: "src/app/globals.css"
      via: "OKLCH value parsing"
      pattern: "oklch\\("
---

<objective>
Replace the existing 6-color hex token system in globals.css @theme with a full OKLCH-based semantic token architecture: 5 warm olive surface elevations, semantic color/border/shadow/radius/z-index tokens, grain texture overlay, and automated WCAG AA contrast validation. This is the visual foundation all subsequent v1.3 phases inherit.

Purpose: Every page shifts from cold neutral grays to warm olive-toned dark surfaces with verified accessible contrast — the identity change that defines v1.3.
Output: Rewritten globals.css @theme, contrast validation script, passing tests, grain texture asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-token-foundation/18-CONTEXT.md
@.planning/phases/18-token-foundation/18-RESEARCH.md
@.planning/phases/18-token-foundation/18-UI-SPEC.md
@.planning/phases/18-token-foundation/18-VALIDATION.md
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests + contrast validation script</name>
  <files>
    src/app/__tests__/tokens.test.ts
    scripts/__tests__/validate-contrast.test.ts
    scripts/validate-contrast.mjs
    package.json
  </files>
  <read_first>
    src/app/globals.css
    src/app/__tests__/home.test.ts
    vitest.config.ts
    package.json
    .planning/phases/18-token-foundation/18-RESEARCH.md
    .planning/phases/18-token-foundation/18-UI-SPEC.md
  </read_first>
  <behavior>
    tokens.test.ts:
    - Test: globals.css @theme block contains --color-bg with oklch value
    - Test: globals.css @theme block contains --color-sunken with oklch value
    - Test: globals.css @theme block contains --color-surface with oklch value
    - Test: globals.css @theme block contains --color-surface-raised with oklch value
    - Test: globals.css @theme block contains --color-overlay with oklch value
    - Test: all 5 surface tokens use hue angle ~85 (within range 80-90)
    - Test: --color-text, --color-muted, --color-accent, --color-param exist with oklch values
    - Test: --color-border and --color-border-subtle exist with oklch values

    validate-contrast.test.ts:
    - Test: contrast script reports all defined pairings as PASS with ratio >= 4.5
    - Test: contrast script correctly identifies a known-failing pairing (e.g., low-contrast text on similar background) as FAIL
    - Test: all 8 critical pairings from UI-SPEC are checked (text/bg, text/surface, text/surface-raised, muted/bg, muted/surface, accent/bg, accent/surface, param/surface)
  </behavior>
  <action>
    1. Install culori as dev dependency: `npm install -D culori`

    2. Create `scripts/validate-contrast.mjs`:
       - Import from culori: parse, wcagContrast (or implement using culori's rgb conversion + standard WCAG formula)
       - Define token map with exact OKLCH values from UI-SPEC:
         - bg: oklch(0.12 0.01 85)
         - sunken: oklch(0.10 0.01 85)
         - surface: oklch(0.16 0.01 85)
         - surface-raised: oklch(0.20 0.015 85)
         - overlay: oklch(0.24 0.015 85)
         - text: oklch(0.93 0.01 85)
         - muted: oklch(0.55 0.01 85)
         - accent: oklch(0.85 0.18 105)
         - param: oklch(0.72 0.15 120)
       - Define 8 pairings from UI-SPEC contrast table (see above)
       - For each pairing: parse both colors, compute WCAG contrast ratio, print "{fg} on {bg}: {ratio}:1 PASS/FAIL"
       - Also check gamut: use culori's displayable() for accent and param colors; warn if out of sRGB gamut
       - Exit code 0 if all pass, 1 if any fail
       - Export the token map and pairings for test consumption

    3. Create `scripts/__tests__/validate-contrast.test.ts`:
       - Import token map and pairings from the script (may need to restructure as ESM exports)
       - Test each of the 8 pairings passes AA (4.5:1)
       - Test a synthetic failing pair (e.g., oklch(0.13 0.01 85) on oklch(0.12 0.01 85)) fails
       - Test accent and param are sRGB displayable

    4. Create `src/app/__tests__/tokens.test.ts`:
       - Read src/app/globals.css as a string (using fs.readFileSync)
       - Parse @theme block content
       - Assert --color-bg contains "oklch(" and hue near 85
       - Assert --color-sunken contains "oklch(" and hue near 85
       - Assert --color-surface contains "oklch(" and hue near 85
       - Assert --color-surface-raised contains "oklch(" and hue near 85
       - Assert --color-overlay contains "oklch(" and hue near 85
       - Assert --color-text, --color-muted, --color-accent, --color-param all contain "oklch("
       - Assert --color-border and --color-border-subtle contain "oklch("
       - Use regex to extract hue values and verify they're in range 80-90 for surface tokens and 80-90 for text/muted/border tokens

    Note: tokens.test.ts tests will FAIL initially (RED) because globals.css still has hex values. They pass after Task 2 (GREEN).
  </action>
  <verify>
    <automated>npx vitest run scripts/__tests__/validate-contrast.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "culori" in devDependencies
    - scripts/validate-contrast.mjs contains "oklch(0.12 0.01 85)" and "wcagContrast" or equivalent contrast computation
    - scripts/validate-contrast.mjs contains all 8 pairings: text/bg, text/surface, text/surface-raised, muted/bg, muted/surface, accent/bg, accent/surface, param/surface
    - scripts/__tests__/validate-contrast.test.ts contains "4.5" (AA threshold) and tests for PASS and FAIL cases
    - src/app/__tests__/tokens.test.ts contains "--color-bg" and "--color-sunken" and "--color-surface-raised" and "--color-overlay" and "oklch"
    - `npx vitest run scripts/__tests__/validate-contrast.test.ts` exits 0
    - `npx vitest run src/app/__tests__/tokens.test.ts` exits non-zero (RED — hex values still in globals.css)
  </acceptance_criteria>
  <done>Contrast validation script passes all 8 pairings at 4.5:1+. Token existence tests written (failing until Task 2). culori installed.</done>
</task>

<task type="auto">
  <name>Task 2: Big bang token swap + globals.css rewrite</name>
  <files>
    src/app/globals.css
    public/textures/grain.webp
  </files>
  <read_first>
    src/app/globals.css
    .planning/phases/18-token-foundation/18-UI-SPEC.md
    .planning/phases/18-token-foundation/18-CONTEXT.md
    .planning/phases/18-token-foundation/18-RESEARCH.md
  </read_first>
  <action>
    Rewrite the @theme block in src/app/globals.css with the complete OKLCH token system. This is a big-bang swap per D-09 — replace ALL existing hex values with OKLCH equivalents plus new tokens.

    **@theme block — replace lines 3-21 entirely with:**
    ```css
    @theme {
      /* === Layer 1: Primitives (raw OKLCH palette) === */
      --color-olive-950: oklch(0.10 0.01 85);
      --color-olive-900: oklch(0.12 0.01 85);
      --color-olive-800: oklch(0.16 0.01 85);
      --color-olive-700: oklch(0.20 0.015 85);
      --color-olive-600: oklch(0.24 0.015 85);
      --color-olive-500: oklch(0.25 0.01 85);
      --color-olive-400: oklch(0.20 0.008 85);
      --color-olive-200: oklch(0.55 0.01 85);
      --color-olive-100: oklch(0.93 0.01 85);

      --color-lime-500: oklch(0.85 0.18 105);
      --color-lime-400: oklch(0.72 0.15 120);
      --color-red-500: oklch(0.55 0.2 25);

      /* === Layer 2: Semantic aliases === */
      --color-bg: var(--color-olive-900);
      --color-sunken: var(--color-olive-950);
      --color-surface: var(--color-olive-800);
      --color-surface-raised: var(--color-olive-700);
      --color-overlay: var(--color-olive-600);
      --color-text: var(--color-olive-100);
      --color-muted: var(--color-olive-200);
      --color-accent: var(--color-lime-500);
      --color-param: var(--color-lime-400);
      --color-border: var(--color-olive-500);
      --color-border-subtle: var(--color-olive-400);
      --color-destructive: var(--color-red-500);

      /* === Spacing (unchanged per D-07) === */
      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;
      --spacing-2xl: 48px;
      --spacing-3xl: 64px;

      /* === Radii === */
      --radius-sm: 4px;
      --radius-md: 6px;
      --radius-lg: 12px;
      --radius-full: 9999px;

      /* === Shadows === */
      --shadow-card: 0 1px 3px oklch(0 0 0 / 0.3);
      --shadow-card-hover: 0 4px 12px oklch(0.85 0.18 105 / 0.08);

      /* === Content widths === */
      --content-narrow: 720px;
      --content-wide: 960px;

      /* === z-index scale === */
      --z-base: 0;
      --z-sticky: 100;
      --z-dropdown: 200;
      --z-tooltip: 300;
      --z-modal: 400;

      /* === Fonts (unchanged) === */
      --font-sans: var(--font-inter);
      --font-mono: var(--font-jetbrains-mono);
    }
    ```

    **Fix color-mix() calls (Pitfall 4 from RESEARCH.md):**
    - Line 112: Change `color-mix(in srgb, var(--color-surface) 80%, var(--color-muted))` to `color-mix(in oklch, var(--color-surface) 80%, var(--color-muted))` OR replace with `var(--color-border-subtle)` (preferred — uses the new token directly)
    - Line 139: Same change for the second color-mix() in .param-table td

    **Fix pulse-glow keyframe (Pitfall 5 from RESEARCH.md):**
    - Lines 199-200: Replace `rgba(200, 255, 0, 0.4)` with `oklch(0.85 0.18 105 / 0.4)`
    - Replace `rgba(200, 255, 0, 0)` with `oklch(0.85 0.18 105 / 0)`

    **Add grain texture overlay (D-05):**
    After the @layer base block closing brace and before @keyframes, add:
    ```css
    /* Grain texture overlay — D-05 */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      opacity: 0.03;
      pointer-events: none;
      background-image: url('/textures/grain.webp');
      background-repeat: repeat;
      mix-blend-mode: overlay;
      z-index: 9999;
    }
    ```

    **Create grain texture asset:**
    Generate a 64x64 pixel noise texture as public/textures/grain.webp. Use a simple Node.js script with sharp (already a Next.js dependency) to create random grayscale noise:
    ```js
    // One-off script — run once, commit result, delete script
    const sharp = require('sharp');
    const pixels = Buffer.alloc(64 * 64);
    for (let i = 0; i < pixels.length; i++) pixels[i] = Math.random() * 255;
    sharp(pixels, { raw: { width: 64, height: 64, channels: 1 } })
      .webp({ quality: 80 })
      .toFile('public/textures/grain.webp');
    ```
    Run this to generate the asset, then delete the script.

    **Preserve all existing CSS rules** in @layer base — only change the @theme block and the specific lines noted above.
  </action>
  <verify>
    <automated>npx vitest run src/app/__tests__/tokens.test.ts --reporter=verbose && node scripts/validate-contrast.mjs</automated>
  </verify>
  <acceptance_criteria>
    - globals.css @theme contains "oklch(0.12 0.01 85)" (bg value)
    - globals.css @theme contains "--color-sunken" and "--color-surface-raised" and "--color-overlay"
    - globals.css @theme contains "--color-border:" and "--color-border-subtle:"
    - globals.css @theme contains "--radius-sm:" and "--radius-md:" and "--radius-lg:"
    - globals.css @theme contains "--shadow-card:" and "--shadow-card-hover:"
    - globals.css @theme contains "--z-base:" and "--z-sticky:"
    - globals.css does NOT contain "#0a0a0a" or "#161616" or "#e8e8e8" or "#737373" or "#c8ff00" or "#a3e635" (all hex tokens removed)
    - globals.css does NOT contain "rgba(200, 255, 0" (pulse-glow fixed)
    - globals.css does NOT contain "color-mix(in srgb" (color-mix fixed to oklch or replaced with token)
    - globals.css contains "body::before" with "grain.webp" (grain overlay)
    - public/textures/grain.webp exists and is > 0 bytes
    - `npx vitest run src/app/__tests__/tokens.test.ts` exits 0 (GREEN)
    - `node scripts/validate-contrast.mjs` exits 0 (all pairings PASS)
  </acceptance_criteria>
  <done>All hex tokens replaced with OKLCH. 5 surface elevations + border + shadow + radius + z-index tokens live. Grain texture applied. pulse-glow and color-mix fixed. All token tests and contrast validation pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/app/__tests__/tokens.test.ts --reporter=verbose` — all token existence and hue angle tests pass
2. `npx vitest run scripts/__tests__/validate-contrast.test.ts --reporter=verbose` — all contrast ratio tests pass
3. `node scripts/validate-contrast.mjs` — all 8 pairings report PASS at 4.5:1+
4. `grep -c 'oklch(' src/app/globals.css` — returns 15+ (all OKLCH values present)
5. `grep '#0a0a0a\|#161616\|#e8e8e8\|#737373\|#c8ff00\|#a3e635' src/app/globals.css` — returns nothing (no hex remnants)
6. `npx next build` — build succeeds with no errors
</verification>

<success_criteria>
- globals.css @theme uses only OKLCH values for all colors
- 5 distinct surface elevations with warm olive hue ~85
- All foreground/background pairings pass WCAG AA (4.5:1)
- Grain texture overlay renders on body
- No hex color remnants in @theme
- Build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-token-foundation/18-01-SUMMARY.md`
</output>
