---
phase: 18-token-foundation
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/dev/tokens/page.tsx
autonomous: false
requirements: [TOKEN-01, TOKEN-04, TOKEN-05]
must_haves:
  truths:
    - "Developer can visit /dev/tokens and see all 5 surface elevation swatches rendered with correct warm olive tones"
    - "Developer can see contrast ratio numbers and AA pass/fail badges for all 8 critical foreground/background pairings"
    - "User confirms warm olive tone is visually distinct from previous cold gray palette"
  artifacts:
    - path: "src/app/dev/tokens/page.tsx"
      provides: "Visual token verification page with surface swatches, text samples, and contrast pairings"
      min_lines: 60
  key_links:
    - from: "src/app/dev/tokens/page.tsx"
      to: "src/app/globals.css @theme"
      via: "Tailwind utility classes consuming token CSS variables"
      pattern: "bg-bg|bg-sunken|bg-surface|bg-surface-raised|bg-overlay"
---

<objective>
Create a /dev/tokens visual verification page that displays all color swatches, surface elevation levels, text color samples, and contrast pairings with computed AA pass/fail badges. Then checkpoint for human visual verification of the warm olive palette.

Purpose: Satisfies D-10's requirement for a visual verification page and provides the human with a single URL to confirm the entire token system looks correct.
Output: /dev/tokens page, visual approval of warm palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-token-foundation/18-CONTEXT.md
@.planning/phases/18-token-foundation/18-UI-SPEC.md
@src/app/globals.css

<interfaces>
<!-- Color tokens from Plan 01 @theme, available as Tailwind utility classes -->
bg-bg, bg-sunken, bg-surface, bg-surface-raised, bg-overlay
text-text, text-muted, text-accent, text-param
border-border, border-border-subtle
text-destructive (from --color-destructive)

<!-- UI-SPEC copy contract for /dev/tokens page -->
Page title: "Design Tokens"
Section: Surfaces -> "Surface Elevations"
Section: Text -> "Text Colors"
Section: Borders -> "Border Colors"
Section: Contrast -> "Contrast Validation"
Pass badge: "AA Pass" (green)
Fail badge: "AA Fail" (red)
Ratio display: "{ratio}:1"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /dev/tokens visual verification page</name>
  <files>src/app/dev/tokens/page.tsx</files>
  <read_first>
    src/app/globals.css
    src/app/page.tsx
    .planning/phases/18-token-foundation/18-UI-SPEC.md
  </read_first>
  <action>
    Create `src/app/dev/tokens/page.tsx` as a Next.js App Router page (server component is fine — no interactivity needed).

    The page has 4 sections per UI-SPEC copy contract:

    **Section 1: "Surface Elevations"**
    - Render 5 rectangular swatches (min 120x80px each), side by side in a flex row:
      - bg (bg-bg) labeled "bg — oklch(0.12 0.01 85)"
      - sunken (bg-sunken) labeled "sunken — oklch(0.10 0.01 85)"
      - surface (bg-surface) labeled "surface — oklch(0.16 0.01 85)"
      - surface-raised (bg-surface-raised) labeled "surface-raised — oklch(0.20 0.015 85)"
      - overlay (bg-overlay) labeled "overlay — oklch(0.24 0.015 85)"
    - Each swatch shows its name and OKLCH value as text inside the swatch

    **Section 2: "Text Colors"**
    - Show text samples on bg-bg background:
      - "Primary text" in text-text
      - "Muted text" in text-muted
      - "Accent text" in text-accent
      - "Param text" in text-param
      - "Destructive text" in text-destructive

    **Section 3: "Border Colors"**
    - Two bordered boxes on bg-bg:
      - Box with border-border (1px solid), labeled "border"
      - Box with border-border-subtle (1px solid), labeled "border-subtle"

    **Section 4: "Contrast Validation"**
    - Table with columns: Foreground, Background, Ratio, Status
    - 8 rows for each critical pairing from UI-SPEC:
      1. text on bg — target 4.5:1
      2. text on surface — target 4.5:1
      3. text on surface-raised — target 4.5:1
      4. muted on bg — target 4.5:1
      5. muted on surface — target 4.5:1
      6. accent on bg — target 4.5:1
      7. accent on surface — target 4.5:1
      8. param on surface — target 4.5:1
    - Each row shows a small color swatch for fg and bg, the pairing name, the ratio (hardcoded from validate-contrast.mjs output — this is a static dev page, not a live calculator), and an "AA Pass" badge in green or "AA Fail" badge in red
    - For now, hardcode the ratio values. The executor should run `node scripts/validate-contrast.mjs` to get the actual ratios and embed them.

    **Page layout:**
    - Title: `<h1>` with text "Design Tokens" in text-text
    - Each section: `<h2>` with section name, then content
    - Page background: bg-bg
    - Use token-based spacing throughout (p-lg for sections, gap-md between swatches)
    - No special styling needed — this is a developer diagnostic page

    **Metadata:**
    - Export metadata: `{ title: "Design Tokens" }`
    - No need for layout.tsx — inherits root layout
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dev/tokens/page.tsx exists with > 60 lines
    - File contains "Surface Elevations" and "Text Colors" and "Border Colors" and "Contrast Validation"
    - File contains "bg-bg" and "bg-sunken" and "bg-surface" and "bg-surface-raised" and "bg-overlay"
    - File contains "text-text" and "text-muted" and "text-accent" and "text-param"
    - File contains "AA Pass" and "AA Fail" (or conditional rendering for pass/fail badges)
    - File contains at least 5 of the 8 ratio values (hardcoded from script output)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>/dev/tokens page renders all 5 surface swatches, text samples, border samples, and 8 contrast pairings with ratios and AA badges. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of warm olive palette</name>
  <files>none</files>
  <action>
    Present the completed warm olive palette for human visual verification. Start dev server if not running (`npm run dev`). Direct user to verification URLs.
  </action>
  <what-built>
    Complete OKLCH token system with warm olive dark palette, 5 surface elevation levels, grain texture overlay, contrast-validated text colors, and /dev/tokens diagnostic page. All existing components automatically inherit the new warm tones through the Tailwind @theme cascade.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000 — the homepage should show warm olive-toned dark surfaces instead of cold neutral grays
    3. Visit http://localhost:3000/dev/tokens — verify:
       a. 5 surface swatches show progressively lighter warm olive tones (not cold gray)
       b. Text colors are readable on their respective backgrounds
       c. All 8 contrast pairings show "AA Pass" badges
       d. Grain texture is barely perceptible on the page background (look closely at flat surfaces)
    4. Visit http://localhost:3000/instruments/evolver/sessions — verify session cards have warm surface tones
    5. Visit http://localhost:3000/instruments/cascadia/patches — verify patch cards have warm surface tones
    6. Verify accent color (#c8ff00 area) still feels vibrant but slightly warmer/more organic
    7. Verify muted text (timestamps, secondary labels) is readable — not too dim
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — requires human visual verification"</automated>
  </verify>
  <done>Human confirms warm olive palette is visually correct across all pages, or provides specific adjustment feedback.</done>
  <resume-signal>Type "approved" to confirm the warm palette looks correct, or describe specific issues (e.g., "surfaces too yellow", "muted text too dim", "accent needs more warmth")</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` — build includes /dev/tokens route without errors
2. Visual inspection at /dev/tokens confirms warm olive palette with AA-passing contrast
3. Human approves the visual shift from cold to warm across existing pages
</verification>

<success_criteria>
- /dev/tokens page renders all color swatches, text samples, border samples, and contrast pairings
- Human confirms warm olive palette looks correct across the app
- No visual regressions on existing pages (layout unchanged, only color temperature different)
</success_criteria>

<output>
After completion, create `.planning/phases/18-token-foundation/18-03-SUMMARY.md`
</output>
