---
phase: 17-content-pedagogy
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/content/instruments/evolver/troubleshooting.md
  - src/content/instruments/cascadia/troubleshooting.md
autonomous: true
requirements:
  - CONTENT-01

must_haves:
  truths:
    - "Evolver troubleshooting guide has 4 symptom sections with 3-5 checklist items each containing specific parameter values"
    - "Cascadia troubleshooting guide has 4 symptom sections with 3-5 checklist items each tailored to semi-modular architecture"
    - "Both troubleshooting files parse successfully against TroubleshootingSchema"
  artifacts:
    - path: "src/content/instruments/evolver/troubleshooting.md"
      provides: "Evolver troubleshooting checklist"
      contains: "No Audio Output"
    - path: "src/content/instruments/cascadia/troubleshooting.md"
      provides: "Cascadia troubleshooting checklist"
      contains: "No Audio Output"
  key_links:
    - from: "src/content/instruments/evolver/troubleshooting.md"
      to: "src/lib/content/schemas.ts"
      via: "TroubleshootingSchema validates frontmatter"
      pattern: "type: troubleshooting"
    - from: "src/content/instruments/cascadia/troubleshooting.md"
      to: "src/lib/content/schemas.ts"
      via: "TroubleshootingSchema validates frontmatter"
      pattern: "type: troubleshooting"
---

<objective>
Author troubleshooting guide content for both instruments: Evolver and Cascadia. Each guide has 4 symptom-based H2 sections with 3-5 ordered checkbox items containing specific parameter names and values.

Purpose: CONTENT-01 requires each instrument to have an accessible troubleshooting guide addressing common issues. Plan 01 built the code plumbing; this plan creates the actual content.
Output: Two troubleshooting.md files in src/content/instruments/{evolver,cascadia}/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-content-pedagogy/17-CONTEXT.md
@.planning/phases/17-content-pedagogy/17-RESEARCH.md
@.planning/phases/17-content-pedagogy/17-01-SUMMARY.md

@references/Evo_Key_Manual_1.3.pdf
@references/evolverguide.pdf
@references/cascadia_manual_v1.1.pdf
@src/content/instruments/evolver/overview.md
@src/content/instruments/cascadia/overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolver troubleshooting guide</name>
  <files>src/content/instruments/evolver/troubleshooting.md</files>
  <read_first>
    - src/content/instruments/evolver/overview.md
    - src/content/instruments/evolver/signal-flow.md
    - references/Evo_Key_Manual_1.3.pdf (skim for parameter numbers, audio routing, common issues)
    - references/evolverguide.pdf (skim for troubleshooting tips, common beginner mistakes)
    - src/lib/content/schemas.ts (verify TroubleshootingSchema fields)
  </read_first>
  <action>
    Create `src/content/instruments/evolver/troubleshooting.md` with this exact structure (per D-01, D-02, D-03, D-04, D-05, D-06):

    Frontmatter:
    ```yaml
    ---
    type: troubleshooting
    instrument: evolver
    title: "Evolver Troubleshooting Guide"
    ---
    ```

    Body must contain exactly 4 H2 sections (per D-05), each with 3-5 checkbox items (per D-06) with specific parameter values (per D-02):

    **Section 1: `## No Audio Output`**
    Checklist items covering: Master Volume parameter (> 80), Oscillator levels (Osc 1 Level and/or Osc 2 Level > 80), VCA Envelope (Env 3 Amount > 0, verify Env 3 destination = VCA), audio cable connection (Main Output, left = mono), external audio interface gain.

    **Section 2: `## Filter Sounds Wrong`**
    Checklist items covering: Filter Frequency position (LPF Freq parameter), Filter Mode selection (2-pole vs 4-pole), Resonance level (check if self-oscillating at high values), Filter Envelope amount and routing (Env 2 to Filter), Audio Path routing (check if both analog and digital paths are active when expected).

    **Section 3: `## Modulation Not Working`**
    Checklist items covering: Mod Destination assignment (verify in Mod Matrix rows 1-4), Mod Amount (> 0), LFO Rate (not at minimum/zero), Envelope trigger mode (check Key Sync), Sequencer on/off state if using sequence modulation.

    **Section 4: `## MIDI / SysEx Issues`**
    Checklist items covering: MIDI Channel match (Global parameter), MIDI cable connection (In/Out/Thru), Local Control setting (On for standalone, Off for DAW), SysEx Enable parameter, Program Change receive enable.

    Each checklist item format (per D-01):
    ```markdown
    - [ ] **Parameter Name** value or condition (parameter number if known)
    ```

    Content should feel like a quick reference card (per specifics in CONTEXT.md): glance at it, check the obvious things, get back to making sound. Total length: approximately 40-60 lines of markdown.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && node -e "
const matter = require('gray-matter');
const fs = require('fs');
const f = fs.readFileSync('src/content/instruments/evolver/troubleshooting.md', 'utf-8');
const { data, content } = matter(f);
const sections = content.match(/^## .+$/gm);
const checkboxes = content.match(/^- \[ \] /gm);
console.log('type:', data.type);
console.log('instrument:', data.instrument);
console.log('sections:', sections?.length);
console.log('checkboxes:', checkboxes?.length);
if (data.type !== 'troubleshooting') process.exit(1);
if (sections?.length !== 4) process.exit(1);
if (!checkboxes || checkboxes.length < 12) process.exit(1);
console.log('PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - src/content/instruments/evolver/troubleshooting.md exists
    - Frontmatter contains `type: troubleshooting` and `instrument: evolver`
    - File contains exactly 4 H2 headings: "No Audio Output", "Filter Sounds Wrong", "Modulation Not Working", "MIDI / SysEx Issues"
    - Each H2 section has at least 3 checkbox items (`- [ ]` lines)
    - At least 3 checkbox items contain specific numeric values (e.g., "> 80", "> 0")
    - File contains "Master Volume" or "Master Vol" in the No Audio Output section
  </acceptance_criteria>
  <done>Evolver troubleshooting guide exists with 4 symptom sections, 3-5 items each, specific parameter values, parseable by TroubleshootingSchema</done>
</task>

<task type="auto">
  <name>Task 2: Cascadia troubleshooting guide</name>
  <files>src/content/instruments/cascadia/troubleshooting.md</files>
  <read_first>
    - src/content/instruments/cascadia/overview.md
    - src/content/instruments/cascadia/signal-flow.md
    - references/cascadia_manual_v1.1.pdf (skim for normalled connections, patch points, CV levels, common issues)
    - src/lib/content/schemas.ts (verify TroubleshootingSchema fields)
  </read_first>
  <action>
    Create `src/content/instruments/cascadia/troubleshooting.md` with this exact structure (per D-01, D-02, D-03, D-04, D-07, D-08):

    Frontmatter:
    ```yaml
    ---
    type: troubleshooting
    instrument: cascadia
    title: "Cascadia Troubleshooting Guide"
    ---
    ```

    Body must contain exactly 4 H2 sections (per D-07), each with 3-5 checkbox items (per D-08) with specific parameter values (per D-02), tailored to Cascadia's semi-modular architecture (patch points, normalled connections, CV levels):

    **Section 1: `## No Audio Output`**
    Checklist items covering: VCA LEVEL knob position (> 30%), Mixer channel levels (at least one source > 0), VCF FREQ not fully closed (> 10%), audio cable in correct output (MAIN OUT or headphone), normalled VCA CV connection (if cable in VCA CV jack, verify source is providing voltage).

    **Section 2: `## No Output from Patch Point`**
    Checklist items covering: Cable fully inserted (half-inserted breaks normalled connection), correct jack (output jacks are filled white, input jacks are dark), source module generating signal (check upstream), attenuator/attenuverter at > 0 if in signal path, normalled connection broken by cable (expected behavior — re-check routing).

    **Section 3: `## Unexpected Sound Behavior`**
    Checklist items covering: VCF MODE setting (LP4 vs LP1 vs BP vs HP — different character), Wave Folder SYMMETRY and SHAPE at noon for neutral starting point, VCO FINE TUNE near center (small drift is normal), multiple modulation sources summing (check what is patched to same destination), Mixer SUB TYPE switch position (Sub vs Noise).

    **Section 4: `## Modulation Routing Issues`**
    Checklist items covering: Attenuverter knob position (noon = zero, CW = positive, CCW = inverted), LFO RATE not at minimum, Envelope ATTACK/RELEASE not at extremes unless intended, S&H RATE > 0 if using sample-and-hold, CV cable patched to correct FM/CV input (not an output).

    Each checklist item format (per D-01):
    ```markdown
    - [ ] **Control Name** value or condition — brief explanation of why
    ```

    Content should be tailored to Cascadia's semi-modular nature (per D-08): emphasize normalled connections, patch point behavior, CV levels, and the fact that inserting a cable breaks the normalled path. Total length: approximately 40-60 lines of markdown.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && node -e "
const matter = require('gray-matter');
const fs = require('fs');
const f = fs.readFileSync('src/content/instruments/cascadia/troubleshooting.md', 'utf-8');
const { data, content } = matter(f);
const sections = content.match(/^## .+$/gm);
const checkboxes = content.match(/^- \[ \] /gm);
console.log('type:', data.type);
console.log('instrument:', data.instrument);
console.log('sections:', sections?.length);
console.log('checkboxes:', checkboxes?.length);
if (data.type !== 'troubleshooting') process.exit(1);
if (sections?.length !== 4) process.exit(1);
if (!checkboxes || checkboxes.length < 12) process.exit(1);
console.log('PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - src/content/instruments/cascadia/troubleshooting.md exists
    - Frontmatter contains `type: troubleshooting` and `instrument: cascadia`
    - File contains exactly 4 H2 headings: "No Audio Output", "No Output from Patch Point", "Unexpected Sound Behavior", "Modulation Routing Issues"
    - Each H2 section has at least 3 checkbox items (`- [ ]` lines)
    - File contains "normalled" (referencing normalled connections)
    - File contains at least 2 references to patch points or cables
  </acceptance_criteria>
  <done>Cascadia troubleshooting guide exists with 4 symptom sections, 3-5 items each, specific control values, semi-modular-aware, parseable by TroubleshootingSchema</done>
</task>

</tasks>

<verification>
- Both troubleshooting.md files parse with gray-matter without errors
- Both have `type: troubleshooting` in frontmatter
- Evolver has sections: No Audio Output, Filter Sounds Wrong, Modulation Not Working, MIDI / SysEx Issues
- Cascadia has sections: No Audio Output, No Output from Patch Point, Unexpected Sound Behavior, Modulation Routing Issues
- Each section has 3-5 checkbox items
- `npx vitest run` still passes (no regressions)
</verification>

<success_criteria>
Both instruments have troubleshooting guides with symptom-based checklists containing specific parameter values. Content is accurate to each instrument's architecture. Files are parseable by TroubleshootingSchema.
</success_criteria>

<output>
After completion, create `.planning/phases/17-content-pedagogy/17-02-SUMMARY.md`
</output>
