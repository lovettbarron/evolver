---
phase: 17-content-pedagogy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sessions/evolver/36-partial-recipe-bass.md
  - sessions/evolver/37-partial-recipe-pad.md
  - sessions/cascadia/26-partial-recipe-texture.md
  - sessions/cascadia/27-partial-recipe-percussion.md
  - src/content/sessions/evolver/36-partial-recipe-bass.md
  - src/content/sessions/evolver/37-partial-recipe-pad.md
  - src/content/sessions/cascadia/26-partial-recipe-texture.md
  - src/content/sessions/cascadia/27-partial-recipe-percussion.md
autonomous: true
requirements:
  - CONTENT-02

must_haves:
  truths:
    - "Each instrument has 2 partial recipe sessions with target sound description, starting patch, and numbered steps with 2-3 blanks per session"
    - "Each blank includes a parenthetical hint referencing a specific prior session number"
    - "New sessions chain into existing prerequisite sequence (Evolver 36->35, 37->36; Cascadia 26->25, 27->26)"
    - "Session files exist in all three locations: sessions/, src/content/sessions/, and ~/song/sessions/"
    - "All new sessions validate against existing SessionSchema"
  artifacts:
    - path: "src/content/sessions/evolver/36-partial-recipe-bass.md"
      provides: "Evolver partial recipe bass session"
      contains: "____"
    - path: "src/content/sessions/evolver/37-partial-recipe-pad.md"
      provides: "Evolver partial recipe pad session"
      contains: "____"
    - path: "src/content/sessions/cascadia/26-partial-recipe-texture.md"
      provides: "Cascadia partial recipe texture session"
      contains: "____"
    - path: "src/content/sessions/cascadia/27-partial-recipe-percussion.md"
      provides: "Cascadia partial recipe percussion session"
      contains: "____"
  key_links:
    - from: "src/content/sessions/evolver/36-partial-recipe-bass.md"
      to: "src/lib/content/schemas.ts"
      via: "SessionSchema validates frontmatter"
      pattern: "session_number: 36"
    - from: "src/content/sessions/cascadia/26-partial-recipe-texture.md"
      to: "src/lib/content/schemas.ts"
      via: "SessionSchema validates frontmatter"
      pattern: "session_number: 26"
---

<objective>
Author 4 partial recipe sessions (2 per instrument) that give the target sound and starting patch but leave 2-3 key parameters as blanks with hints referencing prior sessions.

Purpose: CONTENT-02 requires transitional sessions that scaffold the move from guided to freeform practice. These sessions give incomplete instructions, requiring the learner to fill gaps using knowledge from prior sessions.
Output: 4 session files, each written to all 3 locations (sessions/, src/content/sessions/, ~/song/sessions/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-content-pedagogy/17-CONTEXT.md
@.planning/phases/17-content-pedagogy/17-RESEARCH.md

@src/lib/content/schemas.ts
@src/content/sessions/evolver/35-integration-composition.md
@src/content/sessions/cascadia/25-sound-design-ambient.md
@references/Evo_Key_Manual_1.3.pdf
@references/evolverguide.pdf
@references/cascadia_manual_v1.1.pdf

<interfaces>
From src/lib/content/schemas.ts:
```typescript
export const SessionSchema = z.object({
  title: z.string(),
  module: z.string(),
  session_number: z.number().int().positive(),
  duration: z.number().int().min(5).max(30),
  prerequisite: z.union([z.number(), z.null()]),
  output_type: z.enum(['patch', 'technique', 'recording', 'composition']),
  difficulty: z.enum(['beginner', 'intermediate', 'advanced']),
  tags: z.array(z.string()),
  instrument: z.string(),
  reference: z.string().optional(),
}).passthrough();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolver partial recipe sessions (36 and 37)</name>
  <files>sessions/evolver/36-partial-recipe-bass.md, sessions/evolver/37-partial-recipe-pad.md, src/content/sessions/evolver/36-partial-recipe-bass.md, src/content/sessions/evolver/37-partial-recipe-pad.md</files>
  <read_first>
    - src/content/sessions/evolver/35-integration-composition.md
    - src/content/sessions/evolver/27-recipes-bass.md
    - src/content/sessions/evolver/06-oscillators-analog-detuning.md
    - src/content/sessions/evolver/11-filter-lowpass-basics.md
    - src/content/sessions/evolver/13-filter-resonance-character.md
    - src/content/sessions/evolver/18-modmatrix-introduction.md
    - src/content/sessions/evolver/22-sound-design-pads.md
    - src/lib/content/schemas.ts
    - references/Evo_Key_Manual_1.3.pdf (parameter reference for Evolver controls)
  </read_first>
  <action>
    Create 2 Evolver partial recipe sessions (per D-09, D-10, D-11, D-12, D-13).

    **Session 36: `36-partial-recipe-bass.md`**

    Frontmatter (per D-13, must validate against SessionSchema):
    ```yaml
    ---
    title: "Session 36: Partial Recipe -- Growling Bass"
    module: "Transitional"
    session_number: 36
    duration: 25
    prerequisite: 35
    output_type: patch
    difficulty: advanced
    tags: [partial-recipe, transitional, sound-design, bass]
    instrument: evolver
    ---
    ```

    Body structure (per D-11):
    - H1 matching title
    - **Objective:** paragraph describing the target sound (a growling, aggressive bass with filter movement and analog warmth)
    - **Target Sound:** brief description of what the finished patch should sound like
    - **Starting Patch:** Start from the basic patch (per CLAUDE.md guardrail 1)
    - **Steps:** Numbered steps building the bass sound. Include 2-3 blanks (underscores `____`) for key parameters the learner must fill in. Each blank has a parenthetical hint (per D-12) referencing a specific prior session:
      - Example: Set **Osc 1 Frequency** = `____` (hint: Session 06 covered analog detuning -- what frequency offset creates that thick unison feel?)
      - Example: Set **LPF Frequency** = `____` (hint: Session 11 covered lowpass basics -- where does the cutoff sit for warm-but-not-muddy bass?)
      - Example: Set **Resonance** = `____` (hint: Session 13 explored resonance character -- how much adds growl without self-oscillation?)
    - **Listen For:** what the learner should hear when they get it right
    - **Reflection:** brief prompt to think about what they chose and why

    **Session 37: `37-partial-recipe-pad.md`**

    Frontmatter:
    ```yaml
    ---
    title: "Session 37: Partial Recipe -- Evolving Pad"
    module: "Transitional"
    session_number: 37
    duration: 25
    prerequisite: 36
    output_type: patch
    difficulty: advanced
    tags: [partial-recipe, transitional, sound-design, pad]
    instrument: evolver
    ---
    ```

    Body: Similar structure targeting an evolving pad sound. Blanks should reference different sessions than Session 36 (e.g., Session 18 for mod matrix routing, Session 22 for pad design techniques). 2-3 blanks with hints.

    **Triple-location sync (per CLAUDE.md content pipeline):**
    After creating each file in `sessions/evolver/`, copy the identical content to:
    - `src/content/sessions/evolver/36-partial-recipe-bass.md`
    - `src/content/sessions/evolver/37-partial-recipe-pad.md`
    Also copy to `~/song/sessions/evolver/` (create directory if needed):
    - `~/song/sessions/evolver/36-partial-recipe-bass.md`
    - `~/song/sessions/evolver/37-partial-recipe-pad.md`

    Each session should be approximately 60-90 lines of markdown. Follow the tone and format of existing sessions (H1 title, Objective, sections with clear headers). The sessions should feel like a creative prompt from a music teacher, not a test (per specifics in CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && node -e "
const matter = require('gray-matter');
const fs = require('fs');
for (const n of [36, 37]) {
  const name = n === 36 ? '36-partial-recipe-bass.md' : '37-partial-recipe-pad.md';
  for (const dir of ['sessions/evolver', 'src/content/sessions/evolver']) {
    const f = fs.readFileSync(dir + '/' + name, 'utf-8');
    const { data, content } = matter(f);
    if (data.session_number !== n) { console.error('Wrong session_number in', dir, name); process.exit(1); }
    if (data.instrument !== 'evolver') { console.error('Wrong instrument'); process.exit(1); }
    if (!content.includes('____')) { console.error('No blanks in', name); process.exit(1); }
    if (!content.match(/hint:/i)) { console.error('No hints in', name); process.exit(1); }
  }
}
// Check vault copy exists
const home = require('os').homedir();
const vaultPath = home + '/song/sessions/evolver/36-partial-recipe-bass.md';
if (!fs.existsSync(vaultPath)) { console.error('Vault copy missing'); process.exit(1); }
console.log('PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - sessions/evolver/36-partial-recipe-bass.md exists with `session_number: 36`, `prerequisite: 35`, `instrument: evolver`
    - sessions/evolver/37-partial-recipe-pad.md exists with `session_number: 37`, `prerequisite: 36`, `instrument: evolver`
    - src/content/sessions/evolver/36-partial-recipe-bass.md is identical to sessions/evolver/36-partial-recipe-bass.md
    - src/content/sessions/evolver/37-partial-recipe-pad.md is identical to sessions/evolver/37-partial-recipe-pad.md
    - ~/song/sessions/evolver/36-partial-recipe-bass.md exists
    - ~/song/sessions/evolver/37-partial-recipe-pad.md exists
    - Both files contain at least 2 occurrences of `____` (blanks)
    - Both files contain at least 2 occurrences of `hint:` (parenthetical hints)
    - Both files contain `module: "Transitional"` in frontmatter
    - Both files contain `tags:` with `partial-recipe` in the array
  </acceptance_criteria>
  <done>Two Evolver partial recipe sessions exist in all 3 locations, each with target sound, starting patch, numbered steps with 2-3 blanks and hints, chained into prerequisite sequence</done>
</task>

<task type="auto">
  <name>Task 2: Cascadia partial recipe sessions (26 and 27)</name>
  <files>sessions/cascadia/26-partial-recipe-texture.md, sessions/cascadia/27-partial-recipe-percussion.md, src/content/sessions/cascadia/26-partial-recipe-texture.md, src/content/sessions/cascadia/27-partial-recipe-percussion.md</files>
  <read_first>
    - src/content/sessions/cascadia/25-sound-design-ambient.md
    - src/content/sessions/cascadia/24-sound-design-texture.md
    - src/content/sessions/cascadia/23-sound-design-percussion.md
    - src/content/sessions/cascadia/05-vco-a-waveforms.md
    - src/content/sessions/cascadia/10-vcf-modes.md
    - src/content/sessions/cascadia/14-lfo-basics.md
    - src/content/sessions/cascadia/17-sample-hold.md
    - src/lib/content/schemas.ts
    - references/cascadia_manual_v1.1.pdf (module reference, patch points, normalled routing)
  </read_first>
  <action>
    Create 2 Cascadia partial recipe sessions (per D-09, D-10, D-11, D-12, D-13).

    **Session 26: `26-partial-recipe-texture.md`**

    Frontmatter (per D-13, must validate against SessionSchema):
    ```yaml
    ---
    title: "Session 26: Partial Recipe -- Metallic Texture"
    module: "Transitional"
    session_number: 26
    duration: 25
    prerequisite: 25
    output_type: patch
    difficulty: advanced
    tags: [partial-recipe, transitional, sound-design, texture]
    instrument: cascadia
    ---
    ```

    Body structure (per D-11):
    - H1 matching title
    - **Objective:** paragraph describing the target sound (a shimmering, metallic texture with wave folding and modulation)
    - **Target Sound:** brief description
    - **Starting Patch:** Remove all cables, set all knobs to noon (Cascadia equivalent of "basic patch")
    - **Steps:** Numbered steps building the texture. Include 2-3 blanks (`____`) with hints (per D-12) referencing specific prior Cascadia sessions:
      - Example: Set **VCO A waveform** to `____` (hint: Session 05 covered waveform shapes -- which one has the richest harmonics for the wave folder?)
      - Example: Patch **LFO X** to `____` at rate `____` (hint: Session 14 covered LFO basics -- what destination creates evolving metallic shimmer?)
      - Example: Set **VCF MODE** to `____` (hint: Session 10 explored filter modes -- which mode lets the most harmonics through?)
    - **Listen For:** what the learner should hear
    - **Reflection:** brief prompt

    **Session 27: `27-partial-recipe-percussion.md`**

    Frontmatter:
    ```yaml
    ---
    title: "Session 27: Partial Recipe -- Modular Drum Hit"
    module: "Transitional"
    session_number: 27
    duration: 25
    prerequisite: 26
    output_type: patch
    difficulty: advanced
    tags: [partial-recipe, transitional, sound-design, percussion]
    instrument: cascadia
    ---
    ```

    Body: Similar structure targeting a percussive sound (snappy transient, short decay). Blanks should reference different sessions (e.g., Session 17 for S&H, Session 23 for percussion techniques). 2-3 blanks with hints.

    **Triple-location sync (per CLAUDE.md content pipeline):**
    After creating each file in `sessions/cascadia/`, copy identical content to:
    - `src/content/sessions/cascadia/26-partial-recipe-texture.md`
    - `src/content/sessions/cascadia/27-partial-recipe-percussion.md`
    Also copy to `~/song/sessions/cascadia/` (create directory if needed):
    - `~/song/sessions/cascadia/26-partial-recipe-texture.md`
    - `~/song/sessions/cascadia/27-partial-recipe-percussion.md`

    Each session: approximately 60-90 lines. Cascadia sessions should reference patch points, normalled connections, and cable routing where relevant. Follow tone and format of existing Cascadia sessions.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && node -e "
const matter = require('gray-matter');
const fs = require('fs');
for (const n of [26, 27]) {
  const name = n === 26 ? '26-partial-recipe-texture.md' : '27-partial-recipe-percussion.md';
  for (const dir of ['sessions/cascadia', 'src/content/sessions/cascadia']) {
    const f = fs.readFileSync(dir + '/' + name, 'utf-8');
    const { data, content } = matter(f);
    if (data.session_number !== n) { console.error('Wrong session_number in', dir, name); process.exit(1); }
    if (data.instrument !== 'cascadia') { console.error('Wrong instrument'); process.exit(1); }
    if (!content.includes('____')) { console.error('No blanks in', name); process.exit(1); }
    if (!content.match(/hint:/i)) { console.error('No hints in', name); process.exit(1); }
  }
}
// Check vault copy exists
const home = require('os').homedir();
const vaultPath = home + '/song/sessions/cascadia/26-partial-recipe-texture.md';
if (!fs.existsSync(vaultPath)) { console.error('Vault copy missing'); process.exit(1); }
console.log('PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - sessions/cascadia/26-partial-recipe-texture.md exists with `session_number: 26`, `prerequisite: 25`, `instrument: cascadia`
    - sessions/cascadia/27-partial-recipe-percussion.md exists with `session_number: 27`, `prerequisite: 26`, `instrument: cascadia`
    - src/content/sessions/cascadia/26-partial-recipe-texture.md is identical to sessions/cascadia/26-partial-recipe-texture.md
    - src/content/sessions/cascadia/27-partial-recipe-percussion.md is identical to sessions/cascadia/27-partial-recipe-percussion.md
    - ~/song/sessions/cascadia/26-partial-recipe-texture.md exists
    - ~/song/sessions/cascadia/27-partial-recipe-percussion.md exists
    - Both files contain at least 2 occurrences of `____` (blanks)
    - Both files contain at least 2 occurrences of `hint:` (parenthetical hints)
    - Both files contain `module: "Transitional"` in frontmatter
    - Both files contain `tags:` with `partial-recipe` in the array
  </acceptance_criteria>
  <done>Two Cascadia partial recipe sessions exist in all 3 locations, each with target sound, starting patch, numbered steps with 2-3 blanks and hints, chained into prerequisite sequence</done>
</task>

</tasks>

<verification>
- All 4 session files parse with gray-matter without errors
- All 4 have valid SessionSchema frontmatter (session_number, prerequisite, instrument, output_type, difficulty, tags, module, duration)
- Prerequisite chain: Evolver 36->35, 37->36; Cascadia 26->25, 27->26
- Each session has 2-3 blanks (____) with hints referencing prior sessions
- Triple-location sync: sessions/, src/content/sessions/, ~/song/sessions/
- `npx vitest run` still passes
</verification>

<success_criteria>
Four partial recipe sessions exist (2 per instrument) in all 3 content locations. Each has target sound, starting patch, numbered steps with blanks and hints. Sessions chain into existing prerequisite sequence. All validate against SessionSchema.
</success_criteria>

<output>
After completion, create `.planning/phases/17-content-pedagogy/17-03-SUMMARY.md`
</output>
