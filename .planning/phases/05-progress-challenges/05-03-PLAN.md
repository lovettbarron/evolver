---
phase: 05-progress-challenges
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/markdown/processor.ts
  - src/app/globals.css
  - src/content/sessions/evolver/27-recipes-bass.md
  - src/content/sessions/evolver/28-recipes-leads.md
  - src/content/sessions/evolver/29-recipes-pads.md
  - src/content/sessions/evolver/30-recipes-drums.md
  - src/content/patches/evolver/acid-bass.md
  - src/content/patches/evolver/fm-bass.md
  - src/content/patches/evolver/sub-bass.md
  - src/content/patches/evolver/fm-lead.md
autonomous: true
requirements:
  - CHAL-01
  - CHAL-02
  - CHAL-03
  - CHAL-04
must_haves:
  truths:
    - "Challenge callouts render with distinct styling (accent border, target icon, 'Challenge' header) in session markdown"
    - "Sessions 27-30 each contain at least one [!challenge] callout with a [[patch-name]] wikilink"
    - "Challenge callouts reference existing demo patches as targets to approximate"
    - "3-4 demo patches have challenge_id in frontmatter for realistic demo mode challenge counts"
  artifacts:
    - path: "src/lib/markdown/processor.ts"
      provides: "rehype-callouts configured with challenge type"
      contains: "challenge"
    - path: "src/app/globals.css"
      provides: "Challenge callout CSS differentiation"
      contains: "data-callout=\"challenge\""
    - path: "src/content/sessions/evolver/27-recipes-bass.md"
      provides: "Bass recipe session with challenge callouts"
      contains: "[!challenge]"
    - path: "src/content/patches/evolver/acid-bass.md"
      provides: "Demo patch with challenge_id"
      contains: "challenge_id"
  key_links:
    - from: "src/content/sessions/evolver/27-recipes-bass.md"
      to: "src/content/patches/evolver/acid-bass.md"
      via: "wikilink [[acid-bass]] in challenge callout"
      pattern: "\\[\\[acid-bass\\]\\]"
    - from: "src/lib/markdown/processor.ts"
      to: "rehype-callouts"
      via: "callouts config option with challenge type"
      pattern: "challenge.*title.*Challenge"
---

<objective>
Add challenge callout support and author challenge content in sessions.

Purpose: Enable challenge exercises in recipe/creation sessions (Modules 7-10) that reference demo patches as targets, and link demo patches back to their challenges. This completes the challenge mode feature loop.

Output: Updated markdown processor with `[!challenge]` callout type, challenge callouts in sessions 27-30, challenge_id fields in 4 demo patches, CSS styling for challenge callouts.
</objective>

<execution_context>
@/Users/andrewlovettbarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrewlovettbarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-progress-challenges/05-CONTEXT.md
@.planning/phases/05-progress-challenges/05-RESEARCH.md
@.planning/phases/05-progress-challenges/05-UI-SPEC.md
@.planning/phases/05-progress-challenges/05-01-SUMMARY.md

<interfaces>
<!-- From processor.ts -->
From src/lib/markdown/processor.ts:
```typescript
// rehype-callouts is already imported and used without options:
.use(rehypeCallouts)
// Needs to become:
.use(rehypeCallouts, { callouts: { challenge: { title: 'Challenge', indicator: '...' } } })
```

From src/app/globals.css (existing callout styles):
```css
.prose .callout {
  background-color: var(--color-surface);
  border-left: 3px solid var(--color-accent);
  padding: var(--spacing-md);
}
```

From PatchSchema (updated in Plan 01):
```typescript
challenge_id: z.string().optional(), // e.g., "27-1"
```

Existing demo patches in src/content/patches/evolver/:
- acid-bass.md (session_origin: 27)
- fm-bass.md (session_origin: 27)
- sub-bass.md (session_origin: 27)
- fm-lead.md (session_origin: 28)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure rehype-callouts for challenge type and add CSS</name>
  <files>src/lib/markdown/processor.ts, src/app/globals.css</files>
  <read_first>
    - src/lib/markdown/processor.ts
    - src/app/globals.css
    - .planning/phases/05-progress-challenges/05-RESEARCH.md
  </read_first>
  <action>
**Update src/lib/markdown/processor.ts:**

Change line 55 from:
```typescript
    .use(rehypeCallouts)
```
to:
```typescript
    .use(rehypeCallouts, {
      callouts: {
        challenge: {
          title: 'Challenge',
          indicator: '<svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>',
        },
      },
    })
```

The SVG is the Lucide "Target" icon inlined. rehype-callouts merges custom callout types with its built-in set, so existing callouts (tip, warning, etc.) continue to work.

**Update src/app/globals.css:**

After the existing `.prose .callout` rule, add:
```css
.prose .callout[data-callout="challenge"] {
  border-left-color: var(--color-accent);
}

.prose .callout[data-callout="challenge"] .callout-title {
  color: var(--color-accent);
}
```

This gives challenge callouts accent-colored left border and header text, distinguishing them from other callout types while staying within the design system.
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && grep "challenge" src/lib/markdown/processor.ts && grep 'data-callout="challenge"' src/app/globals.css && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/markdown/processor.ts contains `challenge: {` in the rehypeCallouts config
    - src/lib/markdown/processor.ts contains `title: 'Challenge'`
    - src/lib/markdown/processor.ts contains `indicator:` with SVG string containing circle elements (Target icon)
    - Existing callout types (tip, warning) still work (rehypeCallouts merges, does not replace)
    - src/app/globals.css contains `.prose .callout[data-callout="challenge"]`
    - src/app/globals.css contains `color: var(--color-accent)` for challenge callout title
    - `npx tsc --noEmit` has no errors
  </acceptance_criteria>
  <done>Markdown processor renders [!challenge] callouts with Target icon. CSS gives challenge callouts accent-colored styling.</done>
</task>

<task type="auto">
  <name>Task 2: Add challenge callouts to sessions 27-30 and challenge_id to demo patches</name>
  <files>src/content/sessions/evolver/27-recipes-bass.md, src/content/sessions/evolver/28-recipes-leads.md, src/content/sessions/evolver/29-recipes-pads.md, src/content/sessions/evolver/30-recipes-drums.md, src/content/patches/evolver/acid-bass.md, src/content/patches/evolver/fm-bass.md, src/content/patches/evolver/sub-bass.md, src/content/patches/evolver/fm-lead.md</files>
  <read_first>
    - src/content/sessions/evolver/27-recipes-bass.md
    - src/content/sessions/evolver/28-recipes-leads.md
    - src/content/sessions/evolver/29-recipes-pads.md
    - src/content/sessions/evolver/30-recipes-drums.md
    - src/content/patches/evolver/acid-bass.md
    - src/content/patches/evolver/fm-bass.md
    - src/content/patches/evolver/sub-bass.md
    - src/content/patches/evolver/fm-lead.md
  </read_first>
  <action>
**Add challenge callouts to session markdown files.** Insert each challenge callout block AFTER the final exercise section (before the Output Checklist section if one exists, or at the end of the Exercises section). Each challenge uses `> [!challenge]` syntax with a `[[patch-name]]` wikilink to an existing demo patch.

**Session 27 (27-recipes-bass.md) — add after exercises:**
```markdown
> [!challenge] Create Your Own Acid Bass
> Starting from the basic patch, create an acid bass patch with resonant filter sweep
> and glide. Your patch should approximate the character of [[acid-bass]].
> Save your result with `challenge_id: 27-1` in the frontmatter.

> [!challenge] Create Your Own Sub Bass
> Build a deep sub bass using only analog oscillators with minimal harmonics.
> Your patch should approximate the low-end weight of [[sub-bass]].
> Save your result with `challenge_id: 27-2` in the frontmatter.
```

**Session 28 (28-recipes-leads.md) — add after exercises:**
```markdown
> [!challenge] Create Your Own FM Lead
> Build an FM lead patch using digital oscillator FM modulation with filter envelope shaping.
> Your patch should approximate the timbral complexity of [[fm-lead]].
> Save your result with `challenge_id: 28-1` in the frontmatter.
```

**Session 29 (29-recipes-pads.md) — add after exercises:**
```markdown
> [!challenge] Create Your Own Evolving Pad
> Build a pad that evolves over time using LFO modulation on filter and oscillator parameters.
> Your patch should have slow, breathing movement similar to [[pwm-pad]].
> Save your result with `challenge_id: 29-1` in the frontmatter.
```

**Session 30 (30-recipes-drums.md) — add after exercises:**
```markdown
> [!challenge] Create Your Own Drum Sound
> Build a percussive sound using short envelopes and noise or FM techniques.
> Your patch should approximate the punch of [[analog-kick]].
> Save your result with `challenge_id: 30-1` in the frontmatter.
```

**Add challenge_id to 4 demo patch frontmatter files:**

acid-bass.md — add `challenge_id: "27-1"` to frontmatter
fm-bass.md — add `challenge_id: "27-2"` to frontmatter (NOTE: fm-bass maps to the sub-bass challenge conceptually, but we use it here as a second demo patch with challenge_id for count variety. Actually, use sub-bass.md for 27-2 instead.)

Correction — assign challenge_id to these 4 patches:
- acid-bass.md: add `challenge_id: "27-1"` to frontmatter
- sub-bass.md: add `challenge_id: "27-2"` to frontmatter
- fm-lead.md: add `challenge_id: "28-1"` to frontmatter
- fm-bass.md: add `challenge_id: "27-3"` to frontmatter (bonus challenge response, shows realistic multi-response)

For each patch file, add the `challenge_id` line in the YAML frontmatter block after the last existing field and before the closing `---`. Example for acid-bass.md:
```yaml
---
name: "Acid Bass"
type: bass
session_origin: 27
# ... existing fields ...
challenge_id: "27-1"
---
```
  </action>
  <verify>
    <automated>cd /Users/andrewlovettbarron/src/evolver && grep -l "\[!challenge\]" src/content/sessions/evolver/2[7-9]*.md src/content/sessions/evolver/30*.md && grep -l "challenge_id" src/content/patches/evolver/acid-bass.md src/content/patches/evolver/sub-bass.md src/content/patches/evolver/fm-lead.md src/content/patches/evolver/fm-bass.md && npm run validate-content 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/content/sessions/evolver/27-recipes-bass.md contains `> [!challenge]` (at least one callout)
    - src/content/sessions/evolver/27-recipes-bass.md contains `[[acid-bass]]` wikilink
    - src/content/sessions/evolver/27-recipes-bass.md contains `[[sub-bass]]` wikilink
    - src/content/sessions/evolver/28-recipes-leads.md contains `> [!challenge]` with `[[fm-lead]]`
    - src/content/sessions/evolver/29-recipes-pads.md contains `> [!challenge]` with `[[pwm-pad]]`
    - src/content/sessions/evolver/30-recipes-drums.md contains `> [!challenge]` with `[[analog-kick]]`
    - src/content/patches/evolver/acid-bass.md frontmatter contains `challenge_id: "27-1"`
    - src/content/patches/evolver/sub-bass.md frontmatter contains `challenge_id: "27-2"`
    - src/content/patches/evolver/fm-lead.md frontmatter contains `challenge_id: "28-1"`
    - src/content/patches/evolver/fm-bass.md frontmatter contains `challenge_id`
    - `npm run validate-content` passes (Zod validates challenge_id as optional string)
  </acceptance_criteria>
  <done>Sessions 27-30 have challenge callouts with wikilinks to reference patches. Four demo patches have challenge_id for realistic progress counts in demo mode. Content validation passes.</done>
</task>

</tasks>

<verification>
- `grep -r "\[!challenge\]" src/content/sessions/evolver/` — shows callouts in sessions 27-30
- `grep -r "challenge_id" src/content/patches/evolver/` — shows 4 patches with challenge_id
- `npm run validate-content` — all content passes validation
- `npx tsc --noEmit` — no type errors
- Rendering a session with a `[!challenge]` callout produces HTML with `data-callout="challenge"` attribute
</verification>

<success_criteria>
- Challenge callouts render with distinct styling in session markdown
- Each recipe session (27-30) has at least one challenge referencing a demo patch
- Four demo patches link back to challenges via challenge_id
- All content validates against updated schemas
</success_criteria>

<output>
After completion, create `.planning/phases/05-progress-challenges/05-03-SUMMARY.md`
</output>
