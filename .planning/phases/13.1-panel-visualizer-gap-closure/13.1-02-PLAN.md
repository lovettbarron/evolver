---
phase: 13.1-panel-visualizer-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/evolver-panel.tsx
  - src/components/__tests__/evolver-panel.test.tsx
  - src/app/instruments/[slug]/panel/page.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EPANEL-03
  - EPANEL-06
must_haves:
  truths:
    - "Section tint rectangles render at consistent 0.08 opacity across both Evolver and Cascadia panels"
    - "Section tint opacity test passes without modification"
    - "Standalone panel route passes instrumentSlug to StickyHeader"
    - "EPANEL-03 and EPANEL-06 are marked complete in REQUIREMENTS.md"
    - "EPANEL-06 requirement text reflects inline embedding, not collapsible sidebar"
    - "SessionPanelSidebar orphaned component does not exist in src/components/"
  artifacts:
    - path: "src/components/evolver-panel.tsx"
      provides: "Section tint rendering with 0.08 fillOpacity"
      contains: "fillOpacity={0.08}"
    - path: "src/app/instruments/[slug]/panel/page.tsx"
      provides: "StickyHeader with instrumentSlug prop"
      contains: "instrumentSlug={slug}"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement status"
      contains: "[x] **EPANEL-03**"
  key_links:
    - from: "src/app/instruments/[slug]/panel/page.tsx"
      to: "src/components/sticky-header.tsx"
      via: "instrumentSlug prop"
      pattern: "instrumentSlug=.slug."
---

<objective>
Fix Evolver panel section tint opacity, standalone route StickyHeader prop, remove orphaned sidebar component, and mark requirements complete.

Purpose: The Evolver panel section tint uses 0.15 opacity while the test expects 0.08 and Cascadia uses 0.08 -- standardize to 0.08. The standalone panel route is missing the instrumentSlug prop on StickyHeader. Both EPANEL-03 and EPANEL-06 are functionally complete but need their requirement checkboxes updated. The orphaned SessionPanelSidebar was already deleted but needs verification. EPANEL-06 text must reflect inline embedding.

Output: Consistent section tint rendering, passing tests, correct StickyHeader prop, updated requirements, confirmed orphan removal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-panel-visualizer-gap-closure/13.1-RESEARCH.md

@src/components/evolver-panel.tsx
@src/components/__tests__/evolver-panel.test.tsx
@src/app/instruments/[slug]/panel/page.tsx
@.planning/REQUIREMENTS.md

<interfaces>
From src/components/sticky-header.tsx:
```typescript
interface StickyHeaderProps {
  backHref: string;
  sessionIdentifier: string;
  quickRefContent: Array<...>;
  instrumentSlug?: string;  // Optional prop, already supported
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix section tint opacity and standalone route StickyHeader</name>
  <files>src/components/evolver-panel.tsx, src/app/instruments/[slug]/panel/page.tsx</files>
  <read_first>
    src/components/evolver-panel.tsx
    src/components/__tests__/evolver-panel.test.tsx
    src/app/instruments/[slug]/panel/page.tsx
  </read_first>
  <action>
    Two targeted fixes:

    1. In src/components/evolver-panel.tsx, change the section tint fillOpacity from 0.15 to 0.08 (around line 355):
       FROM: `fillOpacity={0.15}`
       TO: `fillOpacity={0.08}`
       This aligns with the Cascadia panel (0.08 at cascadia-panel.tsx line 1173) and the existing test expectation (0.08 at evolver-panel.test.tsx line 46).

    2. In src/app/instruments/[slug]/panel/page.tsx, add the instrumentSlug prop to StickyHeader (around line 25-29):
       FROM:
       ```
       <StickyHeader
         backHref={`/instruments/${slug}`}
         sessionIdentifier="Panel"
         quickRefContent={[]}
       />
       ```
       TO:
       ```
       <StickyHeader
         backHref={`/instruments/${slug}`}
         sessionIdentifier="Panel"
         quickRefContent={[]}
         instrumentSlug={slug}
       />
       ```
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/evolver-panel.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/evolver-panel.tsx contains `fillOpacity={0.08}` and does NOT contain `fillOpacity={0.15}`
    - src/app/instruments/[slug]/panel/page.tsx contains `instrumentSlug={slug}` inside the StickyHeader JSX
    - `npx vitest run src/components/__tests__/evolver-panel.test.tsx -x` exits 0 (section tint test passes)
  </acceptance_criteria>
  <done>Section tint opacity standardized to 0.08 across both panels, test passes, standalone route has correct StickyHeader prop</done>
</task>

<task type="auto">
  <name>Task 2: Update requirements, verify EPANEL-06 text, and confirm orphan removal</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Three actions:

    1. In .planning/REQUIREMENTS.md, update two requirement checkboxes:
       - Change `- [ ] **EPANEL-06**:` to `- [x] **EPANEL-06**:`
       - Verify `- [x] **EPANEL-03**:` is already checked (it should be from phase 12; if not, check it)

    2. Review the EPANEL-06 requirement body text. Per ROADMAP success criterion 5, it must reflect "inline embedding (not collapsible sidebar)". The current text reads:
       "Panel integrated into four contexts: session detail (inline via `data-evolver-panel` markdown markers with zoom support), patch detail (inline), quick-ref panel (compact tab), and standalone route (/instruments/evolver/panel)"
       This already says "inline" which is correct. Verify it does NOT mention "collapsible sidebar" or "SessionPanelSidebar". If it does, update the text to remove sidebar references and emphasize inline embedding. If the text is already correct, no change needed -- the grep verify below confirms.

    3. Confirm orphan removal: verify that src/components/session-panel-sidebar.tsx does NOT exist (it was deleted in commit a23c220). Run `ls src/components/session-panel-sidebar.tsx` and confirm it errors. Also run `grep -r 'session-panel-sidebar' src/` to confirm zero imports remain.

    4. Update the traceability table entries:
       - EPANEL-03 row: verify status is `Complete` (should already be from phase 12; if not, change `Pending` to `Complete`)
       - EPANEL-06 row: change `Pending` to `Complete`

    5. Update the coverage count line to accurately reflect completion totals after marking EPANEL-06 complete.
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*EPANEL-0[36]\*\*' .planning/REQUIREMENTS.md && grep -c 'EPANEL-06.*Complete' .planning/REQUIREMENTS.md && grep -r 'session-panel-sidebar' src/ ; test $? -ne 0 && echo "ORPHAN_CLEAN" && grep 'inline' .planning/REQUIREMENTS.md | grep -c 'EPANEL-06'</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **EPANEL-03**`
    - REQUIREMENTS.md contains `[x] **EPANEL-06**`
    - REQUIREMENTS.md EPANEL-06 body text mentions "inline" and does NOT mention "collapsible sidebar"
    - REQUIREMENTS.md traceability table shows `Complete` for both EPANEL-03 and EPANEL-06
    - REQUIREMENTS.md coverage count line is accurate
    - `grep -r 'session-panel-sidebar' src/` returns 0 matches (orphan fully removed)
    - src/components/session-panel-sidebar.tsx does NOT exist on disk
  </acceptance_criteria>
  <done>EPANEL-03 and EPANEL-06 marked complete, EPANEL-06 text confirmed as inline-first, orphaned sidebar confirmed deleted, traceability table and coverage count updated</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/__tests__/evolver-panel.test.tsx -x` -- opacity test passes
2. `npx vitest run` -- full suite green
3. `grep 'fillOpacity={0.08}' src/components/evolver-panel.tsx` -- confirms correct value
4. `grep 'instrumentSlug={slug}' src/app/instruments/\[slug\]/panel/page.tsx` -- confirms prop present
5. `grep -r 'session-panel-sidebar' src/` -- returns 0 results (orphan gone)
6. `grep 'inline' .planning/REQUIREMENTS.md | grep 'EPANEL-06'` -- confirms inline text present
</verification>

<success_criteria>
- Evolver section tint opacity matches Cascadia (0.08) and passes existing test
- Standalone panel route StickyHeader receives instrumentSlug
- EPANEL-03 and EPANEL-06 marked complete in REQUIREMENTS.md
- EPANEL-06 requirement text reflects inline embedding pattern
- SessionPanelSidebar orphaned component confirmed absent from codebase
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-panel-visualizer-gap-closure/13.1-02-SUMMARY.md`
</output>
