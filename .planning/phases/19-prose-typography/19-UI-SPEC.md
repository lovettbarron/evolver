---
phase: 19
slug: prose-typography
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-07
---

# Phase 19 — UI Design Contract

> Visual and interaction contract for Prose & Typography. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind v4 CSS-first via `@theme`) |
| Preset | not applicable |
| Component library | none (51 existing components, restyle only) |
| Icon library | Lucide React 1.7.0 (existing) |
| Fonts | Space Grotesk (display, new), Inter (body, existing), JetBrains Mono (code, existing) |

**Source:** CONTEXT.md D-01/D-02, RESEARCH STACK.md "What NOT to Add"

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, code inline padding |
| sm | 8px | Table cell padding, compact element spacing |
| md | 16px | Prose element margins, callout padding, code block padding |
| lg | 24px | Section heading top margin equivalent, section padding |
| xl | 32px | Major heading top margin equivalent |
| 2xl | 48px | Page-level vertical rhythm |
| 3xl | 64px | Page-level spacing |

Exceptions: none. Prose vertical rhythm uses `em`-based margins for heading spacing (proportional to font size) but all fixed padding uses the token scale.

**Source:** Existing globals.css `@theme` block (unchanged from Phase 18)

---

## Typography

### Typeface Assignment

| Role | Typeface | CSS Variable |
|------|----------|-------------|
| Display (h1, h2) | Space Grotesk | `--font-display` via `var(--font-space-grotesk)` |
| Body (h3, h4, p, li) | Inter | `--font-sans` via `var(--font-inter)` |
| Code (inline, blocks) | JetBrains Mono | `--font-mono` via `var(--font-jetbrains-mono)` |

**Source:** CONTEXT.md D-01/D-02/D-03

### Type Scale (4 sizes, 2 weights)

Base body size: 16px (1rem)

| Role | Size | Weight | Line Height | Letter Spacing | Typeface |
|------|------|--------|-------------|----------------|----------|
| Display (h1, h2) | 39px (2.4375rem) | 700 | 1.15 | -0.025em | Space Grotesk |
| Heading (h3, h4) | 25px (1.5625rem) | 700 | 1.3 | -0.015em | Inter |
| Body (p, li) | 16px (1rem) | 400 | 1.6 | 0 | Inter |
| Small (caption, th) | 13px (0.8125rem) | 400 | 1.4 | 0 | Inter |

**Weight palette:** 400 (regular) for body and small text, 700 (bold) for display and heading text. Two weights only.

**Implementation details (not part of the named scale):**
- Code (inline and block) renders at the mono typeface's natural size relative to body (0.875em / ~14px) — this is a font-specific adjustment, not a separate scale entry
- h1 vs h2 differentiation uses CSS `clamp()` responsive scaling (see Responsive Type Scaling below), not separate scale sizes. Both map to the Display role: h1 renders at the full 39px on desktop, h2 at a clamped range topping out at 31px
- h3 vs h4 differentiation: h3 renders at the full 25px, h4 at 20px (0.8x of Heading size) — a proportional step within the Heading role, not a separate scale entry
- Generic tables use Small (13px) for cell text and th headers

**Scale math:** Major third ratio (1.25) from 16px base: 16 x 1.25^2 = 25 (Heading), 16 x 1.25^3.2 ~ 39 (Display). Small = 16 / 1.25 ~ 13.

**Source:** CONTEXT.md D-04/D-05. Sizes rounded to nearest integer for rendering crispness. Negative letter-spacing on headings tightens Space Grotesk for editorial feel (Claude's discretion per CONTEXT.md).

### Quick-Ref Prose (Compact Variant)

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| h1 (in quick-ref) | 20px | 700 | 1.2 |
| h2 (in quick-ref) | 16px | 700 | 1.3 |
| h3 (in quick-ref) | 14px | 700 | 1.3 |

**Source:** Existing globals.css `.quick-ref-prose` rules (preserved, not redesigned)

---

## Color

Phase 19 inherits all color tokens from Phase 18 (Token Foundation). This phase does NOT define new colors; it consumes the palette established in Phase 18.

| Role | Token | Usage in This Phase |
|------|-------|---------------------|
| Dominant (60%) | `--color-bg` | Prose content background |
| Secondary (30%) | `--color-surface` | Code blocks, callout backgrounds, table even-row stripes, param-table backgrounds |
| Accent (10%) | `--color-accent` | Challenge callout border, heading anchor hover, link hover, task list checkbox |
| Param highlight | `--color-param` / `--color-accent-soft` | Inline code color, `.param-value` text |
| Text primary | `--color-text` | Body text, heading text |
| Text muted | `--color-text-muted` | Table headers, caption text |
| Border | `--color-border` | Table row separators, callout left borders (base) |
| Border subtle | `--color-border-subtle` | Table cell bottom borders, param-table row separators |

### Callout Type Colors

| Callout Type | Border Color | Title Color | data-callout Value |
|-------------|-------------|-------------|-------------------|
| Challenge | `oklch(0.75 0.15 85)` (amber) | Same as border | `challenge` |
| Tip | `oklch(0.70 0.15 145)` (green) | Same as border | `tip` |
| Warning | `oklch(0.65 0.20 25)` (red) | Same as border | `warning` |
| Default | `--color-accent` | `--color-accent` | any other |

**Source:** CONTEXT.md D-11/D-12. OKLCH values chosen for perceptual consistency at similar lightness levels (Claude's discretion). Phase 18 tokens are the primitive source.

---

## Prose Rendering Contract

### Baseline

- Apply `@plugin "@tailwindcss/typography"` in globals.css (new dependency, D-06)
- Apply `prose` and `prose-invert` classes on markdown content containers (D-07)
- Override typography plugin defaults with Phase 18 color tokens for dark palette compatibility

### Domain Element Overrides (D-08, D-09)

These elements override within the prose context. The typography plugin handles generic elements; custom CSS handles domain-specific:

| Element | Class | Styling Contract |
|---------|-------|-----------------|
| Param table | `.param-table` | Accent-colored left border (3px solid `--color-accent`), surface background on header row, `.param-name` in body weight, `.param-value` in mono + param color. Table flows inline with prose, no card container (D-10) |
| Callout | `.callout[data-callout]` | Surface background, left border colored per type (see Callout Type Colors above), icon per type, rounded right corners (0 `--radius-md` `--radius-md` 0), md padding (D-11) |
| Task list | `.contains-task-list` | No list-style, no left padding, checkbox accent-color matches `--color-accent` |
| Code block | `pre > code` | Surface background, md padding, `--radius-md` corners, mono font at 0.875em (~14px), horizontal scroll |
| Inline code | `p code, li code` | Surface background, xs vertical + sm horizontal padding, `--radius-sm` corners, param color, mono font at 0.875em (~14px) |
| Obsidian tag | `.obsidian-tag` | Muted text, Small size (13px), no decoration -- unobtrusive metadata (Claude's discretion) |
| Mermaid placeholder | `.mermaid-placeholder` | Surface background, lg padding, `--radius-md` corners, centered muted text, xl vertical margin |
| Heading anchor | `a.heading-anchor` | No underline, inherit color, accent on hover |
| Tables (generic) | `.prose table` | Full width, collapse borders, Small size (13px), th in muted + 700 weight, even-row surface stripe, border-subtle row separators |

### Responsive Type Scaling

Use CSS `clamp()` for h1 and h2 to scale down on mobile:

| Element | Mobile (320px) | Desktop (768px+) |
|---------|---------------|-------------------|
| h1 | 28px | 39px |
| h2 | 24px | 31px |
| h3-h4, body | Fixed sizes (no scaling) | Same |

Implementation: `font-size: clamp(1.75rem, 1.2rem + 2vw, 2.4375rem)` for h1, `font-size: clamp(1.5rem, 1.1rem + 1.5vw, 1.9375rem)` for h2.

**Source:** Claude's discretion per CONTEXT.md (responsive type scaling strategy)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable -- Phase 19 is prose rendering, no user actions |
| Empty state heading | Not applicable -- prose content comes from Obsidian vault markdown files |
| Empty state body | Not applicable |
| Error state | Not applicable -- markdown rendering errors are handled by the existing unified.js pipeline |
| Destructive confirmation | Not applicable -- no destructive actions in prose rendering |

**Note:** Phase 19 is a styling-only phase with no interactive elements, CTAs, or user-facing state management. All copywriting is the markdown content itself, authored in the Obsidian vault.

---

## Component Inventory

No new components are created in Phase 19. Changes are CSS-only in `globals.css` and font setup in `layout.tsx`.

| File | Change Type | Description |
|------|------------|-------------|
| `src/app/globals.css` | Modified | Add `@plugin "@tailwindcss/typography"`, rewrite `.prose` rules with tokens and type scale, add Space Grotesk font variable, add callout type colors, update param-table to accent-border style |
| `src/app/layout.tsx` | Modified | Add `Space_Grotesk` import from `next/font/google`, add `--font-space-grotesk` CSS variable to html className |
| `src/components/quick-ref-panel.tsx` | Verify only | Confirm `.quick-ref-prose` still renders correctly after prose rewrite |

### Affected Rendering Surfaces

All pages rendering markdown content are affected (CSS cascade):
- 60+ session detail pages
- 36+ patch detail pages
- Instrument overview pages
- Module detail pages
- Quick-ref panel (compact variant)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

**Note:** No component registry is used. The only new dependency is `@tailwindcss/typography@^0.5.19`, an official Tailwind Labs package installed via npm (not a shadcn registry block).

---

## Interaction States

Phase 19 modifies only two interactive prose elements:

| Element | State | Visual Treatment |
|---------|-------|-----------------|
| Link (`a` in prose) | default | Body text color, underline, 2px underline-offset |
| Link (`a` in prose) | hover | Accent color text |
| Heading anchor | default | No underline, inherit color |
| Heading anchor | hover | Accent color text |
| Task list checkbox | default | Unchecked, accent-color set |
| Task list checkbox | checked | Filled with accent color |

No new hover, focus, or active states are introduced. Existing interaction patterns are preserved.

---

## Acceptance Criteria (ADHD Constraint)

Per project guardrails, every page must pass the ADHD 5-second test after Phase 19:

1. **Single clear hierarchy** -- h1 in Space Grotesk at 39px is immediately scannable as page title
2. **No competing visual elements** -- callout types use muted accent colors (not saturated neon), param tables use subtle left border (not full card containers)
3. **Scannable structure** -- heading scale creates clear visual waypoints for skimming session content
4. **Same or fewer clicks** -- no navigation changes, prose styling is passive

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
