---
phase: 19-prose-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - TOKEN-02
  - CONTENT-01
must_haves:
  truths:
    - "User sees h1 and h2 headings in Space Grotesk typeface, distinct from body text in Inter"
    - "User sees a modular type scale with clear visual hierarchy across h1-h4, body, and small text"
    - "User sees markdown tables, code blocks, lists, and blockquotes styled by the typography plugin with warm dark palette colors"
    - "User sees generic tables with 13px cell text, muted headers, even-row surface stripes"
    - "User sees inline code in mono font with param color and surface background"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Space Grotesk font loading via next/font/google"
      contains: "Space_Grotesk"
    - path: "src/app/globals.css"
      provides: "Typography plugin registration, type scale, prose color overrides"
      contains: "@plugin"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-space-grotesk on html element"
      pattern: "spaceGrotesk\\.variable"
    - from: "src/app/globals.css"
      to: "@tailwindcss/typography"
      via: "@plugin directive"
      pattern: "@plugin.*typography"
---

<objective>
Install @tailwindcss/typography, add Space Grotesk display typeface, implement modular type scale, and override typography plugin defaults with the project's warm dark palette tokens.

Purpose: Establishes the typographic foundation (TOKEN-02) and baseline prose rendering (CONTENT-01) that all markdown content inherits. This is the structural layer -- domain element restyling follows in Plan 02.

Output: Working typography system with Space Grotesk headings, Inter body, modular type scale, and dark-palette prose rendering across all 60+ session and 36+ patch pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-prose-typography/19-CONTEXT.md
@.planning/phases/19-prose-typography/19-RESEARCH.md
@.planning/phases/19-prose-typography/19-UI-SPEC.md
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install typography plugin and add Space Grotesk font</name>
  <files>src/app/layout.tsx, src/app/globals.css, package.json</files>
  <read_first>
    - src/app/layout.tsx (current font setup pattern to replicate)
    - src/app/globals.css (current @import and @theme block)
    - package.json (current dependencies)
  </read_first>
  <action>
1. Install the typography plugin:
   ```
   npm install @tailwindcss/typography@^0.5.19
   ```

2. In `src/app/globals.css`, add plugin registration immediately after the existing `@import "tailwindcss";` line:
   ```css
   @plugin "@tailwindcss/typography";
   ```

3. In `src/app/globals.css` `@theme` block, add the display font variable after the existing `--font-mono` line:
   ```css
   --font-display: var(--font-space-grotesk);
   ```

4. In `src/app/layout.tsx`:
   - Add `Space_Grotesk` to the import from `next/font/google`:
     ```typescript
     import { Inter, JetBrains_Mono, Space_Grotesk } from 'next/font/google';
     ```
   - Add the font configuration after the `jetbrainsMono` const:
     ```typescript
     const spaceGrotesk = Space_Grotesk({
       subsets: ['latin'],
       variable: '--font-space-grotesk',
       display: 'swap',
       weight: ['700'],
     });
     ```
   - Update the html className to include the new variable:
     ```typescript
     <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable} ${spaceGrotesk.variable}`}>
     ```

Per D-01 (Space Grotesk as display typeface), D-02 (Inter body, JetBrains Mono code unchanged), D-06 (adopt @tailwindcss/typography).
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains `import { Inter, JetBrains_Mono, Space_Grotesk } from 'next/font/google'`
    - layout.tsx contains `variable: '--font-space-grotesk'`
    - layout.tsx contains `weight: ['700']`
    - layout.tsx html className includes `spaceGrotesk.variable`
    - globals.css contains `@plugin "@tailwindcss/typography"`
    - globals.css @theme block contains `--font-display: var(--font-space-grotesk)`
    - package.json contains `@tailwindcss/typography`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Space Grotesk loads via next/font/google with only weight 700, typography plugin registered, display font token defined</done>
</task>

<task type="auto">
  <name>Task 2: Implement type scale and prose color overrides</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (after Task 1 modifications -- read current state)
    - .planning/phases/19-prose-typography/19-UI-SPEC.md (type scale and color specs)
    - .planning/phases/19-prose-typography/19-RESEARCH.md (prose variable override pattern)
  </read_first>
  <action>
In `src/app/globals.css`, within the `@layer base` block, replace the existing `.prose h1, .prose h2, .prose h3, .prose h4` rules and the individual `.prose h1`, `.prose h2`, `.prose h3` rules with the following. Keep all other existing `.prose` rules (links, lists, code, pre, table, param-table, callout, task-list, mermaid-placeholder) intact -- those will be updated in Plan 02.

1. Add prose plugin variable overrides at the TOP of the `@layer base` block (before all other `.prose` rules). These override the typography plugin's default colors with the project's dark palette tokens. Do NOT use `prose-invert` class -- set dark values directly since the app has no light mode:

```css
.prose {
  --tw-prose-body: var(--color-text);
  --tw-prose-headings: var(--color-text);
  --tw-prose-lead: var(--color-text);
  --tw-prose-links: var(--color-text);
  --tw-prose-bold: var(--color-text);
  --tw-prose-counters: var(--color-muted);
  --tw-prose-bullets: var(--color-muted);
  --tw-prose-hr: var(--color-muted);
  --tw-prose-quotes: var(--color-text);
  --tw-prose-quote-borders: var(--color-muted);
  --tw-prose-captions: var(--color-muted);
  --tw-prose-code: var(--color-param);
  --tw-prose-pre-code: var(--color-text);
  --tw-prose-pre-bg: var(--color-surface);
  --tw-prose-th-borders: var(--color-muted);
  --tw-prose-td-borders: color-mix(in srgb, var(--color-surface) 80%, var(--color-muted));
  max-width: none;
}
```

2. Replace the existing heading rules with the new type scale per D-03/D-04/D-05 and UI-SPEC:

```css
/* Display headings: Space Grotesk (per D-03) */
.prose :where(h1, h2) {
  font-family: var(--font-display);
  letter-spacing: -0.025em;
  line-height: 1.15;
  font-weight: 700;
  color: var(--color-text);
}

.prose :where(h1) {
  font-size: clamp(1.75rem, 1.2rem + 2vw, 2.4375rem);
}

.prose :where(h2) {
  font-size: clamp(1.5rem, 1.1rem + 1.5vw, 1.9375rem);
  margin-top: 2em;
  margin-bottom: 0.75em;
}

/* Body headings: Inter bold (per D-03) */
.prose :where(h3) {
  font-family: var(--font-sans);
  font-size: 1.5625rem;
  font-weight: 700;
  letter-spacing: -0.015em;
  line-height: 1.3;
  margin-top: 1.5em;
  margin-bottom: 0.5em;
  color: var(--color-text);
}

.prose :where(h4) {
  font-family: var(--font-sans);
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: -0.015em;
  line-height: 1.3;
  margin-top: 1.25em;
  margin-bottom: 0.5em;
  color: var(--color-text);
}
```

3. Update generic table styling to use Small size (13px) per UI-SPEC:

```css
.prose :where(table) {
  width: 100%;
  border-collapse: collapse;
  margin-bottom: 1.5em;
  font-size: 13px;
}

.prose :where(th) {
  text-align: left;
  padding: 8px 12px;
  border-bottom: 1px solid var(--color-muted);
  color: var(--color-muted);
  font-weight: 700;
  font-size: 13px;
}

.prose :where(td) {
  padding: 8px 12px;
  border-bottom: 1px solid color-mix(in srgb, var(--color-surface) 80%, var(--color-muted));
  vertical-align: top;
}

.prose :where(tr:nth-child(even) td) {
  background-color: var(--color-surface);
}
```

4. Keep the existing `.quick-ref-prose` rules UNCHANGED -- they override the new type scale for the compact variant. Ensure they appear AFTER the new heading rules so they win on specificity.

Note: Use `:where()` pseudo-class on the new heading/table selectors to keep specificity at (0,1,0) for the `.prose` class, making them easy to override by `.quick-ref-prose` and domain-specific rules. If `.quick-ref-prose` rules already have higher specificity (they target class + element), this is fine.

Per D-04 (major third 1.25 ratio), D-05 (h1 ~2.4x, h2 ~1.95x, h3 ~1.56x, h4 ~1.25x body).
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--tw-prose-body: var(--color-text)`
    - globals.css contains `--tw-prose-headings: var(--color-text)`
    - globals.css contains `--tw-prose-code: var(--color-param)`
    - globals.css contains `--tw-prose-pre-bg: var(--color-surface)`
    - globals.css contains `max-width: none` inside `.prose` block
    - globals.css contains `font-family: var(--font-display)` for h1/h2
    - globals.css contains `clamp(1.75rem, 1.2rem + 2vw, 2.4375rem)` for h1
    - globals.css contains `clamp(1.5rem, 1.1rem + 1.5vw, 1.9375rem)` for h2
    - globals.css contains `font-size: 1.5625rem` for h3
    - globals.css contains `font-size: 1.25rem` for h4
    - globals.css contains `font-family: var(--font-sans)` for h3/h4
    - globals.css `.quick-ref-prose` rules are unchanged (h1 20px, h2 16px, h3 14px)
    - globals.css table th contains `font-weight: 700`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Type scale with Space Grotesk display / Inter body split implemented, prose plugin color variables mapped to dark palette tokens, responsive clamp() on h1/h2, generic tables at Small size, quick-ref-prose compact variant preserved</done>
</task>

</tasks>

<verification>
- `npm run build` passes (CSS imports, font loading, no build errors)
- `npm run test -- --run` passes (existing component tests still green)
- globals.css contains no references to `prose-invert` (skipped per research recommendation)
- h1/h2 use `--font-display`, h3/h4 use `--font-sans`
</verification>

<success_criteria>
- Typography plugin installed and registered via @plugin directive
- Space Grotesk loaded with weight 700 only, CSS variable on html element
- Modular type scale (39px/31px/25px/20px) implemented with responsive clamp on h1/h2
- All prose plugin color variables overridden with project dark palette tokens
- No prose-invert class used anywhere
- quick-ref-prose compact variant unchanged
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-prose-typography/19-01-SUMMARY.md`
</output>
