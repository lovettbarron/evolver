---
phase: 25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - instruments/octatrack/instrument.json
  - src/content/instruments/octatrack/
  - src/content/sessions/octatrack/
  - src/content/patches/octatrack/
  - ~/song/instruments/octatrack/
  - ~/song/sessions/octatrack/
  - ~/song/patches/octatrack/
  - src/lib/content/__tests__/reader.test.ts
  - src/app/__tests__/midi-capability.test.tsx
  - src/components/__tests__/nav.test.tsx
  - src/components/__tests__/app-shell.test.tsx
  - src/components/__tests__/patch-detail-octatrack.test.tsx
autonomous: true
requirements:
  - D-03
  - D-05
  - D-07
  - D-09
  - D-11
requirements_addressed:
  - D-03 (triple-write bootstrap — 9 directories populated)
  - D-05 (full Cascadia-parity integration — auto-discovery picks up octatrack once bundle dir exists)
  - D-07 (instruments/octatrack/instrument.json created + triple-written)
  - D-09 (capability flags on disk enable midi-page gating + project-state patch-detail rendering)
  - D-11 (no new instrument-name string checks added — relies on capability flag pattern)

must_haves:
  truths:
    - "All 9 octatrack content directories exist (work / bundle / vault × instruments / sessions / patches)"
    - "instruments/octatrack/instrument.json exists and validates against extended InstrumentConfigSchema"
    - "The existing 4 instrument markdown files + 6 session files + patches README exist in all three content locations"
    - "scripts/check-triple-write.sh octatrack exits 0"
    - "npm run validate-content exits 0 (all three content locations' frontmatter passes schema)"
    - "Wave 0 it.todo for reader/midi-capability/nav/app-shell/patch-detail-octatrack are flipped to real passing tests"
    - "Dev-mode visit to /instruments/octatrack renders overview/signal-flow/basic-patch/modules + lists the 6 existing sessions"
  artifacts:
    - path: instruments/octatrack/instrument.json
      provides: "Capability + metadata for the Octatrack MKII"
      contains: '"sampler": true'
    - path: src/content/instruments/octatrack/instrument.json
      provides: "Bundled instrument config for demo mode"
      contains: '"sampler": true'
    - path: src/content/sessions/octatrack/09-machines-pickup-first-loop.md
      provides: "Bundled copy of existing session (example)"
      contains: 'instrument: octatrack'
  key_links:
    - from: src/content/instruments/octatrack/instrument.json
      to: src/lib/content/reader.ts
      via: "discoverInstruments() auto-picks up the directory"
      pattern: "discoverInstruments"
    - from: src/content/sessions/octatrack
      to: "/instruments/octatrack/sessions page"
      via: "listSessions('octatrack', config) + Next.js route"
      pattern: 'instruments/\\[slug\\]/sessions'
    - from: scripts/check-triple-write.sh
      to: "sessions/octatrack == src/content/sessions/octatrack == ~/song/sessions/octatrack"
      via: "diff -r returns empty"
      pattern: "All triple-write pairs match"
---

<objective>
Bootstrap the Octatrack content presence across all three content locations (working tree, bundle, vault). Without this plan, the extended schema + cascade block from Wave 1 have nothing to operate on — the site renders zero Octatrack content in demo mode. This plan is scaffolding, not authoring: it creates 9 missing directories, writes the canonical `instruments/octatrack/instrument.json`, and triple-writes the already-existing 4 instrument markdown files + 6 session drafts + patches README to the bundle (`src/content/*`) and vault (`~/song/*`) trees.

Purpose: Phase 25 site integration is 80% automatic through existing auto-discovery (`discoverInstruments`, `listSessions`, `listPatches`). After this plan lands, every auto-discovery call returns octatrack, nav/home/selector show it, and the 6 existing sessions render. Curriculum is still only 6/31 deep — plans 25-03a/b author the remaining 25.

Output: 9 populated directories, canonical + triple-written `instrument.json`, 5 Wave 0 test todos flipped to real assertions, `check-triple-write.sh` now exits 0, `validate-content` all green, dev-mode Octatrack a first-class instrument.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-*/25-CONTEXT.md
@.planning/phases/25-*/25-RESEARCH.md
@.planning/phases/25-*/25-00-PLAN.md
@.planning/phases/25-*/25-01-PLAN.md

<interfaces>
Existing working-tree content to propagate:
- `instruments/octatrack/overview.md`, `signal-flow.md`, `basic-patch.md`, `modules.md`
- `sessions/octatrack/{01,09,25,28,29,31}-*.md` (6 files)
- `patches/octatrack/README.md`

Target instrument.json (matches Wave 0 fixture exactly):
```json
{
  "display_name": "Octatrack MKII",
  "tagline": "Performance sampler, live looper, and MIDI brain",
  "manufacturer": "Elektron",
  "sysex": false,
  "patch_memory": false,
  "sampler": true,
  "sequencer": true,
  "midi_sequencer": true,
  "reference_pdfs": []
}
```

From src/lib/content/reader.ts (consumers — unchanged):
- `discoverInstruments()` reads `./src/content/instruments/` in demo mode or `./instruments/` in local mode
- `listSessions(slug, config)` reads `./{src/content,}/sessions/<slug>/*.md`
- `listPatches(slug, config)` reads `./{src/content,}/patches/<slug>/*.md`

No reader/auto-discovery code change required — they work for any slug whose directories exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Write instruments/octatrack/instrument.json + bootstrap 9 triple-write directories with existing content</name>
  <read_first>
    - instruments/octatrack/ (confirm 4 existing markdown files)
    - sessions/octatrack/ (confirm 6 existing session files)
    - patches/octatrack/ (confirm README.md exists)
    - src/content/instruments/cascadia/instrument.json (shape reference)
    - src/lib/content/__tests__/__fixtures__/instruments/octatrack/instrument.json (Wave 0 fixture — same content as production target)
    - evolver.config.json (confirm vaultPath = `~/song`)
    - .planning/phases/25-*/25-RESEARCH.md §"Recommended Directory Structure" + §"Pitfall 1"
  </read_first>
  <action>
    Step 1 — Write `instruments/octatrack/instrument.json` with EXACT content (matches Wave 0 fixture byte-for-byte):

    ```json
    {
      "display_name": "Octatrack MKII",
      "tagline": "Performance sampler, live looper, and MIDI brain",
      "manufacturer": "Elektron",
      "sysex": false,
      "patch_memory": false,
      "sampler": true,
      "sequencer": true,
      "midi_sequencer": true,
      "reference_pdfs": []
    }
    ```

    This is the CANONICAL file — triple-write copies derive from it.

    Step 2 — Create 6 missing directories:
    ```bash
    mkdir -p src/content/instruments/octatrack
    mkdir -p src/content/sessions/octatrack
    mkdir -p src/content/patches/octatrack
    mkdir -p ~/song/instruments/octatrack
    mkdir -p ~/song/sessions/octatrack
    mkdir -p ~/song/patches/octatrack
    ```

    Step 3 — Triple-write all 5 instrument-level files (instrument.json + 4 markdown):
    ```bash
    for f in instrument.json overview.md signal-flow.md basic-patch.md modules.md; do
      cp "instruments/octatrack/$f" "src/content/instruments/octatrack/$f"
      cp "instruments/octatrack/$f" "$HOME/song/instruments/octatrack/$f"
    done
    ```

    Step 4 — Triple-write all 6 existing session files:
    ```bash
    for f in sessions/octatrack/*.md; do
      cp "$f" "src/content/sessions/octatrack/"
      cp "$f" "$HOME/song/sessions/octatrack/"
    done
    ```

    Step 5 — Triple-write patches README:
    ```bash
    cp patches/octatrack/README.md src/content/patches/octatrack/README.md
    cp patches/octatrack/README.md ~/song/patches/octatrack/README.md
    ```

    Step 6 — Run the triple-write verifier:
    ```bash
    bash scripts/check-triple-write.sh octatrack
    ```
    This must now exit 0 and print "All triple-write pairs match."

    Step 7 — Run content validation:
    ```bash
    npm run validate-content
    ```
    This must exit 0. All 6 existing sessions + instrument.json + 4 instrument markdown files must pass schema validation.

    Step 8 — Manual dev verification (strongly recommended):
    - Run `npm run dev`
    - Browse to `/` — expect 3 instrument cards (Evolver, Cascadia, Octatrack)
    - Browse to `/instruments/octatrack` — expect overview, signal-flow, basic-patch, modules to render
    - Browse to `/instruments/octatrack/sessions` — expect 6 sessions listed
    - Browse to `/instruments/octatrack/midi` — expect NoSysexPage (not the MIDI SysEx page) because `sysex:false`

    DO NOT rename, edit, or reformat any copied content. This task is pure copy + directory bootstrap. Session file authoring is plans 25-03a/b.
  </action>
  <verify>
    <automated>bash scripts/check-triple-write.sh octatrack; CTW=$?; npm run validate-content 2>&1 | tail -5; VC=$?; ls src/content/instruments/octatrack/ 2>&1 | wc -l; ls src/content/sessions/octatrack/ 2>&1 | wc -l; ls ~/song/sessions/octatrack/ 2>&1 | wc -l; echo "triple-write=$CTW validate-content=$VC"</automated>
  </verify>
  <acceptance_criteria>
    - File `instruments/octatrack/instrument.json` exists and contains `"sampler": true`, `"sequencer": true`, `"midi_sequencer": true`, `"sysex": false`, `"patch_memory": false` (grep all 5)
    - File `src/content/instruments/octatrack/instrument.json` exists with identical content (diff returns empty)
    - File `~/song/instruments/octatrack/instrument.json` exists with identical content
    - `ls src/content/instruments/octatrack/` returns exactly 5 entries: instrument.json, overview.md, signal-flow.md, basic-patch.md, modules.md
    - `ls src/content/sessions/octatrack/` returns exactly 6 `.md` files (matching sessions/octatrack/)
    - `ls ~/song/sessions/octatrack/` returns exactly 6 `.md` files
    - `ls src/content/patches/octatrack/` returns README.md
    - `bash scripts/check-triple-write.sh octatrack` exits with code 0 and stdout contains "All triple-write pairs match"
    - `npm run validate-content` exits with code 0
    - `diff -r sessions/octatrack src/content/sessions/octatrack` produces no output
    - `diff -r sessions/octatrack ~/song/sessions/octatrack` produces no output
  </acceptance_criteria>
  <done>
    9 directories populated; canonical instrument.json lives at `instruments/octatrack/`; triple-write verifier passes; validate-content passes; dev-mode site shows 3 instrument cards.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Flip Wave 0 it.todo stubs to real tests (reader / midi-capability / nav / app-shell / patch-detail-octatrack)</name>
  <read_first>
    - src/lib/content/__tests__/reader.test.ts (find the Wave 0 `it.todo('discoverInstruments returns octatrack...')` line)
    - src/app/__tests__/midi-capability.test.tsx (find Wave 0 `it.todo('renders NoSysexPage for octatrack...')`)
    - src/components/__tests__/nav.test.tsx (find Wave 0 `it.todo('hides MIDI link on nav when instrumentConfig.sysex is false for octatrack')`)
    - src/components/__tests__/app-shell.test.tsx (find Wave 0 `it.todo('applies data-instrument="octatrack"...')`)
    - src/components/__tests__/patch-detail-octatrack.test.tsx (Wave 0 shell — has todo entries to flip)
    - src/lib/content/reader.ts (understand discoverInstruments signature + return shape)
    - src/components/nav.tsx (how sysex is read + MIDI link rendered)
    - src/components/app-shell.tsx (how data-instrument is set)
    - src/components/patch-detail.tsx (how octatrack branch renders — specifically checking it does NOT render cable-routing for octatrack)
    - src/app/instruments/[slug]/midi/page.tsx (how NoSysexPage is chosen)
    - src/content/instruments/octatrack/instrument.json (now exists from Task 2.1 — tests can assert against it)
  </read_first>
  <behavior>
    - reader.test.ts: discoverInstruments returns an array containing `'octatrack'` (alongside existing evolver/cascadia)
    - midi-capability.test.tsx: when octatrack's instrumentConfig is loaded, the MIDI route renders NoSysexPage (not the MIDI SysEx controls)
    - nav.test.tsx: octatrack nav context hides the MIDI link (because sysex:false)
    - app-shell.test.tsx: AppShell sets `data-instrument="octatrack"` when the current route is under `/instruments/octatrack/...`
    - patch-detail-octatrack.test.tsx: Octatrack patch renders with OctatrackPanel below body and no cable-routing section
  </behavior>
  <action>
    For each of the 5 test files below, replace the Wave 0 `it.todo(...)` entries with REAL passing tests. Adapt imports / harness / mocks to match the existing style in each file (read the file first, don't invent new patterns).

    Step 1 — `src/lib/content/__tests__/reader.test.ts`:
    Replace `it.todo('discoverInstruments returns octatrack once src/content/instruments/octatrack exists');` with:
    ```typescript
    it('discoverInstruments returns octatrack alongside evolver and cascadia', async () => {
      // Use the same config/fixture pattern the surrounding test uses.
      // Replace {testConfig} with whatever config object the existing cascadia test uses.
      const result = await discoverInstruments(testConfig);
      expect(result).toContain('octatrack');
      expect(result).toContain('cascadia');
      expect(result).toContain('evolver');
    });
    ```
    Adapt `testConfig` and the `discoverInstruments` import signature to match what the existing test uses. If tests are unit-level (reading real `src/content/`), use that pattern. If they use a mocked fs or fixture config, use that pattern.

    Step 2 — `src/app/__tests__/midi-capability.test.tsx`:
    Replace `it.todo('renders NoSysexPage for octatrack (sysex:false)');` with a real test that loads the octatrack instrumentConfig (via the same mechanism the existing Cascadia case uses) and asserts the MIDI page renders the "no sysex" branch. Use the exact pattern from the existing Cascadia test in the same file — do not invent a new mock harness.

    Step 3 — `src/components/__tests__/nav.test.tsx`:
    Replace `it.todo('hides MIDI link on nav when instrumentConfig.sysex is false for octatrack');` with a real test that renders Nav with an octatrack instrumentConfig (sysex:false) and asserts the MIDI link is NOT in the document. Use the existing Cascadia parallel test as the template.

    Step 4 — `src/components/__tests__/app-shell.test.tsx`:
    Replace `it.todo('applies data-instrument="octatrack" to app shell on /instruments/octatrack/... routes');` with a real test that mocks `usePathname` to return `/instruments/octatrack/sessions/01-...` and asserts the rendered AppShell wrapper has the attribute `data-instrument="octatrack"`. Follow the existing Cascadia test.

    Step 5 — `src/components/__tests__/patch-detail-octatrack.test.tsx`:
    Flip the todo entries to real tests. The harness:
    ```typescript
    const octatrackPatch = {
      name: 'Test Octatrack Patch',
      type: 'texture' as const,
      session_origin: null,
      description: 'Project-state patch for testing',
      tags: ['octatrack', 'test'],
      instrument: 'octatrack',
      created: '2026-04-17',
      // Deliberately no cable_routing, no knob_settings
    };
    // Render <PatchDetail patch={octatrackPatch} html="<p>Sample list + tracks</p>" />
    ```
    Assertions:
    - `expect(container.querySelector('svg'))` is non-null (the OctatrackPanel renders)
    - `expect(screen.queryByText(/cable routing/i))` is null
    - `expect(screen.queryByText(/knob settings/i))` is null (no parameter-dump format)
    - `expect(container.innerHTML)` contains "Sample list + tracks" (markdown body renders)

    If the PatchDetail component requires additional props (e.g., a prev/next pair, a session lookup, an instrumentConfig), wire them with the minimum fixtures needed — do not stub out components it renders.

    DO NOT add new production code. Tests only. If a test cannot be made to pass without a production code change, raise it in the plan SUMMARY as a blocker for Wave 2 or defer to Wave 5 (final verification wave) and keep the `.todo`.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/content/__tests__/reader.test.ts src/app/__tests__/midi-capability.test.tsx src/components/__tests__/nav.test.tsx src/components/__tests__/app-shell.test.tsx src/components/__tests__/patch-detail-octatrack.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 test files contains at least one new passing `it(...)` test referencing octatrack (grep `it\\(.*octatrack`)
    - No `.todo` entries remain that reference octatrack in these 5 files (grep `it\\.todo.*octatrack` returns empty across these files)
    - `npm test -- --run` for the 5 files collectively exits 0
    - `npm test -- --run` full suite exits 0 (no regressions)
    - If any one of the 5 tests cannot be made real (genuine blocker), that case is explicitly documented in the task completion notes with the specific dependency that is missing — but the remaining tests still flip green
  </acceptance_criteria>
  <done>
    5 Wave 0 todos flipped to real passing assertions (or documented blockers for Wave 5); full test suite green; octatrack is now regression-protected at every integration point.
  </done>
</task>

</tasks>

<verification>
Wave 2 exit gate:
1. `bash scripts/check-triple-write.sh octatrack` exits 0 — "All triple-write pairs match."
2. `npm run validate-content` exits 0 — instrument.json + overview/signal-flow/basic-patch/modules + 6 existing sessions all pass schema
3. `npm test -- --run` exits 0 — all octatrack integration tests flipped from todo to real and passing
4. Dev-mode verification (manual spot-check):
   - `/` shows 3 instrument cards
   - `/instruments/octatrack` renders all 4 overview-level markdown files
   - `/instruments/octatrack/sessions` lists 6 sessions
   - `/instruments/octatrack/midi` renders NoSysexPage
   - `/instruments/octatrack/patches` renders (README.md content, since no patches yet)
   - Global search returns Octatrack Session 01 when searching "foundations"
5. `git status --porcelain src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts` is empty (D-22 panel still frozen)
</verification>

<success_criteria>
- 9 octatrack content directories exist and are populated (instruments/sessions/patches × work/bundle/vault)
- Canonical `instrument.json` exists at `instruments/octatrack/` and is triple-written to bundle + vault
- 5 Wave 0 it.todo tests flipped to real passing assertions (reader, midi-capability, nav, app-shell, patch-detail-octatrack)
- check-triple-write, validate-content, full test suite all green
- Octatrack appears on home page as 3rd instrument card, in nav instrument selector, in global search results
- MIDI route renders NoSysexPage (capability-gated via sysex:false)
- Existing 6 session drafts accessible at `/instruments/octatrack/sessions/XX-...` in dev mode
- No panel source file modifications (D-22)
- No new instrument-name string checks introduced (D-11 — all routing continues via capability flags and slug-based discovery)
</success_criteria>

<output>
After completion, create `.planning/phases/25-*/25-02-SUMMARY.md` recording: exact file counts in each of the 9 directories, list of Wave 0 todos flipped (and any that remained todo with the reason), output of `check-triple-write.sh` and `validate-content`, any manual dev verifications performed, and confirmation that the D-22 panel guardrail held.
</output>
