---
phase: 25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping
plan: 04
type: execute
wave: 4
depends_on:
  - 25-03a
  - 25-03b
files_modified:
  - patches/octatrack/basic-project.md
  - patches/octatrack/scene-morphing-performance.md
  - patches/octatrack/parts-song-sections.md
  - patches/octatrack/live-loop-layers.md
  - patches/octatrack/full-arrangement.md
  - src/content/patches/octatrack/
  - ~/song/patches/octatrack/
autonomous: true
requirements:
  - D-03
  - D-16
  - D-17
  - D-18
  - D-19
requirements_addressed:
  - D-03 (triple-write each patch)
  - D-16 (4 demo project patches, one per focus module 7-10)
  - D-17 (project-state format per patches/octatrack/README.md — sample list, track config, pattern map, scene notes, performance tips; NO parameter-dump format)
  - D-18 (basic-project.md reference starting state)
  - D-19 (bundled to src/content/patches/octatrack/ for demo mode)

must_haves:
  truths:
    - "5 patch markdown files exist in patches/octatrack/ (basic-project + 4 focus-module demo patches)"
    - "Each patch is triple-written to src/content/patches/octatrack/ and ~/song/patches/octatrack/"
    - "Each patch passes PatchSchema validation via npm run validate-content"
    - "Each patch uses the project-state format (tables for sample list, track config, pattern map, scenes) with NO cable_routing or knob_settings frontmatter fields"
    - "session_origin references correspond to actual session_numbers: scene-morphing → 19-21, parts → 22-24, live-loop → 26-28, full-arrangement → 29-31"
    - "scripts/check-triple-write.sh octatrack exits 0"
    - "Patch detail pages render correctly: markdown body + OctatrackPanel, no cable-routing/knob-settings UI"
  artifacts:
    - path: patches/octatrack/basic-project.md
      provides: "D-18 reference starting state"
      contains: "session_origin"
    - path: patches/octatrack/scene-morphing-performance.md
      provides: "Module 7 demo patch (scene/crossfader performance)"
      contains: 'instrument: octatrack'
    - path: patches/octatrack/parts-song-sections.md
      provides: "Module 8 demo patch (Parts as verse/chorus)"
      contains: 'instrument: octatrack'
    - path: patches/octatrack/live-loop-layers.md
      provides: "Module 9 demo patch (Pickup machines + track recorders)"
      contains: 'instrument: octatrack'
    - path: patches/octatrack/full-arrangement.md
      provides: "Module 10 demo patch (full song arrangement)"
      contains: 'instrument: octatrack'
  key_links:
    - from: patches/octatrack
      to: src/content/patches/octatrack
      via: "triple-write + scripts/bundle-content.ts"
      pattern: "bundle"
    - from: "patch markdown bodies"
      to: "src/components/patch-detail.tsx render flow"
      via: "dangerouslySetInnerHTML + OctatrackPanel branch"
      pattern: "OctatrackPanel|octatrack"
---

<objective>
Ship 5 Octatrack demo patches per D-16 and D-18: one reference starting state (`basic-project.md`) and one performance patch per focus module (7-10). Each patch uses the project-state format — sample slot list, track config table, pattern map, scene notes, performance tips — from `patches/octatrack/README.md`. No parameter-dump, no cable routing, no knob settings. Each patch is triple-written (D-03) and bundled for demo mode (D-19). After this plan lands, `/instruments/octatrack/patches/` lists 5 patches, and clicking any one renders the markdown body plus the OctatrackPanel (no cable diagram, no knob settings table — capability-gated off by `patch_memory: false`).

Purpose: Patches are the tangible artifacts of focus-module mastery. Demo mode needs them to feel complete; the learner's progress page references them. Wave 5 (synthetic journey + final UAT) depends on this patch set existing. Per D-04's deferred list, we ship exactly 5 — extended library is out of scope.

Output: 5 markdown patch files, triple-written, validated, rendering correctly in patch-detail pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-*/25-CONTEXT.md
@.planning/phases/25-*/25-RESEARCH.md
@.planning/phases/25-*/25-00-PLAN.md
@patches/octatrack/README.md
@instruments/octatrack/basic-patch.md
@instruments/octatrack/overview.md

<interfaces>
PatchSchema (src/lib/content/schemas.ts:27-44) — every frontmatter must match:
```typescript
{
  name: string,
  type: 'bass' | 'lead' | 'pad' | 'drum' | 'texture' | 'sequence' | 'fx',
  session_origin: number | null,
  description: string,
  tags: string[],
  instrument: string,           // "octatrack"
  created: string,              // ISO date "YYYY-MM-DD"
  // OMIT cable_routing (optional) — Octatrack patches use markdown body instead
  // OMIT knob_settings (optional) — same reason
}
```

Project-state body format (from patches/octatrack/README.md — follow verbatim):
```markdown
# [Patch Name]

**Session**: XX | **Bank**: X | **Type**: composition/performance/exercise

## Description
One sentence: what this sounds like and when you'd use it.

## Sample List
| Slot | Sample | Type | Notes |
|------|--------|------|-------|
| Flex 01 | kick-808.wav | Drums | Main kick |

## Track Configuration
| Track | Machine | Sample | FX1 | FX2 |
|-------|---------|--------|-----|-----|
| T1 | Flex | kick-808 | Compressor | None |

## Pattern Map
| Pattern | Part | Description |
|---------|------|-------------|
| A01 | 1 | Intro — sparse |

## Scene Notes
Scene B: [what it does]

## Performance Tips
How to perform this live (crossfader gestures, mute sequences, etc.)

## Tags
#octatrack #[type]
```

Example patch frontmatter (from RESEARCH.md §"Example 6: Project-state patch"):
```markdown
(example frontmatter — rendered as YAML front-matter in the actual patch)
name: "Live Loop Layers"
type: texture
session_origin: 28
description: "A live-sampling performance project using Pickup machines on Tracks 7-8 and track recorders on Tracks 2-4. Demonstrates Master/Slave sync, overdub layering, and live resampling."
tags: [octatrack, live-looping, pickup-machines, resampling, performance]
instrument: octatrack
created: "2026-04-17"
(example frontmatter — rendered as YAML front-matter in the actual patch)
```

Session origin guidance (D-16):
- scene-morphing-performance → Module 7 → session_origin: 19 (or 20/21 — pick the session whose exercise most directly produces the patch state)
- parts-song-sections → Module 8 → session_origin: 23 (pattern variations produce the verse/chorus split patch)
- live-loop-layers → Module 9 → session_origin: 26 (pickup mastery; OR 28 improvisation)
- full-arrangement → Module 10 → session_origin: 29 (arranger); OR 31 (composition workflow)
- basic-project → session_origin: 1 or null (starting state exists from day one; modules.md session 01 establishes it)

Per-patch spec summary (to guide authoring):

1. basic-project.md — reference starting state spec'd in patches/octatrack/README.md + instruments/octatrack/basic-patch.md
   - Track 1: Flex machine with a simple drum loop
   - Tracks 2-8: empty
   - 1 scene saved (default)
   - 1 pattern saved (A01 — empty/sparse)
   - type: drum (simplest fit for PatchSchema) or texture

2. scene-morphing-performance.md — Module 7 performance state
   - All 8 tracks configured with samples + FX
   - Scene A (clean) and Scene B (destroyed) assigned
   - One pattern (A01) with a groove that uses both scenes dynamically
   - type: texture or sequence

3. parts-song-sections.md — Module 8 song-structure state
   - 2 Parts: verse sound and chorus sound
   - 4+ patterns: A01 (intro, Part 1) / A02 (verse, Part 1) / A03 (chorus, Part 2) / A04 (outro, Part 2)
   - type: sequence or composition-adjacent (pick valid enum)

4. live-loop-layers.md — Module 9 live-looping state
   - Input A/B for live source
   - Track 8 Master Pickup, Track 7 Slave Pickup
   - Tracks 2-4 Flex pointed at recorder buffers (captured from Input A/B)
   - Recorder Trigs arm capture every loop
   - type: texture
   - (Use RESEARCH.md §"Example 6" as-is — it's basically this patch; copy its body verbatim as the starting point, adjust dates and session_origin)

5. full-arrangement.md — Module 10 arranger state
   - 3+ Parts to cover verse / chorus / break / outro
   - 6+ patterns chained in the Arranger
   - Intro (A01 x2) → Verse (A02 x4) → Chorus (A05 x4) → Verse (A03 x4) → Chorus (A06 x4) → Outro (A04 x2)
   - type: sequence (most appropriate enum fit for an arrangement)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Author basic-project.md + scene-morphing-performance.md + triple-write</name>
  <read_first>
    - patches/octatrack/README.md (format spec — authoritative body structure)
    - instruments/octatrack/basic-patch.md (source material for basic-project.md — describes the starting state)
    - sessions/octatrack/19-scenes-fundamentals-assign-fade-mute.md (from 25-03b — for scene-morphing content reference)
    - sessions/octatrack/20-scenes-xvol-xlev-mix.md (from 25-03b — additional scene context)
    - sessions/octatrack/21-scenes-stacking-progressions.md (from 25-03b)
    - src/lib/content/schemas.ts lines 27-44 (PatchSchema — confirm type enum; note: NO octatrack-specific type, use 'drum' or 'texture' or 'sequence')
    - src/components/patch-detail.tsx (verify octatrack branch — what it renders for octatrack patches)
  </read_first>
  <action>
    Step 1 — Author patches/octatrack/basic-project.md. Frontmatter:
    ```yaml
    ---
    name: "Basic Project"
    type: drum
    session_origin: 1
    description: "The reference starting state for every Octatrack session. Track 1 loaded with a simple drum loop via Flex machine; Tracks 2-8 empty; one default scene saved; one empty pattern. Your lab bench."
    tags: [octatrack, basic-project, starting-state, reference, foundations]
    instrument: octatrack
    created: "2026-04-17"
    ---
    ```
    Body: Follow the README.md format verbatim. Pull content directly from `instruments/octatrack/basic-patch.md` — it already describes the state; adapt to the patch-format tables. Expect ~50-80 lines of markdown body. Include:
    - `# Basic Project`
    - `**Session**: 01 | **Bank**: A | **Type**: reference`
    - `## Description` (one sentence)
    - `## Sample List` table (Flex 01 = basic-kick.wav or equivalent)
    - `## Track Configuration` table (T1=Flex, T2-T8=— (empty))
    - `## Pattern Map` table (A01 = empty, Part 1)
    - `## Scene Notes` (Scene A = default, Scene B = unassigned)
    - `## Performance Tips` (minimal — "load your own sample on T1, start exploring")
    - `## Tags` (`#octatrack #basic-project #reference`)

    Step 2 — Author patches/octatrack/scene-morphing-performance.md. Frontmatter:
    ```yaml
    ---
    name: "Scene Morphing Performance"
    type: texture
    session_origin: 21
    description: "A Module 7 performance project demonstrating scene assignment, crossfader morphing, and scene stacking across 8 tracks. Scene A holds a clean mix; Scene B introduces filter sweeps, delay feedback, and lo-fi degradation. Moving the crossfader transforms the entire mix in real time."
    tags: [octatrack, scenes, crossfader, performance, scene-morphing, live-performance]
    instrument: octatrack
    created: "2026-04-17"
    ---
    ```
    Body: Follow the README format. Realistic project state with 8 tracks fully configured:
    - Sample List table: 6-8 samples (kick, snare, hi-hat, bass, pad, lead, fx, texture)
    - Track Configuration table: T1-T8 all configured (Flex or Static machines, FX1 and FX2 assignments)
    - Pattern Map table: 1-2 patterns — one main groove (A01, Part 1)
    - Scene Notes: clear differentiation between Scene A (clean) and Scene B (destroyed) — list which params differ per track (filter cutoff, delay feedback, lo-fi intensity, etc.)
    - Performance Tips: crossfader gestures (slow morph = tension build, fast morph = sudden drop), mute sequences, scene-stacking approach

    Step 3 — Triple-write both files:
    ```bash
    cp patches/octatrack/basic-project.md src/content/patches/octatrack/basic-project.md
    cp patches/octatrack/basic-project.md ~/song/patches/octatrack/basic-project.md
    cp patches/octatrack/scene-morphing-performance.md src/content/patches/octatrack/scene-morphing-performance.md
    cp patches/octatrack/scene-morphing-performance.md ~/song/patches/octatrack/scene-morphing-performance.md
    ```

    Step 4 — Validate:
    ```bash
    npm run validate-content
    bash scripts/check-triple-write.sh octatrack
    ```

    Step 5 — Dev spot-check (recommended, not gated): Navigate to `/instruments/octatrack/patches` → expect 2 patches listed. Click one → expect the markdown tables to render with no cable-routing diagram (there shouldn't be any — `cable_routing` was deliberately omitted from frontmatter).
  </action>
  <verify>
    <automated>ls patches/octatrack/*.md 2>&1; echo '---'; npm run validate-content 2>&1 | tail -5; VC=$?; bash scripts/check-triple-write.sh octatrack 2>&1 | tail -5; TW=$?; echo "validate=$VC triple-write=$TW"; grep -c "session_origin" patches/octatrack/basic-project.md patches/octatrack/scene-morphing-performance.md</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: patches/octatrack/basic-project.md AND patches/octatrack/scene-morphing-performance.md
    - Both files have frontmatter containing `instrument: octatrack`, `name:`, `type:` (one of the valid PatchSchema enum values), `session_origin:` (integer), `tags: [...]`, `created: "YYYY-MM-DD"`
    - Both files use the project-state body format: `## Sample List`, `## Track Configuration`, `## Pattern Map`, `## Scene Notes`, `## Performance Tips` sections present (grep each header)
    - Neither file contains `cable_routing:` or `knob_settings:` in frontmatter (D-17: no parameter-dump format)
    - Files triple-written: `diff patches/octatrack/basic-project.md src/content/patches/octatrack/basic-project.md` returns empty
    - Files triple-written to vault: same diff against `~/song/patches/octatrack/...` returns empty
    - `npm run validate-content` exits 0
    - `bash scripts/check-triple-write.sh octatrack` exits 0
    - session_origin in basic-project.md is 1 (or null — matches modules.md session 01 scope)
    - session_origin in scene-morphing-performance.md is in {19, 20, 21} (Module 7 range)
  </acceptance_criteria>
  <done>
    basic-project and scene-morphing patches exist, triple-written, validate-content green, project-state format confirmed (no cable_routing / knob_settings).
  </done>
</task>

<task type="auto">
  <name>Task 4.2: Author parts-song-sections.md + live-loop-layers.md + full-arrangement.md + triple-write + final verification</name>
  <read_first>
    - patches/octatrack/README.md
    - sessions/octatrack/22-parts-save-reload-copy.md (from 25-03b — parts mental model reference)
    - sessions/octatrack/23-parts-pattern-variations.md (from 25-03b — pattern variations reference)
    - sessions/octatrack/24-parts-multiple-parts-transitions.md (from 25-03b)
    - sessions/octatrack/26-live-sampling-pickup-mastery.md (from 25-03b — live-looping reference)
    - sessions/octatrack/28-live-sampling-improvisation-workflow.md (existing — more live-looping context)
    - sessions/octatrack/29-songwriting-arranger.md (existing — arranger workflow reference)
    - sessions/octatrack/31-songwriting-composition-workflow.md (existing — composition reference for full-arrangement)
    - .planning/phases/25-*/25-RESEARCH.md §"Example 6: Project-state patch" (template for live-loop-layers)
    - previous-task output (basic-project.md, scene-morphing-performance.md) for style continuity
  </read_first>
  <action>
    Step 1 — Author patches/octatrack/parts-song-sections.md. Frontmatter:
    ```yaml
    ---
    name: "Parts — Song Sections"
    type: sequence
    session_origin: 24
    description: "A Module 8 arrangement project using two Parts as verse and chorus sound palettes. Patterns A01-A04 use Part 1 (clean verse sound); A05-A08 use Part 2 (aggressive chorus sound). Transitions between Parts via scene gestures. A complete song in one bank."
    tags: [octatrack, parts, song-structure, verse-chorus, pattern-workflow, arrangement]
    instrument: octatrack
    created: "2026-04-17"
    ---
    ```
    Body: Project-state format. Emphasize the Parts list — add a subsection after Pattern Map:
    ```markdown
    ## Parts
    | Part | Sound Palette | Used in Patterns |
    |------|--------------|-------------------|
    | 1 | Clean verse (subtle reverb, low drive) | A01, A02, A03, A04 |
    | 2 | Distorted chorus (aggressive filter, overdrive) | A05, A06, A07, A08 |
    ```
    Pattern Map table should show all 8 patterns with the Part column populated. Realistic sample list (6-8 samples).

    Step 2 — Author patches/octatrack/live-loop-layers.md. Use RESEARCH §"Example 6" as the starting body (it was crafted for this patch). Frontmatter:
    ```yaml
    ---
    name: "Live Loop Layers"
    type: texture
    session_origin: 26
    description: "A Module 9 live-sampling performance project using Pickup machines on Tracks 7-8 and track recorders on Tracks 2-4. Demonstrates Master/Slave sync, overdub layering, and live resampling from external input."
    tags: [octatrack, live-looping, pickup-machines, resampling, performance, track-recorders]
    instrument: octatrack
    created: "2026-04-17"
    ---
    ```
    Body: Port RESEARCH §"Example 6" body verbatim (starts at `# Live Loop Layers` and ends at `## Tags`). Adjust any date/session references to point at session 26 (or 28).

    Step 3 — Author patches/octatrack/full-arrangement.md. Frontmatter:
    ```yaml
    ---
    name: "Full Arrangement"
    type: sequence
    session_origin: 29
    description: "A Module 10 complete song arrangement: intro → verse → chorus → verse → chorus → outro chained in the Arranger across 3 Parts and 6+ patterns. Uses scene-driven transitions between sections. The graduation project for the Octatrack curriculum."
    tags: [octatrack, arranger, arrangement, song-structure, complete-song, integration, composition]
    instrument: octatrack
    created: "2026-04-17"
    ---
    ```
    Body: Project-state format + an Arranger Rows section:
    ```markdown
    ## Arranger Rows
    | Row | Pattern | Part | Repeats | Length | Tempo |
    |-----|---------|------|---------|--------|-------|
    | 1 | A01 | 1 | 2 | 16 steps | 120 |
    | 2 | A02 | 1 | 4 | 16 steps | 120 |
    | 3 | A05 | 2 | 4 | 16 steps | 120 |
    | 4 | A03 | 1 | 4 | 16 steps | 120 |
    | 5 | A06 | 2 | 4 | 16 steps | 120 |
    | 6 | A04 | 1 | 2 | 16 steps | 120 |
    ```
    Realistic sample list (~8 samples); track config showing drums + bass + leads + pads across 8 tracks.

    Step 4 — Triple-write all 3 new patches:
    ```bash
    for f in parts-song-sections.md live-loop-layers.md full-arrangement.md; do
      cp "patches/octatrack/$f" "src/content/patches/octatrack/$f"
      cp "patches/octatrack/$f" "$HOME/song/patches/octatrack/$f"
    done
    ```

    Step 5 — Final validation:
    ```bash
    npm run validate-content
    bash scripts/check-triple-write.sh octatrack
    npm test -- --run
    ls patches/octatrack/*.md
    ls src/content/patches/octatrack/*.md
    ls ~/song/patches/octatrack/*.md
    ```

    Step 6 — Dev spot-check (recommended): visit `/instruments/octatrack/patches` — expect 5 patches + README excluded from the listing (README.md is not a patch). Visit each patch detail page — expect markdown tables render; OctatrackPanel appears below; no cable-routing diagram, no knob-settings table.
  </action>
  <verify>
    <automated>ls patches/octatrack/*.md 2>&1; echo '---bundle---'; ls src/content/patches/octatrack/*.md 2>&1; echo '---vault---'; ls ~/song/patches/octatrack/*.md 2>&1; echo '---validate---'; npm run validate-content 2>&1 | tail -5; VC=$?; bash scripts/check-triple-write.sh octatrack 2>&1 | tail -5; TW=$?; npm test -- --run 2>&1 | tail -15; FS=$?; echo "validate=$VC triple-write=$TW full-suite=$FS"</automated>
  </verify>
  <acceptance_criteria>
    - 3 new files exist: patches/octatrack/parts-song-sections.md, live-loop-layers.md, full-arrangement.md
    - Combined with Task 4.1 outputs: total 5 patches + README.md in patches/octatrack/
    - Each of the 5 patch files has valid frontmatter per PatchSchema (no cable_routing, no knob_settings)
    - Each session_origin points at a session in the target module: scene-morphing → [19..21], parts-song-sections → [22..24], live-loop-layers → [25..28], full-arrangement → [29..31]
    - Each file body contains `## Sample List`, `## Track Configuration`, `## Pattern Map`, `## Scene Notes` or equivalent
    - parts-song-sections.md contains `## Parts` section (grep)
    - full-arrangement.md contains `## Arranger Rows` or `## Arranger` section (grep)
    - All 5 patches triple-written: `ls src/content/patches/octatrack/*.md | wc -l` returns 6 (5 patches + README.md)
    - Vault: `ls ~/song/patches/octatrack/*.md | wc -l` returns 6
    - `npm run validate-content` exits 0
    - `bash scripts/check-triple-write.sh octatrack` exits 0
    - `npm test -- --run` exits 0
    - `git diff --stat src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts` is empty (D-22)
  </acceptance_criteria>
  <done>
    All 5 patches authored, triple-written, validate-content green, check-triple-write green, full test suite green.
  </done>
</task>

</tasks>

<verification>
Wave 4 exit gate:
1. `ls patches/octatrack/*.md | grep -v README` returns 5 patch files
2. `ls src/content/patches/octatrack/*.md` returns 6 entries (5 patches + README.md)
3. `ls ~/song/patches/octatrack/*.md` returns 6 entries
4. `npm run validate-content` exits 0 — 5 patches + 31 sessions all pass schema
5. `bash scripts/check-triple-write.sh octatrack` exits 0
6. `npm test -- --run` exits 0
7. Manual dev verification (recommended):
   - `/instruments/octatrack/patches` lists 5 patches with correct names
   - Opening any patch renders the markdown body (tables included) and the OctatrackPanel below
   - No cable-routing diagram appears on any octatrack patch
   - No knob-settings UI appears on any octatrack patch
8. `git diff --stat src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts` is empty (D-22)
</verification>

<success_criteria>
- 5 patches authored: basic-project + 4 focus-module demos
- Project-state format used: tables for sample list, track config, pattern map, scene notes, performance tips
- PatchSchema-valid frontmatter with no cable_routing / knob_settings (they're omitted so patch-detail's octatrack branch renders correctly per D-09)
- session_origin references point at real sessions in the target module range
- Triple-write confirmed across all three content locations
- validate-content, check-triple-write, and full test suite all green
- D-22 panel files untouched
- Patch detail page renders correctly: markdown body + OctatrackPanel, no parameter-dump UI
</success_criteria>

<output>
After completion, create `.planning/phases/25-*/25-04-SUMMARY.md` recording: file names and session_origin of all 5 patches, the type enum value selected for each (basic-project, scene-morphing, parts, live-loop, full-arrangement), any frontmatter fields added beyond the required PatchSchema fields, confirmation that cable_routing / knob_settings were NOT added, and the final counts across the three content locations.
</output>
