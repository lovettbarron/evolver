---
phase: 25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping
plan: 01
type: execute
wave: 1
depends_on:
  - 25-00
files_modified:
  - src/lib/content/schemas.ts
  - src/lib/content/__tests__/schemas.test.ts
  - src/app/globals.css
  - src/app/__tests__/tokens.test.ts
  - scripts/validate-contrast.mjs
autonomous: true
requirements:
  - D-06
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
requirements_addressed:
  - D-08 (capability flags added as optional Zod fields — sampler/sequencer/midi_sequencer)
  - D-09 (flag-based gating pattern preserved — existing `sysex` and `patch_memory` are untouched; flag reads continue to drive capability rendering)
  - D-10 (midi_sequencer flag exists in schema — informational, no UI consumption yet)
  - D-11 (capability gating via flag presence, not instrument-name switching — reinforced by adding new flags instead of new instrument-name branches)
  - D-12 (Elektron orange accent primitive added; OKLCH iterated to pass WCAG AA)
  - D-13 (lighter param derivative added; WCAG AA validated)
  - D-14 (`[data-instrument="octatrack"]` cascade block with accent/param/surface overrides)
  - D-15 (three-layer token architecture preserved — primitive → semantic → cascade)
  - D-06 (panel route already resolves octatrack — verified; regression test added if missing)

must_haves:
  truths:
    - "Extended InstrumentConfigSchema accepts octatrack fixture with sampler/sequencer/midi_sequencer optional fields"
    - "Existing Evolver/Cascadia instrument configs still parse without modification (backward compat)"
    - "globals.css contains Octatrack orange primitives in @theme and a [data-instrument='octatrack'] cascade block"
    - "All Octatrack contrast pairings in validate-contrast.mjs pass WCAG AA 4.5:1"
    - "Panel route resolves /instruments/octatrack/panel → OctatrackPanel (regression-protected)"
    - "Wave 0 it.todo entries for schemas.test.ts and tokens.test.ts are flipped to real passing tests"
  artifacts:
    - path: src/lib/content/schemas.ts
      provides: "Extended InstrumentConfigSchema with sampler/sequencer/midi_sequencer optional fields"
      contains: "sampler: z.boolean().optional()"
    - path: src/app/globals.css
      provides: "Octatrack orange primitives + cascade block"
      contains: 'data-instrument="octatrack"'
    - path: src/app/__tests__/tokens.test.ts
      provides: "Octatrack cascade + primitive assertions"
      contains: 'data-instrument="octatrack"'
    - path: scripts/validate-contrast.mjs
      provides: "Finalized OKLCH values for Octatrack accent/param passing WCAG AA"
      contains: "octatrack-accent"
  key_links:
    - from: src/lib/content/schemas.ts
      to: src/lib/content/__tests__/__fixtures__/instruments/octatrack/instrument.json
      via: "InstrumentConfigSchema.parse"
      pattern: "sampler.*optional"
    - from: src/app/globals.css
      to: scripts/validate-contrast.mjs
      via: "TOKEN_MAP mirrors @theme primitive OKLCH values"
      pattern: 'oklch\\([0-9.]+ [0-9.]+ 4[0-5]\\)'
    - from: src/app/__tests__/tokens.test.ts
      to: src/app/globals.css
      via: "fs.readFileSync + regex match"
      pattern: 'data-instrument="octatrack"'
---

<objective>
Land the Wave 1 foundation for Phase 25: (a) extend the InstrumentConfigSchema with three optional sampler-family capability flags so Wave 2's `instrument.json` will validate, (b) add Elektron-orange primitives and a `[data-instrument="octatrack"]` cascade block to globals.css (surface tinting + accent + param), (c) iterate the exact OKLCH values in both globals.css AND validate-contrast.mjs until every Octatrack pairing passes WCAG AA 4.5:1, and (d) verify the panel route wiring for octatrack resolves to OctatrackPanel (add a regression test if none exists).

Purpose: Every downstream wave (bootstrap, authoring, patches, demo-journey) requires the schema and the color identity to be in place. Wave 0 wrote the tests; this wave makes them green.

Output: Extended schema, new globals.css primitives + cascade block, finalized OKLCH values, real passing test assertions replacing Wave 0 `it.todo`s, panel-route regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping/25-CONTEXT.md
@.planning/phases/25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping/25-RESEARCH.md
@.planning/phases/24-instrument-color-identity/24-01-PLAN.md
@.planning/phases/25-complete-the-octatrack-curriculum-work-so-that-it-is-ready-to-add-to-the-evolver-site-focus-on-song-writing-and-arrangement-and-live-sessions-and-looping/25-00-PLAN.md

<interfaces>
From scripts/validate-contrast.mjs (post-Wave 0 — updated palette):
```javascript
// TOKEN_MAP now includes octatrack-bg, octatrack-surface, octatrack-accent, octatrack-param, etc.
// PAIRINGS now includes octatrack-accent on each surface + text/muted on octatrack-bg/surface.
// Starter values: oklch(0.72 0.16 40) accent, oklch(0.80 0.12 42) param.
// Wave 1 iterates these until all pairings PASS.
```

From src/app/globals.css (Phase 24 cascade pattern — target shape for octatrack):
```css
[data-instrument="evolver"] {
  --color-accent: var(--color-blue-500);
  --color-param: var(--color-blue-400);
  --color-bg: oklch(0.12 0.02 245);
  --color-sunken: oklch(0.10 0.02 245);
  --color-surface: oklch(0.16 0.02 245);
  --color-surface-raised: oklch(0.20 0.02 245);
  --color-overlay: oklch(0.24 0.02 245);
  --color-border: oklch(0.25 0.02 245);
  --color-border-subtle: oklch(0.20 0.015 245);
}
/* [data-instrument="cascadia"] follows same shape with hue 250 chroma 0.01 */
```

From src/lib/content/schemas.ts (current):
```typescript
export const InstrumentConfigSchema = z.object({
  display_name: z.string(),
  tagline: z.string(),
  manufacturer: z.string(),
  sysex: z.boolean(),
  patch_memory: z.boolean(),
  reference_pdfs: z.array(z.object({ label: z.string(), file: z.string() })),
}).passthrough();
```

From src/app/instruments/[slug]/panel/page.tsx (already resolves octatrack):
```typescript
const PANEL_CONFIG: Record<string, {...}> = {
  evolver: {...},
  cascadia: {...},
  octatrack: { title: 'Octatrack Panel', description: '...', maxWidth: 'max-w-[1400px]' },
};
```

From src/app/instruments/[slug]/panel/standalone-panel-client.tsx (already resolves octatrack):
```typescript
if (instrumentSlug === 'octatrack') return <OctatrackPanel />;
if (instrumentSlug === 'cascadia') return <CascadiaPanel />;
return <EvolverPanel />;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Extend InstrumentConfigSchema with sampler/sequencer/midi_sequencer optional flags + flip schema test todos to real assertions</name>
  <read_first>
    - src/lib/content/schemas.ts (lines 73-85 — current InstrumentConfigSchema)
    - src/lib/content/__tests__/schemas.test.ts (lines 364-436 — existing InstrumentConfigSchema tests including the Wave 0 it.todo stubs)
    - src/lib/content/__tests__/__fixtures__/instruments/octatrack/instrument.json (created in Wave 0 Task 0.1)
    - src/lib/content/__tests__/__fixtures__/instruments/cascadia/instrument.json (backward-compat reference)
    - .planning/phases/25-*/25-RESEARCH.md §"Pattern 3: Capability Schema Extension" (Option A chosen — existing field names preserved)
  </read_first>
  <behavior>
    - Octatrack fixture JSON parses without throwing: `InstrumentConfigSchema.parse(octatrackFixture)` returns an object where `sampler === true`, `sequencer === true`, `midi_sequencer === true`, `sysex === false`, `patch_memory === false`
    - Evolver/Cascadia fixtures still parse (backward compat): their `sampler` field is `undefined`, not throwing
    - Schema rejects non-boolean values in new fields: `{...cascadiaFixture, sampler: "true"}` throws ZodError
    - The three new `it.todo('accepts valid Octatrack config...')` stubs from Wave 0 Task 0.2 are now real `it(...)` tests with assertions against the fixture
  </behavior>
  <action>
    Step 1 — Edit `src/lib/content/schemas.ts`, replacing the `InstrumentConfigSchema` export block (currently lines 73-83) with:

    ```typescript
    // Instrument configuration schema (Phase 7: multi-instrument support; Phase 25: sampler flags)
    export const InstrumentConfigSchema = z.object({
      display_name: z.string(),
      tagline: z.string(),
      manufacturer: z.string(),
      sysex: z.boolean(),                       // Phase 7 — MIDI SysEx capability (Evolver has, Cascadia/Octatrack don't)
      patch_memory: z.boolean(),                // Phase 7 — patch-dump memory (Evolver has, Cascadia/Octatrack don't)
      sampler: z.boolean().optional(),          // Phase 25 / D-08 — has_sampler (Octatrack: true)
      sequencer: z.boolean().optional(),        // Phase 25 / D-08 — has_sequencer (Octatrack: true)
      midi_sequencer: z.boolean().optional(),   // Phase 25 / D-08, D-10 — has_midi_sequencer (Octatrack: true — informational for now)
      reference_pdfs: z.array(z.object({
        label: z.string(),
        file: z.string(),
      })),
    }).passthrough();
    ```

    Keep the `export type InstrumentConfig = z.infer<typeof InstrumentConfigSchema>;` line below unchanged.

    Step 2 — Edit `src/lib/content/__tests__/schemas.test.ts`. Find the three `it.todo(...)` entries added by Wave 0 Task 0.2 inside `describe('InstrumentConfigSchema', ...)` and replace them with real tests. Exact replacements:

    Replace `it.todo('accepts valid Octatrack config with sampler/sequencer/midi_sequencer flags');` with:
    ```typescript
    it('accepts valid Octatrack config with sampler/sequencer/midi_sequencer flags', () => {
      const data = JSON.parse(
        require('fs').readFileSync(
          require('path').join(__dirname, '__fixtures__/instruments/octatrack/instrument.json'),
          'utf-8'
        )
      );
      const result = InstrumentConfigSchema.parse(data);
      expect(result.display_name).toBe('Octatrack MKII');
      expect(result.manufacturer).toBe('Elektron');
      expect(result.sampler).toBe(true);
      expect(result.sequencer).toBe(true);
      expect(result.midi_sequencer).toBe(true);
    });
    ```

    Replace `it.todo('accepts Octatrack config where sysex=false and patch_memory=false');` with:
    ```typescript
    it('accepts Octatrack config where sysex=false and patch_memory=false', () => {
      const data = JSON.parse(
        require('fs').readFileSync(
          require('path').join(__dirname, '__fixtures__/instruments/octatrack/instrument.json'),
          'utf-8'
        )
      );
      const result = InstrumentConfigSchema.parse(data);
      expect(result.sysex).toBe(false);
      expect(result.patch_memory).toBe(false);
    });
    ```

    Replace `it.todo('treats missing sampler flag as undefined (backward compat for evolver/cascadia)');` with:
    ```typescript
    it('treats missing sampler flag as undefined (backward compat for evolver/cascadia)', () => {
      const cascadiaLike = {
        display_name: 'Cascadia',
        tagline: 'West coast modular synthesis exploration',
        manufacturer: 'Intellijel',
        sysex: false,
        patch_memory: false,
        reference_pdfs: [],
      };
      const result = InstrumentConfigSchema.parse(cascadiaLike);
      expect(result.sampler).toBeUndefined();
      expect(result.sequencer).toBeUndefined();
      expect(result.midi_sequencer).toBeUndefined();
    });
    ```

    Step 3 — Do NOT modify any consumer code that reads `instrumentConfig.sysex` or `instrumentConfig.patch_memory` — those reads are unchanged. New flags are additive; existing consumers are not yet reading them (Wave 2 / Wave 4 may start).

    Step 4 — Do not add `has_` prefixes. RESEARCH §"Pattern 3" chose Option A explicitly (keep field names without `has_` for consistency with existing `sysex`/`patch_memory`).
  </action>
  <verify>
    <automated>npm test -- --run src/lib/content/__tests__/schemas.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/content/schemas.ts` contains the exact strings `sampler: z.boolean().optional()`, `sequencer: z.boolean().optional()`, `midi_sequencer: z.boolean().optional()` (grep all three)
    - `src/lib/content/schemas.ts` still contains `sysex: z.boolean()` and `patch_memory: z.boolean()` (existing fields unchanged — not made optional)
    - `src/lib/content/__tests__/schemas.test.ts` contains the exact string `'accepts valid Octatrack config with sampler/sequencer/midi_sequencer flags'` (the renamed real test)
    - `npm test -- --run src/lib/content/__tests__/schemas.test.ts` exits 0
    - Test output shows ≥ 3 new passing Octatrack-related tests in `InstrumentConfigSchema` block (not `.todo`)
    - `npm test -- --run` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>
    Schema extended, three Wave 0 todos replaced with real passing tests, backward compat preserved, no consumer code changed yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Add Octatrack color primitives + [data-instrument="octatrack"] cascade block to globals.css + tokens.test.ts assertions</name>
  <read_first>
    - src/app/globals.css (full file — need to place primitives inside `@theme {}` block and cascade block after the cascadia cascade block)
    - src/app/__tests__/tokens.test.ts (existing cascade-block tests for evolver/cascadia — model the octatrack assertions after those)
    - .planning/phases/24-instrument-color-identity/24-01-SUMMARY.md (exact OKLCH values used for Evolver and Cascadia as precedent)
    - .planning/phases/25-*/25-RESEARCH.md §"Pattern 4: Color Cascade Block (Elektron Orange)" + §"Example 3: Color cascade block"
    - .planning/phases/25-*/25-CONTEXT.md §D-12..D-15
  </read_first>
  <behavior>
    - `@theme { ... --color-orange-500: oklch(0.72 0.16 40); --color-orange-400: oklch(0.80 0.12 42); ... }` primitives exist in globals.css
    - `[data-instrument="octatrack"] { ... }` cascade block exists with overrides for `--color-accent`, `--color-param`, `--color-bg`, `--color-sunken`, `--color-surface`, `--color-surface-raised`, `--color-overlay`, `--color-border`, `--color-border-subtle`
    - Chroma stays low (~0.03-0.04) on surface tokens per D-14; accent uses the orange-500 primitive; param uses orange-400
    - tokens.test.ts contains real tests (not `.todo`) asserting both primitives and the cascade block exist with hue in range [35, 45]
  </behavior>
  <action>
    Step 1 — Edit `src/app/globals.css`.

    Inside the `@theme { ... }` block, find the section with `/* Cascadia steel gray (Intellijel aluminum) */` around line 18. Add IMMEDIATELY AFTER the `--color-steel-400` line:

    ```css
      /* Octatrack Elektron orange (Phase 25 / D-12, D-13) */
      --color-orange-500: oklch(0.72 0.16 40);
      --color-orange-400: oklch(0.80 0.12 42);
    ```

    After the `[data-instrument="cascadia"] { ... }` block ends (just before the `/* Card base */` comment around line 127), insert this new cascade block:

    ```css
    [data-instrument="octatrack"] {
      --color-accent: var(--color-orange-500);
      --color-param: var(--color-orange-400);
      /* Warm orange-tinted surfaces (Elektron identity — D-14) */
      --color-bg: oklch(0.12 0.03 40);
      --color-sunken: oklch(0.10 0.03 40);
      --color-surface: oklch(0.16 0.03 40);
      --color-surface-raised: oklch(0.20 0.03 40);
      --color-overlay: oklch(0.24 0.03 40);
      --color-border: oklch(0.25 0.03 40);
      --color-border-subtle: oklch(0.20 0.025 40);
    }
    ```

    Step 2 — Edit `src/app/__tests__/tokens.test.ts`. Add new test cases (NOT `.todo` — real assertions) inside the appropriate `describe` block. Models:

    ```typescript
    describe('Phase 25 — Octatrack color identity', () => {
      const css = readFileSync(join(process.cwd(), 'src/app/globals.css'), 'utf-8');

      it('declares --color-orange-500 primitive in @theme block', () => {
        expect(css).toMatch(/--color-orange-500:\s*oklch\(0\.72 0\.16 4[0-5]\)/);
      });

      it('declares --color-orange-400 primitive (lighter param derivative) in @theme block', () => {
        expect(css).toMatch(/--color-orange-400:\s*oklch\(0\.80 0\.12 4[0-5]\)/);
      });

      it('[data-instrument="octatrack"] cascade block exists', () => {
        expect(css).toMatch(/\[data-instrument="octatrack"\]\s*\{[^}]*--color-accent:\s*var\(--color-orange-500\)/);
      });

      it('octatrack cascade block sets --color-accent, --color-param, and all 5 surface tokens', () => {
        const blockMatch = css.match(/\[data-instrument="octatrack"\]\s*\{([^}]+)\}/);
        expect(blockMatch).not.toBeNull();
        const block = blockMatch![1];
        expect(block).toContain('--color-accent');
        expect(block).toContain('--color-param');
        expect(block).toContain('--color-bg');
        expect(block).toContain('--color-sunken');
        expect(block).toContain('--color-surface');
        expect(block).toContain('--color-surface-raised');
        expect(block).toContain('--color-overlay');
      });

      it('octatrack surface tokens use warm hue 35-45 (Elektron orange range)', () => {
        const blockMatch = css.match(/\[data-instrument="octatrack"\]\s*\{([^}]+)\}/);
        expect(blockMatch).not.toBeNull();
        const block = blockMatch![1];
        // All oklch values in the block must use hue in [35, 45]
        const oklchValues = block.match(/oklch\([^)]+\)/g) || [];
        expect(oklchValues.length).toBeGreaterThan(0);
        for (const v of oklchValues) {
          const hueMatch = v.match(/oklch\([^ ]+ [^ ]+ (\d+(?:\.\d+)?)\)/);
          if (hueMatch) {
            const hue = parseFloat(hueMatch[1]);
            expect(hue).toBeGreaterThanOrEqual(35);
            expect(hue).toBeLessThanOrEqual(45);
          }
        }
      });

      it('octatrack surface chroma stays below 0.04 (avoid visual noise per D-14)', () => {
        const blockMatch = css.match(/\[data-instrument="octatrack"\]\s*\{([^}]+)\}/);
        const block = blockMatch![1];
        // Check explicit surface tokens: --color-bg/sunken/surface/surface-raised/overlay/border
        const surfaceLines = block.match(/--color-(bg|sunken|surface|surface-raised|overlay|border(?:-subtle)?):\s*oklch\([^)]+\)/g) || [];
        expect(surfaceLines.length).toBeGreaterThanOrEqual(5);
        for (const line of surfaceLines) {
          const chromaMatch = line.match(/oklch\([^ ]+ ([^ ]+) [^)]+\)/);
          if (chromaMatch) {
            const chroma = parseFloat(chromaMatch[1]);
            expect(chroma).toBeLessThanOrEqual(0.04);
          }
        }
      });
    });
    ```

    If the existing `tokens.test.ts` file uses different import conventions (e.g., `node:fs` vs `fs`), adapt imports to match the existing style — do not introduce a new convention.

    Step 3 — Do NOT modify the @theme semantic-alias block (`--color-accent`, `--color-param`, etc. at default/neutral-route level). The orange primitives are referenced ONLY inside the cascade block, keeping the neutral base route using `--color-neutral-*` per Phase 24.
  </action>
  <verify>
    <automated>npm test -- --run src/app/__tests__/tokens.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains the exact string `--color-orange-500: oklch(0.72 0.16 40)` (grep)
    - `src/app/globals.css` contains the exact string `--color-orange-400: oklch(0.80 0.12 42)` (grep)
    - `src/app/globals.css` contains the exact string `[data-instrument="octatrack"]` (grep)
    - `src/app/globals.css` contains `--color-accent: var(--color-orange-500)` inside the octatrack block (grep multi-line)
    - The octatrack cascade block contains all 7 surface-token overrides (bg, sunken, surface, surface-raised, overlay, border, border-subtle)
    - `src/app/__tests__/tokens.test.ts` contains the test name string `'[data-instrument="octatrack"] cascade block exists'` OR equivalent assertion
    - `npm test -- --run src/app/__tests__/tokens.test.ts` exits 0
    - Test output shows ≥ 5 new passing Octatrack token tests (not `.todo`)
    - Existing Phase 24 tests for evolver/cascadia cascade blocks still pass (no regressions)
  </acceptance_criteria>
  <done>
    Octatrack primitives and cascade block in globals.css, tokens.test.ts asserts their presence + hue range + chroma limit, full test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Iterate Octatrack OKLCH values until validate-contrast.mjs PASSES every octatrack pairing</name>
  <read_first>
    - scripts/validate-contrast.mjs (as updated in Wave 0 Task 0.4 — currently has starter values that MAY fail WCAG)
    - src/app/globals.css (as updated in Task 1.2 — starter values)
    - .planning/phases/25-*/25-RESEARCH.md §"Pitfall 3" + §"Pattern 4"
    - .planning/phases/24-instrument-color-identity/24-01-SUMMARY.md (Phase 24 approach — they iterated too)
  </read_first>
  <action>
    Step 1 — Run `node scripts/validate-contrast.mjs` and inspect the output. Record which octatrack pairings FAIL. Expected trouble spots:
    - `octatrack-accent on octatrack-surface-raised` (lightness 0.20 vs accent 0.72 — usually borderline)
    - `octatrack-param on octatrack-surface-raised` (param 0.80 vs 0.20 — should be OK but chroma matters)
    - `text on octatrack-bg` (text 0.93 vs 0.12 — should pass comfortably)

    Step 2 — If any octatrack pairing FAILS 4.5:1:
    - For accent pairings: increase `--color-orange-500` lightness (try 0.75, 0.78, up to 0.82) while keeping chroma ≤ 0.16 to stay in sRGB gamut. Hue stays in [35, 45].
    - For param pairings: increase `--color-orange-400` lightness (try 0.82, 0.85) with chroma ≤ 0.12.
    - For surface contrast issues: generally NOT the issue — text/muted on octatrack surfaces should pass at chroma 0.03 because lightness deltas are huge (0.93 vs 0.12 = ratio ~14:1).

    Step 3 — When you change a value, update it in BOTH places simultaneously (keep them synchronized):
    - `src/app/globals.css` (the `@theme` primitive and/or the cascade block)
    - `scripts/validate-contrast.mjs` `TOKEN_MAP` (the corresponding `octatrack-*` entries)

    Step 4 — Also update `src/app/__tests__/tokens.test.ts` regex bounds if lightness moved outside the starter range (e.g., if accent lightness changes from 0.72 to 0.78, the regex `oklch\\(0\\.72 0\\.16 4[0-5]\\)` won't match — relax to `oklch\\(0\\.(7[2-9]|8[0-5]) 0\\.1[2-7] 4[0-5]\\)`). Keep assertions meaningful — don't make them vacuously true.

    Step 5 — Rerun `node scripts/validate-contrast.mjs`. Iterate steps 2-4 until ALL octatrack pairings output `PASS`. Also confirm gamut check reports octatrack-accent and octatrack-param are in sRGB gamut (not clamped).

    Step 6 — Rerun `npm test -- --run src/app/__tests__/tokens.test.ts` to confirm the relaxed regex still matches the final values.

    Step 7 — Commit the final values. Record the final OKLCH tuple for accent and param in the plan SUMMARY so future phases can reference them.

    Max iterations expected: 2-3. If you cannot find values that pass after 5 iterations, something is wrong with the validator logic — escalate.
  </action>
  <verify>
    <automated>node scripts/validate-contrast.mjs 2>&1 | grep -E "octatrack|All pairings" | tail -15; echo "exit=$?"; node scripts/validate-contrast.mjs &gt; /dev/null 2&gt;&amp;1; echo "validator exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-contrast.mjs` exits with code 0 (every PAIRING passes WCAG AA 4.5:1)
    - Validator output contains the substring `octatrack-accent on octatrack-bg:` followed by a numeric ratio ≥ 4.5 and the word `PASS`
    - Validator output contains the substring `octatrack-accent on octatrack-surface-raised:` followed by `PASS`
    - Validator output contains the substring `octatrack-param on octatrack-surface:` followed by `PASS`
    - Validator gamut check reports `octatrack-accent: in sRGB gamut` (NOT `OUT OF sRGB GAMUT`)
    - Validator gamut check reports `octatrack-param: in sRGB gamut`
    - The final OKLCH tuple in `scripts/validate-contrast.mjs` for `octatrack-accent` matches the tuple in `src/app/globals.css` `--color-orange-500` exactly (same lightness / chroma / hue)
    - Same synchronization check for `octatrack-param` ↔ `--color-orange-400`
    - `npm test -- --run src/app/__tests__/tokens.test.ts` still exits 0 after any regex-bound adjustments
  </acceptance_criteria>
  <done>
    Every octatrack contrast pairing passes WCAG AA. Final OKLCH values synchronized across globals.css, validate-contrast.mjs, and tokens.test.ts. sRGB gamut clean. Values recorded for SUMMARY.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.4: Verify panel route resolves octatrack + add regression test if missing</name>
  <read_first>
    - src/app/instruments/[slug]/panel/page.tsx (confirmed to have octatrack branch per interfaces block — verify)
    - src/app/instruments/[slug]/panel/standalone-panel-client.tsx (confirmed to have octatrack branch per interfaces block — verify)
    - src/components/octatrack-panel.tsx (reads-only — D-22 panel frozen)
    - Look for any existing test file: `src/app/instruments/**/panel/**/__tests__/*.ts*` and `src/components/__tests__/standalone-panel-client.test.tsx` if it exists
    - .planning/phases/25-*/25-CONTEXT.md §D-06 + §D-22
  </read_first>
  <behavior>
    - `PANEL_CONFIG.octatrack` exists in page.tsx with title, description, maxWidth
    - `StandalonePanelClient` renders `<OctatrackPanel />` when `instrumentSlug === 'octatrack'`
    - A regression test exists that asserts this mapping — either the test already exists or is created here
    - Panel files (`src/components/octatrack-panel.tsx`, `src/lib/octatrack-panel-data.ts`) are NOT touched (D-22)
  </behavior>
  <action>
    Step 1 — Read `src/app/instruments/[slug]/panel/page.tsx` and `src/app/instruments/[slug]/panel/standalone-panel-client.tsx`. Confirm both have octatrack branches. Per the interfaces block above, both already resolve octatrack. If either does NOT have an octatrack branch (unlikely — discovery said they do), add the branch using the same pattern as cascadia.

    Step 2 — Check for an existing test file for `standalone-panel-client`. Use `ls src/components/__tests__/standalone-panel-client.test.tsx` or similar.

    Step 3 — If NO test file exists for standalone-panel-client, create one at `src/components/__tests__/standalone-panel-client.test.tsx` (note: the component lives under `src/app/instruments/[slug]/panel/` but the test can live in a co-located `__tests__` or in `src/components/__tests__` — match existing project convention. Check other component test locations; default to `src/app/instruments/[slug]/panel/__tests__/standalone-panel-client.test.tsx`).

    Test content (adapt imports to match existing conventions):
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import { StandalonePanelClient } from '../standalone-panel-client';

    describe('StandalonePanelClient — instrument panel resolution', () => {
      it('renders EvolverPanel for instrumentSlug="evolver"', () => {
        const { container } = render(<StandalonePanelClient instrumentSlug="evolver" />);
        expect(container.querySelector('svg')).not.toBeNull();
      });

      it('renders CascadiaPanel for instrumentSlug="cascadia"', () => {
        const { container } = render(<StandalonePanelClient instrumentSlug="cascadia" />);
        expect(container.querySelector('svg')).not.toBeNull();
      });

      it('renders OctatrackPanel for instrumentSlug="octatrack" (D-06)', () => {
        const { container } = render(<StandalonePanelClient instrumentSlug="octatrack" />);
        expect(container.querySelector('svg')).not.toBeNull();
      });

      it('falls back to EvolverPanel for unknown slug', () => {
        const { container } = render(<StandalonePanelClient instrumentSlug="unknown-instrument" />);
        expect(container.querySelector('svg')).not.toBeNull();
      });
    });
    ```

    Step 4 — Verify no changes to panel source of truth (D-22). Use `git status` and confirm `src/components/octatrack-panel.tsx` and `src/lib/octatrack-panel-data.ts` are NOT in the modified set from this task.

    Step 5 — Run the new/existing test to confirm it passes against the current code.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(panel|PANEL|Panel)" | head -10; echo '---'; git status --porcelain src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/instruments/[slug]/panel/page.tsx` contains the string `octatrack:` inside `PANEL_CONFIG`
    - `src/app/instruments/[slug]/panel/standalone-panel-client.tsx` contains the string `instrumentSlug === 'octatrack'` AND `<OctatrackPanel />`
    - A test file exists that asserts the octatrack-slug → OctatrackPanel mapping (either pre-existing or newly created by this task)
    - The test file contains the string `'octatrack'` in an assertion block
    - `npm test` passes the new/confirmed panel resolution tests (grep the output for the test names)
    - `git status --porcelain src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts` returns EMPTY (D-22 — panel files untouched)
  </acceptance_criteria>
  <done>
    Panel route resolution for octatrack is protected by a regression test. Panel source files are not touched. Full suite green.
  </done>
</task>

</tasks>

<verification>
Wave 1 exit gate:
1. `npm test` exits 0 — all tests pass, including Wave 0 todos that have now been flipped to real tests (schemas + tokens)
2. `node scripts/validate-contrast.mjs` exits 0 — every pairing PASSES WCAG AA 4.5:1, octatrack-accent and octatrack-param in sRGB gamut
3. `npm run validate-content` exits 0 — schema still valid for existing 6 octatrack sessions and for evolver/cascadia content (backward compat)
4. `git diff --stat src/components/octatrack-panel.tsx src/lib/octatrack-panel-data.ts` is empty (D-22 guard)
5. Visual spot-check (manual, optional — becomes real during Wave 2): open any `/instruments/octatrack/...` route in dev — expect subtle warm orange tint on surfaces and Elektron-orange accent on links/headings
</verification>

<success_criteria>
- InstrumentConfigSchema extended with three new optional flags; existing flags unchanged; backward compat preserved
- Three Wave 0 schema test `it.todo`s replaced with real passing tests referencing the Octatrack fixture
- globals.css has orange-500 and orange-400 primitives in `@theme` and a `[data-instrument="octatrack"]` cascade block with 9 overrides (accent, param, 5 surfaces, 2 borders)
- tokens.test.ts has ≥ 5 new passing tests for the octatrack cascade (hue range + chroma limit + presence)
- validate-contrast.mjs PASSES every octatrack pairing; OKLCH values synchronized across 3 locations (globals.css, validate-contrast.mjs, tokens.test.ts regex bounds)
- Panel route resolution for octatrack regression-protected by a test
- Panel source files unchanged (D-22)
- Full test suite green, validate-content green, validate-contrast green
- Record final OKLCH tuples in SUMMARY so downstream plans can reference
</success_criteria>

<output>
After completion, create `.planning/phases/25-*/25-01-SUMMARY.md` recording: final OKLCH values for --color-orange-500 and --color-orange-400 (exact lightness/chroma/hue tuple), number of iterations needed to land WCAG-passing values, schema field names chosen (confirmed `sampler`/`sequencer`/`midi_sequencer` without `has_` prefix per Option A), any adjustments to tokens.test.ts regex bounds, and confirmation that panel source files were untouched.
</output>
