---
phase: quick-260419-0b2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ikarie-panel.tsx
  - src/lib/ikarie-panel-data.ts
autonomous: true
requirements: [QUICK-01]
must_haves:
  truths:
    - "The RESONANCE fader renders at a diagonal angle (~35 degrees) matching the physical Ikarie panel"
    - "The RESONANCE label appears alongside the diagonal fader track"
    - "All existing controls continue to render correctly"
    - "Existing tests pass without modification"
  artifacts:
    - path: "src/components/ikarie-panel.tsx"
      provides: "DiagonalSliderComponent with rotation support"
      contains: "DIAGONAL_SLIDER_ANGLE"
    - path: "src/lib/ikarie-panel-data.ts"
      provides: "Control positions (unchanged structure)"
  key_links:
    - from: "src/components/ikarie-panel.tsx"
      to: "src/lib/ikarie-panel-data.ts"
      via: "CONTROL_METADATA slider type triggers diagonal slider render for resonance"
      pattern: "DIAGONAL_SLIDER_ANGLE"
---

<objective>
Add diagonal fader rendering for the Ikarie RESONANCE control, matching the physical panel's distinctive angled fader layout.

Purpose: The Ikarie panel's most visually distinctive feature is its diagonally-mounted RESONANCE fader. The current code renders it as a standard vertical fader, which looks nothing like the real module.

Output: Updated panel component with diagonal slider rendering for the RESONANCE control, with the rotation angle stored as an adjustable constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ikarie-panel.tsx
@src/lib/ikarie-panel-data.ts
@.claude/skills/synth-panel-builder/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagonal slider rendering for RESONANCE fader</name>
  <files>src/components/ikarie-panel.tsx</files>
  <action>
Add a `DIAGONAL_SLIDER_ANGLE` constant at the top of the constants section, set to `-35` (upper-left to lower-right diagonal, matching the physical panel photo where the fader runs from upper-left down to lower-right).

Create a `DiagonalSliderComponent` function component (or modify `SliderComponent` to accept an optional `rotation` prop). The simplest approach is a new component that wraps the existing slider rendering in an additional `<g>` with `transform="rotate(DIAGONAL_SLIDER_ANGLE)"` applied at the control's center point. Specifically:

```
DiagonalSliderComponent:
- Same props as SliderComponent plus `angle: number`
- Outer <g> with id={id} and transform={`translate(${x}, ${y})`}
- Inner <g> with transform={`rotate(${angle})`} to rotate the track+thumb+highlight
- Track, thumb, and highlight rects render identically to SliderComponent but inside the rotated group
- Label "RESONANCE" rendered OUTSIDE the rotated group (so text stays horizontal/readable), positioned below the rotated fader area
```

In the main render loop's `case 'slider':` branch, check if the control is `slider-ikarie-resonance` — if so, render `DiagonalSliderComponent` with `angle={DIAGONAL_SLIDER_ANGLE}`. Otherwise render the standard `SliderComponent` (preserving backward compatibility for any future vertical sliders).

Also remove the invented section labels ("CONTROLS", "MODULATION", "I/O") from the SVG — these do not exist on the physical Ikarie panel silk-screen. The physical panel has only the IKARIE title at top and BASTL branding at bottom. Keep the divider lines if they help visually separate jack rows, but remove the `<text>` section labels. Also remove the "FOLLOW normalled to MOD" text — this is not printed on the panel.
  </action>
  <verify>
    <automated>cd /Users/albair/src/evolver && npx vitest run src/components/__tests__/ikarie-panel.test.tsx src/lib/__tests__/ikarie-panel-data.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - RESONANCE fader renders at a ~35-degree diagonal angle via SVG rotate transform
    - DIAGONAL_SLIDER_ANGLE constant exists and can be adjusted
    - Label remains readable (not rotated with the fader)
    - Invented section labels removed
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` passes for both ikarie test files
- Visual inspection: RESONANCE fader renders diagonally on the panel at `/instruments/ikarie/panel`
</verification>

<success_criteria>
- The RESONANCE slider renders at a diagonal angle matching the physical Ikarie panel
- The rotation angle is stored as a named constant (DIAGONAL_SLIDER_ANGLE = -35) for easy adjustment
- The label "RESONANCE" remains horizontal and readable
- No invented section labels appear on the panel
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260419-0b2-fix-ikarie-panel-layout-to-use-diagonal-/260419-0b2-SUMMARY.md`
</output>
